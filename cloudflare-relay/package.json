{
  "name": "tesela-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-knowledge sync relay for Tesela — Cloudflare Worker implementation. Same wire protocol as crates/tesela-relay.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250515.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.20.0"
  }
}
