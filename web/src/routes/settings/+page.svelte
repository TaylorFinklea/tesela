<script lang="ts">
  // `+page.ts` redirects to `/settings/general`. This file exists so
  // SvelteKit recognizes `/settings` as a valid route + still mounts
  // the parent `+layout.svelte` while the redirect is in flight.
</script>
