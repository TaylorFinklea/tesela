# Current State

> **Backlog/ready-queue → beads (`bd ready`) as of 2026-06-30 (pilot).** New actionable work is filed in beads, not roadmap Now or this file. `.beads/` is git-excluded (stealth, local-only); decisions/phases/loop-state stay prose. See AGENTS.md → "Task tracking — beads pilot".

## Loop state — fresh context: start here, then `bd ready`
- **FLEET WAVE 1 LANDED (2026-07-01, Fable-orchestrated): 8 beads merged to main + closed** — engc.1 single apply_import orchestrator (opus, adversarially equivalence-verified), pfix.1 recurrence fixture + biweekly drift both clients (sonnet + qwen reject→fix→approve), sclr.1 palette/truncation, rlyf.1 conformance caps (CI unblocked), tp0.1 rotation spec (Lead-approved), engc.3 loro pin, xq3 colon focus, hygb.1 roadmap surgery. BONUS root-cause fix tesela-sz8: same-ms presence re-set lost LWW at peers (EphemeralStore drops timestamp ties) — cursor_presence now 10/10, the "known flake" is DEAD. New beads: a7b (web remote-cursors test harness), 6c6 (server pick_free_port TOCTOU flakes). Fleet lessons in bd memory `fleet-worktree-dispatch` (pre-cut worktrees; cargo clean rule; pi impl unreliable / pi review fine). WAVES 2+2b LANDED 2026-07-02 (14 more beads): engc.5 residency audit (classification on qql + 3 proven #[ignore] landmine tests) · engc.2 dead scaffolding -251 · cmdd.1 explicit bootstrap · ows.1 step-1 lifecycle->core (step-2 Lead constraints on the bead) · ows.2 CLI-through-engine (flock-first) · pfix.3 NLP-lift fixture · pfix.4 canonical ValueType · ejn.1 auto-update · tp0.2 Keychain storage (qwen critical caught+fixed: verified-write-gated migration) · z1e+4mc iOS fixes · a7b (web 525/525, first green) · hygb.3/rlyf.4. Web suite fully green; salvage-finisher pattern proven (recovers quota-killed WIP + catches its bugs). WAVE 3 LANDED 2026-07-02 (12 more beads): fte PURE MAX-TREEID merged e4a61454 (adversarially approved; bead OPEN pending Taylor's live CF-relay device test — harness-deck `tesela/20260702-fleet-waves`) · tp0.3 multiuser key-hierarchy spec (ADR proposal = harness-deck ask) · cmdd.2 command manifest + GET /commands · ra7.3+rlyf.2 relay parity/race fixes · sclr.3+sclr.4 deposit efficiency · ows.3 MCP-through-engine (qwen reject = false positive, overridden w/ evidence) · qy4 unlinked refs · 6c6 harness flake KILLED · hygb.2/4 doc cleanup. iOS build 70 UPLOADED (waves 1-2b). WAVE 3b LANDED (pfix.2 recurrence-FFI verified, zpr inert iOS snapshot deposits — Mac no longer sole durability point, rlyf.3 RELAY FREEZE DECLARED — Taylor: flip worker-conformance to required in GitHub UI; hygb epic closed). iOS build 71 cutting (fte engine + FFI). WAVE 4 LANDED 2026-07-02 (14 beads merged): qql LAZY-LOAD (locked doc model lands; eviction follow-up b8v) · ug7 NLP-lift Rust+FFI on iOS · cmdd.3/4/5 (manifest-driven MCP+slash+keybind config; cmdd epic nearly done) · engc.4+7 · ejn trio (ejn epic CLOSED) · 9t0+1fe · 95h+rlyf.5 (rlyf epic CLOSED — RELAY FROZEN) · ya4 DECOMPOSED (7 children, kanban largely pre-built). FLEET ARC COMPLETE THROUGH WAVE 4d (2026-07-02, ~50 beads landed total): cib iOS palette merged; 4ju per-note apply lock landed via a post-engc.7/qql PORT (all three entry paths locked, record_local hammer test revert-proven). ONE ITEM PARKED: ows.1 step-2 — TWO adversarial rejects; attempt-3 Lead constraints on the bead (lifecycle props STAY in the typed container; rendering solved render-side; revert-discriminating acceptance tests); worktree .claude/worktrees/w4-ows1s2 holds the quarantined commits. DESKTOP REBUILT+RELAUNCHED (:62104, post-wave-4 — the mixed-fleet cross-tombstoning Taylor hit was the stale desktop binary; re-test requested). MULTIUSER ADR WRITTEN (decisions.md 2026-07-02, Taylor-approved). **HANDOFF 2026-07-03 (Fable 5 out for ~weeks — successor = Opus or Sonnet-5 Lead orchestrator; routing: sonnet=impl default, opus=Lead/convergence-critical, gpt-5.5=review+bounded impl via pi wrapper (limit RESET 07-03), opencode-go pool (qwen/glm/minimax) back ~07-05; dispatch pattern = bd memory `fleet-worktree-dispatch` + `workflow-agents-no-shared-tree-stash`; scorecard `~/.claude/model-scorecard.md`).**
- **ENGINE ARC COMPLETE (2026-07-03 evening): c7s MERGED 55610a47** (round-2 approve, ZERO findings; RelayTicker hand-spliced with 96y at merge — repair-then-health-stamps; Rust suites green on main; TeselaTests main-tree gate PASSED post-splice — main is fully verified on all three platforms). 96y merged 84b9f761. All wave worktrees removed (only the ancient `.worktrees/sync-live-debug` remains, predates this arc, deletable). **SHIPPED AT HANDOFF: iOS build 74 UPLOADED (bump pushed df7a4db1); desktop rebuilt + installed 11:35, verified stack-blocked on the Keychain Always-Allow dialog (gxf) — Taylor's click releases it (then confirm serving: `lsof -nP -p $(pgrep -f tesela-desktop) | grep LISTEN` + curl /notes/daily); build-74 product-test deck PUBLISHED (`tesela/20260703-build74-product-test`, 4 asks, same-block hard test = headline).** Successor: act on the deck answers (responses.json) — a sync-health screenshot in any problem note is the diagnostic gold; each finding becomes a bead.
- **THEN: desktop rebuild + iOS build 74 + deck.** `bash scripts/build-desktop.sh` (EXPECT the macOS Keychain prompt — tesela-gxf unfixed; if the app serves nothing after relaunch it is wedged on that dialog (tesela-bv8) — Taylor must click Always Allow) · `bash scripts/ios-testflight.sh` from repo root + commit the bump · publish the build-74 product-test report to harness-deck (drift HARD-test vs the strand fix incl. wedge-recovery-without-restart, sync-health panel truthfulness, NLP fixes j7g/fr1 on device, token colors incl. the p2 amber-vs-yellow verdict (tesela-x2g), colored phrase chips verdict, and the STILL-unanswered build-70/71 extras).
- **STANDING USER WORKAROUND until c7s ships:** sync wedge (devices stop seeing each other) → restart the apps; safe, edits ride snapshots.
- **2026-07-03 incident record (closed):** same-block hard test lost one side + fleet-wide wedge. Investigation (tesela-9iy closed, gpt-5.5 second-opinion approve-with-nits): NO twins, keep-winner EXONERATED; class = outbound deposit strand (see decisions.md 2026-07-03). Frozen evidence: `ai-scratch/tesela-evidence-20260703/` (loro .bins + db + diagnosis JSON); probe on branch `w7-conv`.
- **QUEUE after the gate (bd ready is triaged):** tesela-baa (Lead design, PROMOTED to required — per-edit splice authoring; blocked on c7s) · ya4.2 + ya4.3 (unblocked; kanban keyboard UX + sets/table) · b8v eviction (carries the mmos has_unbroadcast_tail residency note) · pfix.5 (Opus crate-boundary review BEFORE impl) · cmdd.6 · engc.6 (loro_engine split — do NOT run concurrently with other engine merges) · 6lh, x2g, bv8, gxf, 64g, mp0.4/5, 2tt · mmos lanes per spec §12 strictly after b8v. **HANDOFF FILLS (2026-07-03, ~2 months of runway):** tesela-nnm daily-driver cutover EPIC (P1, the north-star gate — its import-fidelity + Logseq-parity AUDIT lanes are agent-runnable discovery; its product ask goes to Taylor via harness-deck) · vp9 JQL query UX (XL, Lead decomposition first; coordinate w/ pfix.5) · 6yw GroupKey rotation impl (spec approved, crypto review bar) · ce5 offline lifecycle roll. Big arcs needing decomposition when reached: pfix.5 (post boundary-review), baa (post design), nnm children.
- HISTORY (2026-07-02): fte validated then drift regression 16:06 = the UNLOCKED apply race — the desktop binary (built 10:28) PREDATED the 4ju merge (12:59) and the phone was on build 71 (pre-4ju). Engine markdown showed the exact signature (desktop's own "nice"→"nic" one-char drop; iOS block node alive with its text ops dropped — matches xh4's 7-13-of-20-writes-dropped proof). REMEDY: desktop rebuilt from main 16:21 (FIRST end-to-end ffy verify — script passed) but WEDGED at launch: new binary signature → macOS Keychain auth dialog blocks tesela_sync keychain_load (tp0.2 GroupKey) → entire server unresponsive (stack-sampled; filed tesela-bv8 server-wedge-on-keychain + tesela-gxf dev-signing-reprompt). AWAITING TAYLOR: (1) Keychain "Always Allow" click; (2) iPhone → build 72+; (3) DRIFT RE-TEST = top ask on deck 20260702-build72-product-test — the 4ju-everywhere fleet must prove clean BEFORE any engine merge. WAVES 5/5b/5c/6a ALL LANDED 2026-07-02 (reviewed+verified+pushed): ffy (build-desktop Keychain fallback, minimax first-shot) · qr4 (leader-tree surface filter, minimax) · sclr.2 (content-hash parsed-blocks cache — touched-refresh 106→47ms @552-note / 1511→638ms @5k; UNBLOCKED the ya4 chain) · xh4 (hammer test now discriminates 0/15↔15/15 vs the 4ju lock, gpt-5.5 impl + sonnet-engineered fix) · ya4.1 (kanban data layer on executeQuery; review caught silent card-move no-op, reject→fix→approve) · pfix.6 (inline-span fixture + iOS code/strike/link; honesty follow-up tesela-6lh) · fa7 (Anytype colored phrase chips, both surfaces) · ect (note-scoped COALESCED live refresh; reject→fix→approve) · 3ze (QR Continue fixed = onPaired deferred past fullScreenCover teardown) · mmos.1 spec rev2 MERGED (glm+gpt reviews addressed; Fable Lead sign-off; 6-lane decomposition §12; b8v bead carries the has_unbroadcast_tail residency-independence note). OWS.1 STEP-2 APPROVED after FOUR adversarial rounds but MERGE-HELD on branch w5-ows1s2 (bead note has full state: clear_incompatible_child representation-class fix; ensure_mergeable_* proven live-doc-breaking via loro 1.13.6 source; suite 738/0 excl. pre-existing sigterm_triggers_validated_backup flake) — merge on Taylor's drift-clean verdict, then desktop rebuild + iOS build to ship it. iOS build 72 uploaded 13:55 (qql/ug7/4ju parity); BUILD 73 CUTTING (ect/3ze/pfix.6/fa7 = the QR + live-refresh + colored-chips build; deck retargets the re-test to it on upload). QUOTAS: opencode-go pool (qwen+glm+minimax, ONE shared weekly quota) AND openai-codex gpt-5.5 both DEAD until ~07-05 — all-native routing (bd memory fleet-worktree-dispatch). Scorecard + model-bench.jsonl updated for all wave dispatches. STILL PENDING TAYLOR (non-blocking): build 70/71 product-test extras (on the deck), GitHub worker-conformance required-check flip. NEXT: post-drift-verdict ows merge → desktop rebuild + build 74; then ya4.2/ya4.3 (unblocked), b8v (w/ mmos accessor note), pfix.5 (Opus crate-boundary review first), cmdd.6, engc.6, 6lh, bv8, gxf; mmos.cancel→…lanes after b8v. WAS-WAVE-4-DISPATCH: qql lazy-load (sonnet, convergence gate = un-ignore the engc.5 stubs) · ows.1 step-2 engine lifecycle (opus, Lead constraints binding) · ya4 Lead DECOMPOSITION (opus, kanban-web-first) · cib iOS palette over the manifest · cmdd.3/4/5 · ug7 NLP FFI hoist · engc.4+7 · 4ju · ejn.2/3/4 · 9t0+1fe · 95h+rlyf.5. 3 harness-deck asks await Taylor: fte device validation, multiuser-key ADR, root-binaries cleanup.
- **ARCH REVIEW DONE (2026-07-01, Fable 5): 9 ADRs in decisions.md 2026-07-01 + FULL beads re-triage — `bd ready` IS the marching order now.** 7 new epics (rlyf relay-freeze · engc engine-consolidation · cmdd commands-as-data · pfix parity-fixtures · ows one-writer · sclr scale · hygb hygiene) + children under ejn/tp0; 3 stale beads closed (ai5, ra7.2, fkg — all verified already-fixed/superseded); P1 head: engc.1 (single apply orchestrator, Opus-review gate), pfix.1 (recurrence biweekly-drift, user-visible), sclr.1 (palette missing ~52 of 552 notes, user-visible), rlyf.1 (worker-conformance CI is RED). **All 5 product asks ANSWERED 2026-07-01 (decisions.md 2026-07-01b):** pure max-TreeID APPROVED (fte unblocks after engc.1) · spine-first (cmdd+pfix before ya4) · kanban-web-first slice · Lua park ratified · multi-mosaic = ONE SERVER N MOSAICS committed (new epic tesela-mmos, gated on qql). Report closed (done).
- **Backlog/what-next is in beads.** `bd ready` (priority queue) → `bd show <id>` → `bd update <id> --claim` → do it, run the repo Verify (build/test) → `bd close <id> --reason "…"`. `bd create` for work discovered mid-task. **I own queue accuracy — close completed items myself; don't ask Taylor to.**
- **Branch `main` — pushed continuously by the orchestrator (push authorized for this role since 2026-07-02).** `.docs/ai/review/` + `.beads/` + `ai-scratch/` untracked; `AuthKey_*.p8` gitignored — NEVER commit.
- **Latest iOS = TestFlight build 72 cutting 2026-07-02** (qql/ug7/4ju engine parity; 71 = fte engine+FFI; 70 = waves 1-2b; 69 = ra7 P0.3b recovery-phrase screens). Desktop installed + serving (random loopback port — see landmines; was :52980 on 2026-07-02). Taylor is sole tester; cut iOS builds freely. **Build-69 recovery-phrase product test APPROVED on device 2026-07-02 (deck → done).** Older mp0 onboarding test `tesela/20260630-mp0-onboarding-confirm` still open.
- **Disk hygiene automated (2026-07-01):** iOS archives + `target/` (~47G) filled the disk mid-cut once; `cargo clean` recovered it. A launchd agent `com.tesela.cargo-sweep` now runs `cargo sweep --time 7 ~/git/tesela` daily 03:00 (script `~/.local/bin/tesela-cargo-sweep`, needs `cargo-sweep`). Not chezmoi-tracked yet. Landmine + details: bd memory `ios-build-disk-cargosweep`.
- **NOW: iOS onboarding epic `tesela-mp0` re-scoped + mp0.1 & mp0.2 SHIPPED (2026-06-30, Sonnet-5 Lead+Senior trial, Opus-gated).** Lead found 2 of 3 epic premises STALE (QR freeze + RelayTicker race already fixed); "block daily until wired" is wrong for local-first. Shipped: `mp0.1` relay-attach 3-way `displayState` (`85dcf0a8`); `mp0.2` onboarding "You're synced" confirmation via an `onPaired` closure, Settings-scoped out (`048d05b4`, TeselaTests 318/0). Queue: `mp0.3` short-code DOA + `mp0.5` scanner-stall nudge are **SUPERSEDED/gated by `tesela-ra7`** (the recovery-phrase model removes the server-URL dependency — don't re-ask Taylor about it, it's decided) · `mp0.4` no camera-perm recheck on return from Settings = the one still-standalone onboarding item (P3). Rationale + AppShell evidentiary correction in decisions.md. Sonnet 5 now Senior/Lead-capable on the scorecard (pair its Lead re-scopes with a fact-check — it over-states evidence; tree-mutating verifiers need an isolated worktree).
- **NOW (Opus-owned): epic `tesela-ra7` — Anytype recovery-phrase + QR pairing/identity model.** Fork (Taylor 2026-06-30): a BIP39 phrase that IS the GroupKey; subsumes mp0.3, `tesela-tp0` (Ed25519 = P2), roadmap step-4. Spec `phases/2026-06-30-recovery-phrase-spec.md`. ⚠️ E2E-crypto: TDD + adversarial review each slice. **P0 BACKEND DONE:** P0.1 crypto (`149a1ff3` BIP39⇔key + one-way discovery handle), P0.2a Rust relay `/discover` (`0de6d7de`), P0.2b CF Worker parity (`3e687610`, verified via `wrangler dev` conformance), P0.3a recover-from-phrase FFI (`e8f53c57` — phrase→disc→discover→**relay-only pairing code** that reuses the existing adopt path; `recovery_e2e` + conformance 25/25 green). **Both relays resolve `disc→group_id` with no server URL.** **P0 UI CODE DONE + SHIPPED (2026-07-01, Sonnet-5 impl ×2 parallel, Opus reviewed+gated):** `P0.3b` iOS Enter/Show recovery-phrase screens — build 69, replaces mp0.3's dead short-code path (`be9f68d5`); FFI Swift bindings regenerated (were stale since P0.3a) + drift-green. `P0.3c` web/desktop Show-phrase — `GET /sync/recovery-phrase` + devices-settings reveal (`d3b49a4d`, cargo+svelte green). **P0 COMPLETE — ra7.4 device-verified + CLOSED 2026-07-02** (was: pending on-device recover-from-phrase — **use the pre-filled default relay** (no override): `tesela-0ln` CF Worker is now DEPLOYED (`tesela-relay.finklea.workers.dev`, version `3acfda4f`, free-plan SQLite DOs, smoke 200/404) and the desktop RE-REGISTERED your group on it (register + snapshot deposit + live ops confirmed via `wrangler tail` 2026-07-01) so `disc→group_id` resolves on CF. ra7.5 closed pending a browser self-QA of the live reveal (needs a desktop rebuild-with-latest-web). **THEN (Taylor's sequencing): finish Loro convergence (y11 layer-2 + 49d garble repair).** Deferred children: zeroize, CF admin-disc-scrub, body-cap-conformance.
- **CF relay migration (2026-07-01, `tesela-8ce` in progress):** `tesela-0ln` DONE (CF live). Desktop moved to CF (its `~/Library/Application Support/tesela/desktop.toml` already targeted `finklea.workers.dev`; was pointing at a not-yet-deployed Worker — reconciles decisions.md 640's silent-`wrangler tail`). **Remaining:** (a) ~~prod admin secret~~ DONE 2026-07-01 — `TESELA_RELAY_ADMIN_TOKEN` set on the Worker (256-bit); the only retained copy is macOS Keychain service `tesela-relay-admin-token` (wrangler secrets are write-only). TODO: add that service to AGENTS.md API-keys table. (b) **re-pair iOS devices to CF** (they still hold the old relay URL); (c) desktop rebuilt+relaunched with latest web 2026-07-01 (build-desktop.sh; server on :63073). P0.3c mechanical self-QA PASSED: `GET /sync/recovery-phrase` → 200 + 24-word phrase; reveal UI markers present in the built bundle; svelte-check clean. Visual reveal = quick Taylor eyeball (not a blocker). Deploy detail in the `tesela-0ln` close note.
- **FINISH multi-device Loro convergence (Taylor's sequencing after ra7).** `tesela-y11` **SHIPPED 2026-07-01 (Opus, adversarial-reviewed before commit):** deterministic KEEP-WINNER disjoint-twin convergence (max-TreeID node kept, no text force-write → kills the transient split-brain + the `betaBbetaB` doubling), UNIFIED across the WS + authoritative-snapshot paths — which fixes a CONFIRMED **total-block-loss** when the two paths disagreed on the survivor (latent in pre-y11 code too). Full ADR + the adversarial findings in decisions.md 2026-07-01; tests `crates/tesela-sync/tests/relay_inbound_rebase.rs` (13 cases); `cargo test -p tesela-sync -p tesela-sync-ffi -p tesela-server` = 345 green. **⚠️ NOT device-verified yet** — recommend a real multi-device edit test on the live CF-relay before full trust. Follow-ups filed in bd: provably-convergent pure-max-TreeID (drops per-view modifiers + genuine-edit preference — product decision), verify per-note apply serialization (#4). Residual `already_resident` gate coupled to `tesela-qql` (landmine on that bead). **`tesela-49d` SHIPPED + DIAGNOSED CLEAN 2026-07-01 (closed):** `tesela repair-garbled-blocks [--apply]` (dry-run default, mosaic-locked) scans + collapses residual disjoint-lineage TWIN blocks via the same `twin_winners_for` rule as live sync (engine `scan_disjoint_twins`/`heal_disjoint_twins`). **Ran the read-only scan on the live mosaics (desktop briefly quit + relaunched): NO twin residue in `logseq` OR `default`; the suspect daily notes (06-30, 07-01) are clean — no concatenation.** So the convergence fixes + y11 already healed the residue; nothing to repair. Tool stays available for any future/other residue; a single-node UNION concatenation still can't be auto-split (unsound) — if one ever surfaces, name it and build a history-assisted manual helper. (Survey 2026-06-30: `roadmap.md`'s convergence "Now" is STALE — trust current-state + beads.)

## DONE — north-star arc: multi-device sync + presence + iOS NLP/capture (2026-06-29/30, builds 56→66)
Verified LIVE by Taylor across iPhone/iPad/desktop ("Holy crap it works"). Full detail in git + closed bd items; summary:
- **Sync convergence (the big one).** Garble (concatenated block text under concurrent edits) = a device authoring on a fresh DISJOINT Loro lineage. Fixed by **bootstrap-before-author**: import the relay's authoritative note as a shared base before the first `record_local`, wired into ALL server authoring entries incl. create paths (`285bc557`, `74fb4689`). Separate **deposit-strand** bug (broadcast cursor ahead of current `oplog_vv` → empty delta → no `PUT /ops`, phone edits stranded) fixed with a snapshot fallback, self-healing (`10aafd6c`, build 57). Also: past-day heal `cf212bee`; bootstrap-when-behind + relay `X-Tesela-Compaction-Seq` `131a1039`; convergent idempotent `write_block_text` `8171b0b8`.
- **Presence.** Device-labeled remote cursors; iOS block-level chips + peer-color block tint + un-clipped name flags; per-day cursors in the multi-day journal (`e5b5b90c`, `68c8fc18`, `3293861b`, `61ff7d13`). CF Worker presence relay deployed by Taylor. Compact capture sheet (a keyboard-avoidance over-correction was reverted, `319e14bb`).
- **iOS NLP / slash / capture parity with web.** Slash `/p1`, inline NLP (auto-lift on blur), and live token coloring now on EVERY surface — today, pages, past-day, and Capture (which gained a type/tag picker + add-time NLP). Capture & blocks share ONE resolver `PropertyRegistry.effectiveLiftRegistry` (builtins fallback ⇒ NLP works pre-sync; live wins when liftable) so they can't drift (`2ecb51e7`, `ec3d39aa`, `63255ad4`, `0c2bc21d`, `7c89f944`). Bare TRAILING dates lift the deadline (no "due" needed; mid-prose still requires intent word).
- **Hardening audit** (find→verify, 14 confirmed): fixes shipped; deferred items filed in bd (qql/fkg/9je/9t0/ug7 etc.).

## Landmines / how-to (don't re-learn)
- **Desktop redeploy when WEB (`web/src`) changed:** `cargo tauri build` does NOT rebuild the web (no `beforeBuildCommand`; `frontendDist=../web/build`). Use `scripts/build-desktop.sh` (npm build → cargo tauri build → install-desktop.sh) OR `cd web && npm run build` first — else a STALE web bundle ships (bit hard 2026-06-30). Rust-only change → plain `cargo tauri build` + `bash scripts/install-desktop.sh` is fine. (Memory: project_desktop_web_build_gotcha.)
- **Desktop server binds a RANDOM 127.0.0.1 port** (`TESELA_SERVER_BIND=127.0.0.1:0`), NOT 7474. install-desktop.sh probes the real port via lsof. Check live: `lsof -nP -p $(pgrep -f Tesela.app/Contents/MacOS/tesela-desktop|head -1)|grep LISTEN`.
- **iOS:** `scripts/ios-testflight.sh` rebuilds the Rust FFI for iOS + xcodegen + archive + auto-bumps `CFBundleVersion` in project.yml + uploads. SourceKit shows phantom cross-file / `No such module UIKit|XCTest` / `Cannot find type` errors — **trust `xcodebuild`, not the editor** (memory: project_ios_sourcekit_false_positives).
- **Dead branch `wip/concurrent-convergence-shared-base`** = the abandoned deterministic-seed approach (bootstrap-before-author / "Option A" won). Deletable.

## Shipped specs (historical — work done)
- `phases/2026-06-29-concurrent-convergence-spec.md` (convergence — shipped as Option A bootstrap-before-author).
- `phases/2026-06-29-ios-surface-parity-spec.md` (iOS slash/NLP/presence on pages+past-day — shipped).
- `phases/2026-06-27-relay-presence-spec.md` + `2026-06-27-multidevice-presence-spec.md` (presence — shipped).
