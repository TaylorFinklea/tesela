# Current State

## Branch
- `main` @ HEAD — **2026-06-19**. Clean tree; `.docs/ai/review/` is untracked (the 3 open-source arch-review reports, kept for reference).

## This session
- [x] **iOS: past dailies no longer dimmed** (build 34 → TestFlight). Dropped `.opacity(0.72/0.6)` on yesterday/past-daily blocks in `GrDailyView.swift` (live Graphite shell) — Taylor disliked the grayed/darker look. Legacy `DailyView`/`WorkspaceGridView` still dim but aren't live (`useLegacyShell` off); offered to strip those too.

## Plan
- [x] **Arch-review eval + hygiene batch.** Adversarially verified the 3 open-source review reports (`.docs/ai/review/`) vs real code (ultracode, Claude-only) — ~20% signal, ~80% cargo-culted team/SaaS advice. Acted on the real findings: C23 (backup in-place-restore 409 guard — only data-loss item) + hygiene C19/C20/C21/C24/C6 (CI clippy + svelte-check blocking, `cargo audit`, delete `tesela-loro-spike`, fix `AGENTS.md` Next.js→Svelte + crate count, drop drifted `default_types()`). Declined the rest. See git log `3fec1b62`/`9d5d9b7c`.
- [x] **iOS editor sprint → TestFlight builds 21–28** (all pushed + Opus-verified xcodebuild+tests). Marker unification (Agenda/Inbox), Enter-indent + empty-outdent + insert-after-cursor, word-wrap (real fix = `sizeThatFits`), capture target-swatch menu, `[[`/`#`/`/` inline autocomplete on ONE trigger-detection framework (`EditorAutocomplete`/`LinkSuggest.detectTrigger`; `LinkSuggestTests` 18/18), complete page+tag source via new FFI `SyncEngineHandle.index_entries()` (Loro index, fixes unmaterialized-note gap), Graphite Search view. See git log + `decisions.md` 2026-06-18.
- [x] **#64 mobile command palette** (build 29) — keyboard-toolbar "Commands" button (new `.commandPalette` item) → `GrCommandPalette` sheet over the `GrCommand` catalog (tab nav + Sync now + Settings), via a `\.openCommandPalette` env action → `GrAppShell.runCommand`. Insert verbs stay in `/` slash; block actions stay on the toolbar. `GrCommandTests` 5/5.
- [x] **#65 capture sheet footer clipped behind keyboard** (build 30) — text-path autofocus was racing the sheet present-transition; deferred to 320ms so the keyboard rises against a settled layout. Intermittent keyboard-timing → needs on-device confirm.
- **iOS editor track COMPLETE** — every reported item shipped (TestFlight builds 21–30, all pushed).
- [x] **Cross-device sync bug (#70) + durability P1** (2026-06-18). (a) Desktop /g didn't live-update on relay-pulled remote edits — `sync_relay::TickOutcome.applied_updates` now carries the applied bytes + the daemon re-broadcasts them (the post-apply re-export returned None). **DESKTOP-ONLY; deploy pending** (Tauri rebuild + /Applications swap — task #73; `web/build` already rebuilt). (b) ⚠ The scary one: an iOS capture sat unpushed to the relay for 2h (foreground-only push; background stranded the queue). **P1 shipped (build 31):** `RelayTicker.flushOnBackground()` drains the outbound queue in a `UIApplication` bg task before suspend. Taylor chose **"go big"** → full plan in `phases/2026-06-18-sync-durability-spec.md`.
- [x] **Sync durability P2a** (#71, build 32): BGProcessingTask catch-up (`app.tesela.ios.relay-catchup` → `RelayTicker.runBackgroundCatchup()`). Built via a **3-way pi head-to-head** (minimax-m3 won vs gpt-5.5 + qwen3.7-max; worktree-isolated; all 3 build-verified + scored).
- [x] **P2b = DEAD END** (#71): the relay PUT is Rust/`reqwest` (`transport/relay.rs:248`) called via FFI, NOT Swift `URLSession` — a background URLSession can't carry it, and it's redundant with P1 (~30s bg task) + P2a (periodic retry). Closed; no code.
- [x] **Sync durability P3a** (#72, build TBD this session): iOS APNs silent-push receiver — `AppDelegate` (via `@UIApplicationDelegateAdaptor`) registers for remote notifications, captures the token, and on a content-available push runs `runBackgroundCatchup()`. `UIBackgroundModes += remote-notification`. Built via a **4-way pi head-to-head** (minimax won vs gpt-5.5/qwen/kimi; ALL 4 build-verified + scored). **Not functional end-to-end** — see P3b/P3c + Taylor deps.
- [x] **Sync durability P3b/P3c CF Worker relay side** (#72, commit `a1121293`): `device_tokens` table + MAC-authed `POST /groups/:id/devices` in the per-group DO; `handlePutOp` fires a content-available APNs push to the group's other tokens via `apns.ts` (ES256-JWT+HTTP/2). Best-effort, no-ops without `Env.APNS_*`, zero-knowledge. `tsc` clean. apns.ts via a **3-way pi head-to-head** (qwen won — first win; all 3 typecheck-verified + scored).
- [x] **Sync durability P3b iOS token-POST + entitlement** (#72, build 36, commit `d05820ed`): `RelayClient.register_device` (MAC POST /devices, mirrors `ack`) → FFI `RelayClientHandle.registerDevice` (bindings regen, drift clean) → `RelayTicker.maybeRegisterApnsToken()` (pulls `AppDelegate.deviceTokenHex`, idempotent). `aps-environment` entitlement (device-only) archived clean → **Push IS enabled** for the App ID.
- [x] **CF Worker APNS secrets SET** (#72): `APNS_KEY_ID=C2DP446WQ9`, `APNS_TEAM_ID=K7CBQW6MPG`, `APNS_BUNDLE_ID=app.tesela.ios`, `APNS_KEY_P8` (gitignored `AuthKey_*.p8`; `*.p8` now in .gitignore). Deposit-safety hardened (push wrapped in try/catch). Dry-run clean.
- [x] **CF Worker deployed + validated** (Taylor deployed; `wrangler tail` confirmed live) — BUT the iOS devices sync via the **Rust self-host relay** (`pairing.relayUrl` = `127.0.0.1:60192` local / HA), NOT the CF Worker (tail was silent during a deposit). So the Worker APNs isn't in the device path.
- [x] **Rust `tesela-relay` APNs port** (#74, commit `19f350ef` + `typ=None` parity fix): `relay_device_tokens` + MAC-gated `POST /devices` + `apns.rs` (jsonwebtoken ES256 + reqwest http2) + spawned best-effort push from `put_op`; `APNS_*` clap+env config. 41/0 tests, clippy clean. **Adversarially reviewed (`w6m8o2we9`): code correct, no blocker.** + HA add-on wired (`config.yaml`/`run.sh` apns_* options, v0.3.0).
- **DECISION (2026-06-19): use the CF Worker NOW; HA is deferred.** Both relays are zero-knowledge (verified); the Worker is already deployed + secrets set + a public HTTPS URL (no Tailscale needed). The Rust/HA port is committed + ready for whenever full self-hosting is wanted.
- [ ] **⚠ Switch the devices to the CF Worker**: point `pairing.relayUrl` at `https://tesela-relay.finklea.workers.dev` (update `~/Library/Application Support/tesela/desktop.toml` `relay_url`, restart desktop, RE-PAIR iOS). Then test instant-sync.
- **⚠ CORRECTION (review `w6m8o2we9`): a TestFlight build = PRODUCTION APNs token** (distribution signing overrides the `development` entitlement string). So `APNS_HOST` must be **UNSET = production** (`api.push.apple.com`) — the CF Worker already is. The earlier `…sandbox…` guidance was WRONG (sandbox is only for Xcode-direct dev installs).
- **Test caveats (review):** test with the phone NOT in **hub mode** (Mac reachable over WS bypasses the relay → no deposit, no push; use cellular / Mac off). First push needs the OTHER device to have already registered its token (foreground tick). Diagnose via the relay's `[apns]` logs (`wrangler tail` for the Worker).
- [x] **Switched to CF Worker + fixed the real blocker** (2026-06-19, builds 36→38, commit `bb94742a`). Devices showed `413 Payload Too Large` on `put_envelope` — the CF Worker's `wrangler.toml` had the 1 MiB default; bumped `TESELA_RELAY_MAX_BODY` → 16 MiB (the same relay-413 fix the HA relay already had). After redeploy: **sync flows** (web→iPad fast). The 413 was ALSO blocking APNs (failed tick never reached token registration). Build 38 added an in-app **"APNs push"** diagnostic row (Settings → Sync) + `AppDelegate.lastRegistrationError`. Detour learned: entitlement file value is moot — `ExportOptions method=app-store-connect` re-signs to production aps-environment regardless.
- [x] **✅ APNs instant-sync WORKING end-to-end** (#72, 2026-06-19, build 39). `wrangler tail` confirmed the full chain on the CF Worker: both devices `registered ✓` (POST /devices), and a deposit fires `[apns] deposit seq=N → waking 1 → push <token>… → 200 OK` in BOTH directions (iPhone↔iPad), Apple accepting every push. The diagnostic row had to move to `GrSettingsView` (build 39) — build 38 put it in the unused legacy `SyncSettingsView`. Edit on one device → relay deposit → content-available push → other device catches up. (Suspended-device wake is the receiving side, P3a build 33+; relay→APNs delivery is the confirmed-hard part.)
- [ ] **#75 multi-device clobber** (flagged 2026-06-19, NOT urgent per Taylor): an iOS edit was overwritten by a web/iPad edit on the CF Worker. Family of the known convergence work. Reproduce + diagnose later; potential data loss — don't lose it.
- [x] **Dead-APNs-token prune** (2026-06-20, commit `dc9a8af5`, autonomous): review-flagged gap — a reinstalled device left a stale token pushed on every deposit forever. `sendApnsBackgroundPush` now returns delivered/dead/failed; the deposit path prunes `dead` (HTTP 410 / BadDeviceToken). Both relays (CF Worker `deleteDeviceTokenByToken` + Rust `delete_device_token` w/ unit test). cargo 42/0 + clippy + tsc clean. **⚠ CF Worker needs a `wrangler deploy` to go live (not urgent — only matters after a device reinstall).**
- [x] **3E code-block rendering = DONE** (roadmap was stale, verified 2026-06-20): web (`cm-decorations.ts` + `code-highlight.ts` syntax highlighter; `block-parser.ts segmentText`) + iOS (`BlockText.swift`) already render fenced code monospaced, tags/wikilinks excluded inside. Roadmap updated.

## Blockers
- None (sync durability P2/P3 are planned phases, not blockers).

## Open Questions
- Taylor verifying builds 28–31 on device (autocomplete, palette, capture-clip, **flush-on-background**: capture → immediately background → block reaches other devices without relaunching the phone).
- **#70 desktop deploy** (#73): rebuild the Tauri app + swap /Applications — Taylor's running-app environment, needs his OK/involvement.
- Sync durability: continue P2 → P3 (the rock-solid endgame) vs interleave other tracks.
