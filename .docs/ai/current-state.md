# Current State

> **Backlog/ready-queue → beads (`bd ready`) as of 2026-06-30 (pilot).** New actionable work is filed in beads, not roadmap Now or this file. `.beads/` is git-excluded (stealth, local-only); decisions/phases/loop-state stay prose. See AGENTS.md → "Task tracking — beads pilot".

## Branch
- `main` — pushed through `f7f31f32`+; newer doc/spec commits may be unpushed (delete-refresh fix, e2e test, multi-device spec, this). **Remind Taylor to push.** `.docs/ai/review/` + `AuthKey_*.p8` untracked (latter gitignored — NEVER commit).

## DONE this run (sync stabilization — all resolved)
- Liveness, date chip, web→iOS delete, iOS→desktop push, desktop crash-loop (loro 1.12→1.13.6), disjoint-lineage convergence, desktop delete-refresh (`38b6ac3b` + e2e `pnpm test:e2e`).
- **Convergence (Phase 0 / layer-2) CONFIRMED working**: the `.relay` rebase-catch-up already exists (iOS `catchUpFromRelaySnapshots` on pending → `import_authoritative_snapshot`). June 25/26 stuck-forks HEALED when Taylor edited them on desktop (re-broadcast → pending → catch-up). Clean days + today work. (Residual theoretical nuance: snapshot-via-normal-tick uses lossy min-TreeID dedup not rebase — deferred, #12; do NOT blind-rebase relay-inbound = ping-pong risk.)

## 🔴 NOW — TOP PRIORITY: complete concurrent multi-device convergence
- Presence (Phase 3b) is BUILT (CF Worker `70338b18`, desktop bridge `a0845bf4`, iOS Option B `8e04f01d`, build 56) but UNCOVERED a deeper data bug under concurrent editing. Presence is PARKED behind getting convergence solid.
- **Symptom (Taylor 2026-06-28/29):** concurrent multi-device editing of today's daily → garbled CONCATENATED block text (`Bothnice onenice one` = separate blocks' text merged into one bid) + persistent divergence. Sequential editing is fine.
- **Fixed this arc (committed):** past-day heal-deposit `cf212bee`; bootstrap-when-behind compaction watermark + relay `X-Tesela-Compaction-Seq` header `131a1039` (DEPLOYED to CF by Taylor); convergent idempotent `write_block_text` `8171b0b8` (within-lineage writes).
- **ROOT CAUSE (confirmed, the remaining bug):** `doc_for_note_mut` (loro_engine.rs:1528) creates a BARE fresh disjoint `LoroDoc::new()` per note — no seed, no relay bootstrap. Two devices first-authoring the same daily → DISJOINT lineages → text_seq UNION on merge → garble. `views_upsert` (loro_engine.rs:1928) already solves this class (import deterministic seed before first write = shared base); notes/dailies don't get that treatment.
- ✅ **FIX IMPLEMENTED (Option A — bootstrap-before-author) — `285bc557`, 2026-06-29.** iOS was ALREADY correct (RelayTicker recordAndPush/spliceAndPush await bootstrapNoteIfNeeded before every author). The **DESKTOP server** was the culprit: `routes/notes.rs` authored via `record_local` on a fresh disjoint doc. Fix: new `bootstrap_note_if_needed(state, slug)` — best-effort fetch relay authoritative snapshot + `import_authoritative_snapshot` (shared base) before the FIRST record_local for a non-resident note; deadlock-safe (no handle.state.write()), resident-gated, non-destructive. Wired into ALL authoring entries (fix-the-class): upsert_blocks, update_note, get_daily_note, delete_block, set_block_property, clear_block_property. Engine TDD (realistic random bids: garble repro + bootstrap cure) + integration test green; review closesProdGarble + deadlockSafe + approve. The deterministic-seed step-1 (parked on branch `wip/concurrent-convergence-shared-base`) was NOT used (Option A is cleaner, no stray placeholder). **Desktop rebuild in progress (`b0h6qgvlw`); NO new iPhone build (build 56 already correct).** ON-DEVICE VERIFY PENDING: edit on desktop a daily the iPhone created → clean merge, no garble.
- LOW follow-ups (tracked): create_note/rename/cleanup author without bootstrap (genuine first-create / resident / admin — low risk); doc_version memory-only → on-disk-not-resident same-bid un-broadcast edit could lose to server-wins rebase (untested edge).
- Existing garbled blocks (Taylor's today) need MANUAL cleanup (text recoverable, not lost); a gated one-shot repair is a possible later add.

### ✅ SEPARATE BUG FIXED — iOS deposits stranded (`10aafd6c`, 2026-06-29)
- **Symptom (live):** iPhone (Relay) records edits (`Last splice applied=1`) but `tick_outbound` sends **0 ops, no error, forever**; relay tail = ZERO `PUT /ops` from phone. Phone→desktop dead; desktop→phone fine. Edits durable on-device, just never left.
- **NOT a build-55/56 regression** (verified: neither `8e04f01d` presence nor `131a1039` convergence touched the deposit path; engine byte-identical). **Latent engine bug since build 54**, triggered by the convergence rebases.
- **ROOT CAUSE:** `export_doc_update` (loro_engine.rs:505) snapshot-fell-back only on an UNDECODABLE cursor / export-Err, NOT when the broadcast cursor decodes to a VV **at-or-ahead of** current `oplog_vv` (an authoritative import rebasing a note 'backward' leaves the committed cursor ahead). There `export(updates(ahead_vv))` = `Ok(empty)` → `Some(empty)` → ships a content-less frame forever.
- **FIX:** added `Ok(vv) if vv.includes_vv(&doc.oplog_vv()) => Snapshot` arm; disjoint cursors stay incremental. Engine fix → desktop + iOS. **SELF-HEALING:** stranded device snapshots on next produce tick; confirmed PUT rewrites a fresh cursor → back to incremental. Recovers already-stranded edits, no reinstall.
- TDD `produce_re_emits_snapshot_when_broadcast_cursor_is_ahead_of_current` (RED rendered `""` → GREEN snapshot); `cargo test -p tesela-sync` 175 green.
- **SHIPPED:** desktop rebuilt+installed (both this + garble fix, :53579). **iOS build 57** cutting (auto-bump 56→57, rebuilds Rust FFI). VERIFY: install 57 → type on phone → appears on desktop + `PUT /ops` fires.
- 🛠️ install-desktop.sh now probes the real (random) server port (`c074f4ac`) — the embedded server binds 127.0.0.1:0, NOT 7474 (that misdiagnosis caused much churn).

## NORTH STAR ARC: multi-device live presence + cursors (collab) — UNBLOCKED (sync solid 2026-06-29)
- ✅ **Multi-device sync VERIFIED LIVE 2026-06-29** (Taylor: "Holy crap it works") — iPhone + iPad + desktop converging; desktop renders both remote devices' cursors. Garble + deposit-strand both fixed (see NOW). Presence work resumed.
- ✅ **Phase 3 iOS presence rendering + device labels SHIPPED — `e5b5b90c`, 2026-06-29.** (a) iOS showed NO remote cursors — root cause: iOS only painted a caret inside the active editing UITextView (read-mode blocks had no caret layer). Fix: block-level presence chip on BlockRow (dot+device name, mirrors TagChip styling, read+edit mode, 2+"+N") + name flag on the edit caret + 3s prune timer (pruneStale was never called on iOS). (b) device labels: `name` field already on the wire (Rust opaque) — iOS sends `device.friendlyName`||UIDevice.name; desktop new `GET /info`→device_display_name(), web fetches once + stamps name (desktop renderer already drew it). iOS xcodebuild + 10 presence tests green; server clean; web check 0 err. **Shipped: desktop installed + iOS build 58.** VERIFIED LIVE by Taylor: works; iOS chip liked.
- ✅ **Presence polish — `68c8fc18` (build 59), 2026-06-29.** Taylor feedback: (a) desktop name flag was CLIPPED (each block = auto-height single-line CM editor, .cm-scroller ~24px overflow:auto coerces overflow-y→clip). Fix: SCOPED `EditorView.theme` (blockScrollerOverflow, overflow:visible) added to remoteCursorExtension — ONLY block editors, NOT the global baseTheme (that leaked to DocumentEditor/mode:document + would break its scroll — caught + re-scoped; live-verified doc page still scrolls + block flag un-clipped, coexisting). + flip-flag-below-caret near scroll-pane top. (b) iOS: tint the whole BlockRow with the peer color (fill ~0.12 + 3pt accent bar, mirrors GrChip) alongside the chip — clearer on wide iPad. **Desktop installed (:58959) + iOS build 59 on TestFlight.**
- ✅ **Sync/presence HARDENING (audit) + iOS SURFACE PARITY — 2026-06-29, builds 60 + 61.** Ran a find→verify integration audit (14 confirmed). Fixed (T1): create-path bootstrap-before-author (`74fb4689`, M1 — closes the create-path garble class: create_note/tag-page/rename/ensure_tag_pages); iOS presence robustness (`3293861b`, M5 flicker/M6 re-pair/L2 leak/L3 throttle); web multi-day presence (`61ff7d13`, M3 per-day slug + L4 name warm); desktop-release.sh path+web-rebuild (`eb4fd332`, H1/M7). **Build 60.** Then (T2, `2ecb51e7`, build 61) iOS surface parity per spec `phases/2026-06-29-ios-surface-parity-spec.md`: slash /p1 + inline NLP + live presence now on PAGES + yesterday/past-day (were plain TextField); NLP **auto-lifts on blur** (web parity); fuzzy slash filter; builtins-registry fallback. Engine splice seam was already slug-generic → clean per-surface splices, today path untouched. xcodebuild green; 65 tests. **VERIFY (on-device, builds 60+61):** desktop journal shows per-day remote cursors; iOS chips stable (no flicker); on a PAGE + PAST-DAY block tag #Task → /p1 sets priority + `due tomorrow`+blur auto-lifts.
- Audit deferred (low/structural, tracked): M2 doc_for_note_mut empty-doc-on-miss (gated by eviction work — hard prereq); L1 rebase_twins server-wins (narrow, add test); L5 same-offset flag overlap (cosmetic); L6 tombstone-survives-GC (test gap); scoreFuzzy/detectLifts unit tests; optional shared-Rust NLP parser to stop TS↔Swift drift.
- Spec: `phases/2026-06-27-multidevice-presence-spec.md`. loro 1.13.6 gives `EphemeralStore` (presence) + stable `Cursor` FREE (verified), not in FFI yet. Transport: WS broadcast (desktop real-time) ✅; CF relay is store-poll → iOS-over-relay needs a CF-DO WebSocket later.
- [x] **Phase 1: FFI-wrap Cursor + EphemeralStore** (#13) DONE — engine `b7d26e92` (mint/resolve_block_cursor op-anchored + cross-engine portable; EphemeralStore presence round-trip/multi-peer/LWW; 4 tests) + FFI `5b6a8bf3` (mint_cursor/resolve_cursor/set_presence/apply_presence/presence_peers + PresencePeer Record; FFI round-trip test). loro-internal = "1.13" added (EphemeralStore not in public loro). Full tesela-sync (166+) + ffi (29) green.
- [x] **Phase 2: desktop presence over WS** (#14) DONE — verified end-to-end (two-page Playwright `presence.spec.ts`: page A's caret → live `.cm-remote-cursor` on page B, over the WS, no relay/reload).
  - SERVER `cce9afa7`: presence rides the EXISTING ws_delta_tx binary fan-out by a `PRES` magic — route_inbound_binary splits PRES (fan out, never engine/persist) from TLR2. 4 ws tests.
  - WEB `e8374836`: loro/presence.ts (PRES codec) + remote-cursors.ts (peer→caret store, LWW/10s decay, per-tab id+color) + cm-remote-cursors.ts (StateField + RemoteCursorWidget; .map auto-remaps through local edits, rebuild on fresh presence) + ws-client onPresence routing + BlockEditor throttled caret publish + extension per bound block + +layout onPresence. svelte-check 0 err; web unit 514 + 11 new; e2e green.
  - ARCH locked: web uses PLAIN {bid,utf16_offset} + CodeMirror decoration auto-remap (NOT loro Cursor).
- ✅ **DONE: past-day relay convergence FIX (#12) — `cf212bee`, VERIFIED on-device 2026-06-28** (Taylor: "It is working"). Root cause = lazy-residency + the gated snapshot compaction (5-min cadence / inbound_cursor>0 / catchup-empty) depositing the heal-snapshot too late/never for cold notes. Fix: relay tick deposits each broadcast note's snapshot INERT (covers_seq=0) right after broadcasting, EXCLUDING failed/pending/catchup notes (clobber guard — a first attempt without it regressed tick_holds_cursor + was reverted). server suite 49 green.
- ✅ **DONE: multi-device DIVERGENCE fix — `131a1039`, 2026-06-28** (root-caused from a live `wrangler tail`). Cause: a device whose inbound cursor fell below the relay's compaction watermark NEVER re-bootstrapped in the ongoing tick (BOTH desktop Rust + iOS Swift) — `GET /ops?since=N` (N<compaction_seq) returns empty/partial with NO behind-signal (indistinguishable from caught-up), and `bootstrap_from_snapshots` only ran at bring-up. iPhone froze at seq 533 while relay head=541. Fix (4 parts): (1) relay `X-Tesela-Compaction-Seq` header on GET /ops (both relays, additive); (2) desktop poll reads it → tick calls bootstrap_from_snapshots when compaction_seq>cursor (deadlock guard: drop state.write() first; clobber guards: non-destructive merge, all-or-nothing jump, inbound-only); (3) iOS FFI surfaces it → RelayTicker bootstraps mid-run; (4) heal-throttle: per-note CONTENT-HASH dedup (kills the 21-PUT churn, new content always deposits). TDD red→green (tick_bootstraps_when_behind_compaction_watermark). Verified: tesela-server 53 + tesela-relay + ffi 34 + xcodebuild 289 green; review convergenceOk+approve. **Desktop build INSTALLED + serving (port 55339); iPhone build 56 cutting.** Test: reopen desktop + iPhone→56, edit both → stay converged + self-heal after falling behind. Follow-ups (low): CORS expose-header (only if a browser polls relay directly), strictly-< negative test.
- 🔭 **FOLLOW-UP (minor, non-blocking): iOS delete → desktop needs manual refresh.** Taylor 2026-06-28: deleting on iOS syncs but the DESKTOP web view doesn't re-render the removal without a refresh ("working well enough for now"). This is the web-UI reconcile gap (cousin of #10) — the delete propagates + persists; only the live desktop render lags. Revisit after presence.
- Desktop deploy (2026-06-28): `scripts/install-desktop.sh` swaps the freshly-built bundle into `/Applications` (graceful quit → backup to /tmp/Tesela-prev.app → swap → relaunch → verify). Allow-rule `Bash(bash scripts/install-desktop.sh:*)` (Taylor added). My agent `open` DOES launch a serving instance — fine to install + relaunch from here.
- ✅ **CORRECTED — the "launches don't serve" landmine was a PORT MISDIAGNOSIS.** The embedded tesela-server binds `127.0.0.1:0` (a RANDOM port — `src-tauri/src/main.rs:155 set_var TESELA_SERVER_BIND=127.0.0.1:0`), **NOT 7474**. All my "server won't serve / agent launches don't work / needs a human reopen" conclusions were me curl-ing 7474 (always empty). The server was up the whole time on a random port (verified: PID listens on e.g. :55339, /health=ok). install-desktop.sh now probes the real listening port via lsof (no more false "reopen" prompts). To check the desktop server: `lsof -nP -p $(pgrep -f Tesela.app/Contents/MacOS/tesela-desktop|head -1) | grep LISTEN` → curl that port's /health.
- 🟢 **ACTIVE: relay presence (Phase 3b)** — PIVOT: presence → the RELAY (daily-driver), not hub-mode. Verified contract + stage plan locked in `phases/2026-06-27-relay-presence-spec.md` (## VERIFIED CONTRACT, workflow `w4ljve190`). Reuses PRES codec + LoroPresence + RemoteCursorStore unchanged.
  - [x] **Stage 1: CF Worker presence WS** — `70338b18`. MAC-verified upgrade, hibernation API, zero-knowledge broadcast. **DEPLOYED by Taylor 2026-06-28** (`wrangler deploy`).
  - [x] **Stage 2: desktop bridge** — `a0845bf4`. `presence_aad` (single-source Rust, TDD seal→open) + `crates/tesela-server/src/presence_relay.rs` (MAC-auth WS to CF /presence/ws; outbound seal local PRES, inbound open+fan-out origin=None; reconnect 1s..32s + 30s heartbeat; zero-knowledge). Wired by subscribing to ws_delta_tx (no ws.rs change). Reviewed (approve); I removed a dead inbound echo-guard (compared PRES `peer` web-tab-id to device hex — never matched; loop-safety = outbound origin guard + relay exclude-self). Build clean; tesela-server lib 52 + tesela-sync aead 8 green. **SCOPE: WS client in tesela-server (not tesela-sync) — keeps iOS unbloated; iOS-share is the Stage 3 call.**
  - [x] **Stage 3: iOS client (Option B)** — `8e04f01d`. Pure-fn FFI presence_seal/open/ws_headers (tesela-sync-ffi, byte-match desktop; 5 new tests) + Swift PresenceRelaySocket (URLSessionWebSocketTask, FFI MAC headers fresh per reconnect, LoroPresence+RemoteCursorStore reused) wired in both shells when relayUrl present. UniFFI bindings regen (drift in sync). cargo ffi 34 + xcodebuild 287 green. **TestFlight build 55 cutting** (b4b46eed7).
  - [~] **Stage 4: integrate + verify** — needs: (a) iPhone on build 55 (cutting), (b) desktop running Stage 2 (REBUILD+install via install-desktop.sh + Taylor reopen — PENDING), (c) CF deployed ✓. Then: iPhone + desktop both on relay → see each other's live cursors.
  - FAST-FOLLOW (tracked, non-blocking, none on the https happy path): (1) hoist `OuterPayload` into tesela-sync so desktop+FFI share ONE wire-format (makes the parity test exercise real shared code, not a copy — review's [low]); (2) PresenceRelaySocket presenceURL ws/wss passthrough; (3) clear inactive liveSync.onPresence in the relay branch.
  - DECISIONS: CF-only backend (Axum relay presence-less); web stays hub-mode (browser WS can't set MAC headers). hub-mode (build 54/55) = same-LAN fallback.
- [~] **Phase 3: iOS presence** (#15) — hub-mode IMPLEMENTED + on TestFlight (build 54), but hub-mode deprioritized (see PIVOT). Reuses into Phase 3b relay presence.
  - FFI bindings regen `4598efbb` (Phase 1 methods drifted them — required or ios-testflight aborts at check-ffi-drift step 2).
  - FOUNDATION `fe5c3d64`: LoroPresence.swift (PRES codec, byte-identical to web — plain offset, NOT op-anchored, for web parity), RemoteCursorStore.swift (peer→caret, LWW/10s, per-launch id+color), LiveSyncSocket onPresence + PRES detect + sendPresence. 7 tests green on sim.
  - VIEW LAYER `7fcbdff2`: CollabTextView onCaretMove (textViewDidChangeSelection) + remoteCarets render (CALayer bars via caretRect); BlockRow threads them; MockMosaicService remoteCursors store + publishPresence/applyPresence; AppShell wires onPresence→apply + sendPresence→socket; GrDailyView TODAY section per-block. BUILD SUCCEEDED.
  - Transport: HUB-MODE WS only (iOS→Mac /ws, reuses Phase 2 PRES fan-out) — needs the iPhone in .http backend pointing at the Mac (tailnet). NOT relay-mode yet (CF-DO WS = Phase 3b).
  - SCOPE: today section only (primary case); yesterday/past = same pattern, follow-up.
  - ⚠ VERIFY ON-DEVICE: (1) cross-platform match assumes iOS block.noteId == web slug + block.id == web bid (both parsed from `<!-- bid -->`); if iPhone↔desktop cursors don't show, check these strings match. (2) iPhone must be hub-mode (.http → Mac), not .relay.
  - TestFlight build cutting now (b9a4hwpvj, ios-testflight.sh). After upload: commit project.yml + Info.plist build bump.
- [ ] Phase 4: collab polish (selection ranges, peer names/labels — web widget already supports a name flag, just not populated yet; follow-mode; presence sidebar; Savanne multi-user).
- Mode: BURN THROUGH autonomously (ultracode) — Phases 0/1/2 done; Phase 3 is the device boundary Taylor set.

## Deferred polish
- iOS #3 `/p1` slash deep-filter; #4 inline NLP (sim repro). Per-type color+logo. CF-DO-WebSocket presence transport (Phase 3 decision).
