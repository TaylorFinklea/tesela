# Current State

## 2026-06-10 (night) — SAVED VIEWS shipped end-to-end (4 waves, ~6h); build 8 cutting

The full feature from `phases/2026-06-10-views-registry-spec.md` (Taylor's 3 locked decisions): **synced views registry + DSL-first editing + views-are-inbox**, all platforms.
- **W1 core/engine:** `c3afb69` DSL comma-OR (tight-comma In-lists) + `INBOX_VIEW_DSL` const + the **99-case shared conformance fixture** (Rust=truth; all 3 implementations consume it — drift structurally impossible). `f02b5b2`/`267c981`/`9e60b80` synced views doc (`VIEWS_DOC_ID = b"tesela.views.reg"` IN the docs map → rides relay streams with zero wire changes; excluded from ALL note machinery; reseed-survives; 15 convergence tests) + FFI viewsList/Upsert/Delete.
- **W2 parity:** `615317c` server /views CRUD+reorder+`views_changed` WS+bring-up seed (validation = non-empty DSL parsing to zero predicates → 400; parser is deliberately total). `b221ceb` web parser = full Rust port (99/99; kanban op-encoding call-site fixed). `7e38d00` iOS LocalQueryEngine 99/99 (Inbox default now works in .relay).
- **W3 surfaces:** `da3e9f4` web GrInbox → Views switcher + inline DSL editor (live validation, chip inserters via `view-dsl.ts` toggleClausesInDsl, autocomplete, reorder, display modes via synthetic-Widget QWV) + `dc0881f` (pre-existing unit-suite loader breakage fixed — was masking 3 stale Model-A tests since ~Jun 7). `b672221` iOS Inbox tab → view chips + GrViewEditorSheet (engine-routed writes via RelayTicker seams; selection scoped per backend; relaunch-persistence sim-proven).
- **W4 review (2 reviewers):** architecture CLEAN; 2 importants FIXED: `36cb13b` builtin seed = byte-identical reserved-peer-0 ops (concurrent seeds are the SAME ops — no LWW race; migration direction favors edit-preservation) + seed gated AFTER relay bootstrap (server main.rs + iOS shouldSeedBuiltinViews(coordinator!=nil)); `84ef2eb` iOS ORDER BY carve-out structural (parseSimpleDsl surfaces sort) not substring. Suites: 248 Rust / 142 iOS / 281 web, all green.
- ⚠ **Fleet-migration wart (review note):** OLD builds (≤7) receiving the views doc show a harmless phantom empty-named note + warn logs (un-guarded refresh_note_derived index entry); heals on update. Taylor should update devices to build 8 promptly. Other accepted notes: `kind:block` match-all carve-out is deliberate; reorder = N snapshot writes (inefficiency); kanban falls back to table unless tag-scoped (pre-existing QWV).
- Debug server + web bundle rebuilt (desktop relaunch serves Views); **build 8** cutting in background.

## 2026-06-10 (evening) — Round-2 feedback: ALL 9 items shipped; cutover COMPLETE; build 7 cut

Taylor's round-2 list (9 items) → all addressed same-day; test card: harness-deck `20260610-round3` (ask `round3-result`).
- **iOS 6-bug batch** ✓ (`0737a96`/`c16a1d6`/`1ea57d8`, review CLEAN, 106/106): scroll past Yesterday (progressive 7-day windows); mic→capture sheet voice-mode (pendingVoiceCapture, no autofocus fight); capture appends at BOTTOM (captureInsertIndex skips trailing placeholders); swipe-dismiss DragGesture; **task-toggle revert ROOT-CAUSED** (whole-note writeback wrote in-text `status::` → P1.8 keeps container authoritative → A4 dedup dropped the line → file re-rendered `todo`; fix = typed property op via setBlockPropertyAndPush, writeback only as bid-not-found fallback); agenda checkbox marks done (BlockRow contentShape pattern — Buttons don't receive taps in that row). Known nit → backlog: dismissing capture mid-recording leaves recording running (pre-existing).
- **B3 cutover** ✓ COMPLETE (see the Stream B section below) — `a12a804` −4287 lines, one chrome.
- **Importer tasks** ✓ (`be1abbd`/`a2eecf0`): Task tag + status mapping (IN-PROGRESS/WAIT/CANCELLED added; org `cancelled`→canceled) + SCHEDULED/DEADLINE keep HH:MM + tolerate repeaters (were silently DROPPED). ⚠ Logseq repeaters still don't map to `recurring::` (org does) — backlogged. **USER STEP: backfill-task dry-run→apply on the real mosaic (desktop closed), commands in the round-3 card.** After apply: revert the live `tasks.md` query to `kind:block tag:Task -status:done` if it still has the has:status workaround.
- **Backups** ✓ provable (`72c7378` v2 authority capture, `1a59e55` scheduler+/backup/status, `b7f97aa` drill) + **adversarial review caught 2 CRITICALS, fixed** (`8009e49` automated non-local backups REFUSE plaintext when no age identity — was fail-open leaking group_key.bin; `3e17d08` the drill's VV assertion was VACUOUS for real slug-derived notes (reseed reproduces id+VV) → rebuilt on a two-peer lineage marker + the perturb-proof is a permanent test `reseed_cannot_reproduce_restored_lineage`; `1de2797` restore takes the mosaic lock vs prune race). USER: relaunch desktop → first v2 backup ~15s → `curl 127.0.0.1:7474/backup/status`.
- **Saved views/inbox** — product locked (registry + DSL-first + views-are-inbox; `eb73b11` spec) → NEXT milestone-let. History time-travel acknowledged → planned (Loro-derived history, M3-adjacent).
- **Build 7** cutting at session end (background). ⚠ Concurrent-agent tree clobbers happened 3× today (stash sweep, hard reset, checkout) — lesson recorded: parallel WRITER agents in one tree need commit-early discipline or worktree isolation; stash@{0} "task-tag-wip" kept as a recovery point (can drop once round 3 verifies).

## 2026-06-10 (midday) — Round-1 product test: 5 findings, ALL FIXED + shipped as 1.1 (build 6)

**Taylor's round-1 result:** sync works BOTH directions incl. same-block concurrent edits; quiet period seemed OK. 5 findings, all fixed same-day (details in the A15/A16 entries below + commits): (1) iOS deletes didn't propagate + resurrected (3 engine seams, worst = the destructive NoteUpsert reseed gate vacuously true on every device — `ddc84ba`/`54e80ab`); (2) phantom empty block above Today (placeholder writeback suppressed); (3) Agenda/Inbox empty in .relay (`9935979` LocalQueryEngine mirrors server semantics + refreshTick); (4) triage swipes/mark-done were silent no-ops in .relay (`2325a16`/`fd233ec` P1.11 block-property FFI + engine-routed writes — page-prop FFI fns still pending); (5) keyboard toolbar missing on the collab editor (UITextView needs a real inputAccessoryView — `c268617`).
**SHIPPED: TestFlight 1.1 (build 6)** — ⚠ NEW VERSION SCHEME (Taylor, `1c22b31`): plain integer builds ("build 6"), NEVER timestamps; **version truth = project.yml** (xcodegen regenerates Info.plist — plist-only stamps reset!); the script bumps the counter in both files, commit them post-upload. 1.1 = one-time reset (Apple needs ascending builds per version). Test card refreshed: harness-deck ask `build6-result`.
**Soak:** `498f321` real-relay soak test (env-driven rounds across compaction quiet periods); its FIRST run caught a live bug — transient register 5xx misdiagnosed as 409-conflict/hijack (the audit's medium finding) — fixed `9dad7d8` + stub-503 regression test. **12-round soak vs the live HA relay (0.2.2): PASSED 12/12** — both directions every round across 7-min quiet periods (full compaction each round), convergence mostly <50ms; rounds 7/9 had transient retry spikes (42s/18s wall) but converged. The quiet-period black hole is dead on the deployed image. Rerun: `TESELA_SOAK_RELAY_URL=http://100.85.144.53:8484 cargo test -p tesela-relay --test soak soak_real_relay -- --ignored --nocapture`.
**Next:** Taylor's build-6 test round → then Stream B (B2-B4 cutover) + Milestone 3. HA stays 0.2.2 (no relay change today; the register fix is client-side — lands in the next desktop relaunch + future builds).

## 2026-06-09 (evening) — Ultracode audit + product review → two-stream plan

**Branch:** main (un-pushed commits — remind Taylor to push at session end, adopted 2026-06-09).
**What happened:** full-repo multi-agent bug bash + arch review (91 confirmed findings, 42 fact-checked recs). Report: `~/.harness/reports/tesela/20260609-bugbash-arch-review/` (read `artifacts/all-findings.json` for file:line evidence before fixing anything). Decisions → `decisions.md` 2026-06-09. Execution spec → `phases/2026-06-09-audit-hardening-spec.md`.
**HOLD LIFTED (2026-06-10): test on TestFlight `202606100921` + HA add-on 0.2.2.** The two criticals (relay seq black hole `61506af`; iOS `.relay` read-only `182210d`) are fixed AND shipped — new TestFlight build uploaded, `:0.2.2` image on GHCR. Taylor's setup: update the HA add-on → relaunch the desktop → TestFlight update → run the 5-step test (harness-deck ask `relay-hardened-build-result`).
**⚠ Mojibake risk on the REAL mosaic today:** tag rename/delete corrupts ALL non-ASCII text (`tag_rewrite.rs` byte-as-char). Avoid tag renames until A8 lands.

### Plan (two parallel streams — items detailed in the spec; pick ONE per iteration)
- [x] **A1** relay seq fix + conformance case + HA add-on 0.2.1 ✓ (`61506af` fix + test, `1aa4987` HA bump). TDD red→green; conformance now 20 cases, **green vs BOTH impls** (Rust + CF Worker via wrangler dev); `cargo test -p tesela-relay` + `-p tesela-sync` all green. ⚠ Live HA relay still has the black hole until: Taylor pushes → `relay-container.yml` builds `:0.2.1` green → HA refresh repo + UPDATE the add-on.
- [x] **A2-A3** ✓ auth_key stripped from GET /registration AND the 409 register echo, BOTH impls (`ff6fdde`; never load-bearing — clients derive via HKDF; conformance test_16 locks it). Poison-envelope skip-not-wedge (`7069a48`: `PollBatch{rows,skipped}`, per-row open_relay_row, server folds skipped into max_seq, FFI counts into `errors`; perturb-checked load-bearing). Conformance **21/21 vs both impls** (CF via wrangler). HA bumped 0.2.2 (`ed72238`) — supersedes 0.2.1: after next push+green CI, update HA straight to **0.2.2**.
- [x] **A4-A5** ✓ cursor-past-failure family + scoped cursors, ALL layers. Rust `f4cdad6` (RelayApplyReport surfaces per-note failed/pending; server tick holds cursor at a failed apply w/ bounded retry → catchup_notes; Loro-pending → snapshot catch-up same tick; bootstrap jumps cursor only when ALL imports succeed — TDD red→green), `b5c8b54` (server RelayState scoped to (relay_url,group_id); mismatch → fresh state + re-bootstrap; legacy file adopted in place), `5bb37d6` (FFI: `TickOutboundRecord.batchesAttempted/batchesFailed/lastError`, tick_inbound holds cursor at failed applies (MAX_APPLY_RETRIES=5) + `needsCatchupNoteIdsHex`; bindings regenerated, CFFI unchanged; 13 FFI tests green). iOS `ddd8def`: UserDefaults cursors keyed `relay.*.<relayUrl>|<groupIdHex>` (legacy bare keys adopted ONCE by the first post-upgrade coordinator build, then re-keyed); fresh identity → cursor 0 → bootstrap runs; adopting a NEW pairing code drops the live coordinator + lastPushedVV (was: kept ticking the old group); iOS bootstrap = all-or-nothing cursor jump (partial failure holds + surfaces via lastError); tickOnce consumes `needsCatchupNoteIdsHex` → fetchSnapshots + importNoteSnapshotById for exactly those notes. Convergence-harness cases → A12; iOS side compile/trace-verified (unit target → A13).
- [x] **A6-A7** ✓ iOS `.relay` writes + honest status. `182210d`: all 3 write gates accept `.relay` (no HTTP tails existed — the PUT path was removed in Phase 2.1; pushPage's bound baseURL was vestigial, pushTodayBlocks' param dropped); daily slug falls back to `dailyId(0)` on a fresh pair (refresh(.relay) also seeds serverDailyId); `applyRemoteChange` gates only `.mock` (relay inbound now refreshes the UI); `attach(non-mock)` clears MockSeed; cached pairing code cleared ONLY on definitive failure (InvalidPairingCode / crypto "hijack"/"does not verify") — transient errors keep it + backoff (was: any blip spanning 2 ticks bricked .relay sync until re-scan). `c13e2dc` (A7 Swift): `noteOutboundOutcome()` at all 4 tickOutbound sites — `batchesFailed>0` ⇒ lastError + consecutiveErrors backoff (both Settings sync panels already key off lastError → red w/ real error text); `LiveSyncSocket.sendDelta` now async + send-completion-confirmed (was: returned true pre-handshake w/ completion ignored → commitPushedDelta permanently excluded dropped ops from WS delivery). Verify: xcodebuild sim BUILD SUCCEEDED ×3 + sim launch smoke (`-graphite` renders the .mock seed; `-graphite -backend.mode relay` shows the honest EMPTY state — no fake seed — alive 10s+, no crash logs); the runtime .relay write→relay→peer round-trip = A14 product test.
  - **Adversarial review (both halves) ✓** — Rust: CLEAN all axes (Generated/CFFI byte-diffed in sync; cursor provably never regresses; deposit-skip-while-catchup = deliberate safety-over-liveness, only costs compaction). 2 review fixes committed `3acbeb0`: tickOnce now captures cursorScope WITH the coordinator (mid-tick re-pair could write the old group's cursor under the new key → silent stall) + `.relay` serverDailyId re-derived every refresh (foreground-across-midnight wrote into yesterday's daily). Noted for later: tick still holds the state write-lock across network I/O (pre-existing, tracked medium); legacy no-identity state file adopts-in-place on the upgrade launch (one-time edge, self-corrects).
- [x] **A8-A9** ✓ mojibake (`73cffda`: byte-as-char → str-slice runs, 4 TDD tests incl. byte-identical pass-through; ALSO fixed a latent PANIC at the wiki-pass tail on multi-byte boundaries; tag renames safe again). PUT-now-propagates (`bbb2e04`: record_sync_* return Result → 500 w/ honest body at every handler site; ensure_tag_pages stays warn-and-continue). note_tree (`2aa53e3`: GATE not preserve — stamp_existing_notes serializes + strips intended bid stamps + skips/warns on any other diff; true preservation needs a doc-model raw-segment home, deferred; PUT-path stamp_block_ids residual noted in code).
- [x] **A10** ✓ (`ded5a50`) Reminders auto-sync default-OFF behind `TESELA_REMINDERS_AUTOSYNC` (non-empty = on; kills the 30s self-retrigger loop + EK fail-open clobbers); manual POST /sync/reminders untouched; M3 re-route plan noted at the trigger site.
- [x] **A11-A13** ✓ CI: `24f8af6` fmt-all (45 files) + `93d506a` ci.yml rebuild — 4 jobs (rust: tauri-deps + fmt-check + advisory clippy(~26 warns) + workspace tests; web: node-24-pinned unit tests 190/190 BLOCKING + advisory svelte-check; worker-conformance: wrangler dev + suite vs the CF Worker; notify-failure → pinned GH issue, also added to relay-container.yml). release.yml → workflow_dispatch-only (purge still parked). ios-smoke.yml dispatch-only (FFI .a not committed; macos-26/Xcode 26). ⚠ WATCH next push: first-ever Linux build of src-tauri (deps installed; fallback = --exclude tesela-desktop); workerd boot in Actions; then dispatch ios-smoke once. Pre-existing flake noted: `block_granular_write::post_blocks_after_bid_inserts_adjacent` under parallel load. Harness: `c9e9990` convergence_harness.rs (5 scenarios, content-equality asserts, perturb-checked vs the A1+A3 reverts — A1 revert failed 3 scenarios incl. the literal data-loss signature; 0.2s runtime; runs in workspace tests → CI-gated). iOS: `d28337e` TeselaTests 24 new tests (cursor-key schema, legacy migration, pairing routing, definitive-vs-transient failures, bootstrap jump rule) 58/58 green + gated in ios-testflight.sh (step 4/6); `2278a26` scripts/check-ffi-drift.sh (perturb-checked) gated step 2/6.
- [x] **A14** ✓ SHIPPED — TestFlight **`202606100921`** uploaded (`7e4e25d` stamp; version 1.0; the new gates ran live: drift check ✓, 58 unit tests ✓, SwiftWhisper heal ✓); HA `:0.2.2` image green on GHCR; debug tesela-server rebuilt (desktop picks it up on relaunch). Product test refreshed on harness-deck `20260609-relay-sync-rollout` (ask `relay-hardened-build-result`, HOLD lifted). [?] awaiting Taylor: HA update to 0.2.2 + desktop relaunch + the 5-step on-device test (incl. the 6-min quiet-period black-hole case + phone-edit persistence). **STREAM A COMPLETE (A1-A14).** Remaining = Stream B (B2-B4) + Milestone 3.
- [x] **A15 (product-test fallout, 2026-06-10)** ✓ iOS delete-revert + phantom-empty-block FIXED — `ddc84ba` (engine: NoteUpsert = non-destructive per-bid reconcile w/ deleted-wins — the materialize_dir "server-only" reseed gate was vacuous post-cutover and resurrected deleted blocks / reopened vector #2 / minted twins; BlockDelete tombstones ALL same-bid live nodes; record_note_diff first-author records a NoteUpsert so the doc gets root.slug → file materializes + slug reaches peers) + `54e80ab` (iOS: placeholder-only fresh-daily writeback suppressed — was the [empty, dude, empty] top-stray; pre-materialization splices route via writeback so keystrokes can't drop). Decisions → decisions.md 2026-06-10. Verify run: workspace green (51 suites incl. convergence harness), FFI drift check ✓ (bindings regen — doc-checksum only), 61/61 TeselaTests on sim, sim relay-mode launch smoke clean. ⚠ Relay crate untouched — NO HA bump needed; desktop relaunch picks up the rebuilt debug tesela-server. [?] awaiting: NEW TestFlight build (CFBundleVersion untouched per instructions) + Taylor's delete-retest (delete on iOS → gone locally after refresh + gone on web).
- [x] **A16 (P1.11 block slice, 2026-06-10)** ✓ relay-mode triage writes land — `2325a16` FFI `set_block_property`/`clear_block_property` on SyncEngineHandle (BlockPropertySet via record_local; typing = the server's REGISTRY-LESS unknown-key degrade: SetText coerce-and-keep + the `tags` Clear+AddToList convention; NO in-text strip — A4 render-dedup covers it, double-strip avoided; returns 1/0 w/ 0 = bid not in the rendered view, the 404 mirror; 6 TDD tests, tags + dedup perturb-proven; bindings regen + CFFI synced, drift check ✓) + `fd233ec` iOS (setBlockProperty/saveInboxDsl `.relay` branches via new awaitable `onLocalPropertySet`/`onLocalNoteWrite` seams wired in BOTH shells; line→bid via splitBlockAddress/blockBid mirroring block_bid_from_suffix; rejected write THROWS so triage rows don't vanish over nothing; 12 new TeselaTests, 99/99). Workspace green (52 suites). **Sim-verified end-to-end** (idb-driven relay-mode swipe → materialized file gained ONE `status:: done`, row dropped; bid-less note's row stayed + file untouched). ⚠ P1.11 REMAINDER: page-property FFI fns (set/clear PAGE property) not yet exposed — no iOS call site needs them yet. ⚠ Known boundary (doc'd on the fn): FFI clear un-hides a legacy in-text line for the same key (server strips; device relies on render-dedup).
- [x] **B1** all 8 /g parity bugs FIXED ✓ (`65c11a4` drills stay in /g, `1e1e505` shell key wiring — g leader/⌘K toggle/modal guards/missing chords/Ctrl-W gate, `47cfedd` colon+peek styling + live-store re-point). Browser-QA'd on a sandbox mosaic (7/8; autocomplete recency boost trace-only); adversarial code review: CLEAN, /v4 byte-identical, no vim regressions. Notes for B3: orphaned store files remain (still imported by orphaned `lib/commands.ts`); /g has no in-chrome PIN affordance (pre-existing — pins set on /v4 surface in GrRail).
- [x] **B2** ✓ default flip (`b46b756`): `/` → /g (redirect, mirrors the old /→/v4); `/p/<id>` → `/g#tile=` (g/+page.svelte consumes hash + scratch-prune boot sweep); stubs retargeted; `/v4` reachable; desktop already opens /g directly (picks up bundle rebuilds); static-build QA'd. Supersedes the roadmap-backlog "/v4 route removal" item.
- [x] **B3** ✓ DONE — Taylor's product pass ("PASS — delete v4/v5, I'm fully on /g") → executed: `1916cb5` (TagPageRenderer/PageTagsChips/CompactQueryView → lib/components root) + `a12a804` **delete (+31/−4287, 21 files)** + `f11e09f` checklist. `/v4`→`/` + `/v4/p/<slug>`→`/g#tile=` redirect stubs. Verified: grep-zero legacy imports; **svelte-check 0 errors** (the chronic VoiceCaptureButton error died with the file); unit 221/221; e2e nlp 10/10, vim 19/19, property-readmodel 9/9-fresh; browser QA 17/17. Bundle rebuilt at HEAD. **GRAPHITE CUTOVER COMPLETE — one web chrome.** Preserve-list final locations in the checklist doc. ⚠ Coordination lesson recorded: concurrent agents in one tree clobbered an uncommitted stage twice today (a stash sweep + a hard reset) — recovered both times; commit-early or isolate when parallelizing writers.
- [x] **B4** ✓ all 8 confirmed web-editor audit findings FIXED (`a5c929e` sync/save invariants: remote multi-run delta misapply → pure `deltaToChanges` orig-doc coords + per-event dispatch; kind-aware BlockOpsSaver (move folds into pending upsert); no-op `handleBlockChange` early-return kills the autocomplete/slash double-write; NORMAL Enter/Backspace vim guards (vim `<CR>` drill-in now live); `isClientMintedId` dirty-guard superset for `:merged-`/`:tmpl-`/`:split-` — op-builder gating keeps narrow `isLocalOnlyId`; `0c8604e` triage/attach → `api.setBlockProperty` container op, 404→false preserves the stale-row contract; `dc9fac3` NLP lift pre-claims literal ranges (wiki/md links, URLs, inline code) — highlight+lift consistent via shared `detectTokens`; `4874da4` JournalView bounded `dailyWalkDates` — future-only dailies render, future "tomorrow" no longer dropped). 221/221 unit (31 new), svelte-check no new errors. E2E vs static build of HEAD: NLP 10/10, vim-undo 8/8, vim-registers 7/7, vim-counts 4/4 (needs the bold/link/image/hr fixture daily); property-readmodel 8/9 — failing check A/B-reproduced IDENTICALLY on the pre-fix base build (600ms disk-poll races splice materialization, pre-existing flake). Browser-QA'd (playwright-MCP, sandbox mosaic): inbox `x`/`t` → ONE POST /blocks/set-property, NO note PUT, exactly one `status::` line on disk; NORMAL Enter mid-text = no split/no save; NORMAL Backspace col-0 = no merge; INSERT split+merge still work; future-only-daily mosaic renders Jun-15→Today gap-free. ⚠ Discovered pre-existing (A/B-proven on base, → roadmap Backlog): split + merge-back inside the 500ms window orphans the absorbed block's server row.

**After both streams → Milestone 3 (decided): finish the sync spine** (CF deploy, key/pairing min slice, cursor migration, WS-hub demotion, Reminders/recur-bump engine re-route, NoteDelete tombstone spec).

## 2026-06-08 — Multi-device sync = always-on RELAY (HA → CF), NOT a Mac-hub

**Decision (Taylor):** the always-on multi-device spine is the **encrypted relay** — **Home-Assistant-hosted relay FIRST** (it's already running on his HA box, always-on; see [[project-relay-413-blocks-sync]] task #140), **then migrate to Cloudflare** (roadmap step 1). NOT a Mac-as-hub. Goal unchanged: multi-device works with the Mac off ([[project-multidevice-next-milestone]]).

**Stopgap built then reverted today:** to get iOS pairing working immediately I (a) launched a standalone LAN `tesela-server` hub on `logseq` (Tailscale `100.112.34.59:7474`) and (b) added a **desktop remote-connect mode** (`feat(desktop): remote-connect mode`, committed) so the native app could wrap the hub's `/g`. Taylor redirected to the relay instead. **Reverted to a clean state:** killed the hub, removed `~/Library/Application Support/tesela/desktop.toml`, quit the app → the desktop is back to its standard self-contained loopback embed (launch `/Applications/Tesela.app` normally). The remote-mode CODE stays committed but **inactive** (an option if we ever want a hub or to wrap the relay's UI).

**Why the desktop couldn't pair iOS:** its embedded server binds loopback by design (not a hub); pairing code was `127.0.0.1`. The fix is the relay path, not LAN-exposing the desktop.

**Next sync work (prioritized):**
- [ ] **Update the HA relay first — its CI build was BROKEN (dark since 2026-05-29), now FIXED.** HA log showed it trying `:0.1.0`→`:0.1.1`, got **404** (the `:0.1.1` image never existed), then **removed the add-on**. Root cause: `relay-container.yml` failed every run since 05-29 — TWO stacked issues, both fixed (`fix(relay,ci)` commit, verified with a clean local `docker build` on rust 1.95 → image builds + binary runs):
  1. builder pinned `rust:1.86`, but `heapless@0.9.3` needs rustc 1.87 → bumped to `rust:1.95-bookworm`.
  2. Dockerfile didn't `COPY src-tauri` (workspace member added ~06-03 in `60e37ad`) → `cargo --locked` couldn't resolve the workspace. Added it. Plus a new `.dockerignore`.
  Also bumped add-on `version` 0.1.1→**0.2.0** so HA sees a new tag.
  **✓ DONE:** pushed (`fdf9dd5`), GH Actions `relay-container.yml` run `27180605109` = **green**, `ghcr.io/taylorfinklea/tesela-relay:0.2.0` **verified live on GHCR (multi-arch amd64+arm64)**. **→ Remaining (Taylor, testing now):** in HA refresh the add-on repo + **reinstall** the (removed) Tesela Sync Relay add-on — pulls `:0.2.0` (has the snapshot endpoints).
- Point devices at the **HA relay** — spec [`phases/2026-06-09-relay-spine-sync-spec.md`](phases/2026-06-09-relay-spine-sync-spec.md). KEY FINDING: the relay tick was **fully built + integration-tested, just gated** — not stubbed.
  - [x] **Phase 1 DONE + tested** (`feat(server): TESELA_RELAY_URL env fallback`): `load_relay_url_from_config` honors `TESELA_RELAY_URL` env (mirrors `TESELA_DEFAULT_MOSAIC`). Verified: two standalone servers (shared group, local relay) **converge A→B through the relay** (CONVERGED ✓, inbound_cursor 0→1). + `tick_deposits_snapshots_then_fresh_device_bootstraps` green.
  - [x] **Phase 2 DONE + tested** (`feat(desktop): opt the embed into the relay spine`): `resolve_embed_relay_url()` (`TESELA_EMBED_RELAY_URL` env / `relay_url` in desktop.toml, DRY `desktop_config_value`); `spawn_server` drops `TESELA_DISABLE_RELAY` + injects `TESELA_RELAY_URL` when opted-in (mDNS stays off). Verified: the desktop embed opted-in **registered + converged a note from a separate node through the relay** (CONVERGED ✓, one writer).
  - [x] **Phase 3 (iOS) DONE + compile-verified** (consumer-only, open-Q#4). iOS already envelope-syncs; this adds the offline-bootstrap robustness half so iOS converges past the relay's GC window when the Mac (depositor) is off. `feat(ffi)` `ff6233d`: `RelayClientHandle::fetch_snapshots` + `SyncEngineHandle::import_note_snapshot_by_id` (note_id-keyed; wraps inner `RelayClient`/`import_authoritative_snapshot`, group-AAD in Rust). `feat(ios)` `f339b8a`: `RelayTicker.buildCoordinator` bootstrap step (fetch → import each note_id snapshot → jump cursor to compaction_seq → tail poll), UniFFI bindings regenerated (library mode) + CFFI header synced. **Verified: `cargo build -p tesela-sync-ffi` clean + the regenerated `tick_deposits...` server test still green + iOS `xcodebuild` (sim) = BUILD SUCCEEDED** (RelayTicker compiles against the new bindings). Logic mirrors the proven server `bootstrap_from_snapshots`. (iOS DEPOSIT `put_snapshots` deferred — desktop deposits; iOS consumes.)
  - [~] **Phase 3 runtime sim-convergence NOT run by me** — driving a sim through pairing + orchestrating relay-GC timing is heavy + iOS testing wants Taylor's device; folded into the product test (the Mac-off offline-bootstrap case). Compile + the server-side proof of the same path are the automated coverage.
  - ⚠️ **Live-test finding (2026-06-09): pairing to the desktop EMBED was broken on iOS** — `PairScanView.adopt` set `backend = .http(code.url)` where the embed's `code.url` is its **loopback** (`127.0.0.1:port`, unreachable from the phone), and HTTP mode gates the relay tick OFF (`hubMode`). So scanning the embed → "Connection failed / Sync error / last tick never / hub" (Taylor's screenshot). **FIXED `fix(ios): pair via relay when the inviter is a loopback embed` (`8fce292`):** `RelayTicker.isRelayOnlyPairing` (loopback/empty url + a relay_url) → `adopt` caches the scanned code (`RelayTicker.cachePairingCode`) + switches to **Mock mode** so `hubMode` is off and the relay tick syncs via the code's relay URL; the data layer's existing **local-first read** ("if we have a materialized file, show it") surfaces the synced notes. Reachable-url nodes still pair HTTP-direct. **Compile-verified (xcodebuild sim BUILD SUCCEEDED); needs a TestFlight build to test on device.** (Short-code pairing needs a reachable server to resolve the code → not a relay-embed path.)
  - 🛑 **CORRECTION (2026-06-09 live test): the Mock-mode approach above is WRONG — `8fce292` does NOT work.** Taylor: "Mock just replaces the mosaic with FAKE data." Mock mode = the built-in fake snapshot, NOT the on-device relay-synced notes. So switching to Mock for relay-sync surfaces fake data, not real notes. The "local-first read" only applies in `.http` mode (read local → HTTP-freshen), NOT `.mock`. **Root reality: iOS has NO backend mode that both runs the relay AND shows the real relay-synced notes** — `.http` = Mac-direct (relay off, needs Mac reachable), `.mock` = fake. The relay applies edits into the on-device engine but no UI read-path surfaces them with the Mac off.
    - **THE REAL FIX (next focused session — Taylor approved):** add a first-class iOS **relay / local-first backend mode** — the data layer reads the on-device engine's relay-synced materialized notes (not fake Mock, not Mac-HTTP) while the relay tick runs. Reproduce on a **sim I drive** ([[feedback-drive-sims-not-devices]]), build the mode, verify desktop↔sim convergence with the Mac's HTTP unreachable, THEN cut a TestFlight build + ensure it lands in Taylor's test group. Plan + approval in harness-deck `20260609-relay-sync-rollout`.
    - ✅ **DONE + sim-verified (2026-06-09):** `.relay` backend mode shipped. `feat(ios): local-first relay backend mode` (`07ab601`): `BackendSettings.Mode.relay` + `MockMosaicService.Backend.relay`; refresh/loadPage/search read the relay-synced local notes (reuse `applyLocalRefreshFallback`/`readLocalNote`/`localSearch`), no HTTP; PairScanView routes relay-only inviters to `.relay`; GrSettingsView Relay segment. `fix(ios): start the relay tick on launch` — `start()` was ONLY in `.onChange(of:scenePhase)`, which doesn't fire for the INITIAL value → the tick never started on a fresh launch (fatal for .relay); now also in `activateBackend()` (runs on launch). **Sim-verified** (the relay tick runs — 289 ticks, coordinator built, hubMode false, NO error, cursor synced, REAL notes shown via the relay). TestFlight **`202606091254`** (version 1.0 unchanged). `ITSAppUsesNonExemptEncryption=false` → no export-compliance hold (likely why `202606091025` never appeared for Taylor).
    - ⚠️ **CRITICAL DEBUGGING GOTCHA for the next session:** `xcrun simctl spawn <sim> defaults read <bid> <key>` reads a DIFFERENT domain than the app's `UserDefaults.standard` WRITES — it shows the app's own writes as UNSET (false negatives that cost ~4 wasted build rounds here). Read the app's REAL defaults from its sandbox: `get_app_container <sim> <bid> data` → `Library/Preferences/<bid>.plist` (PlistBuddy). Also: `strings` does NOT surface Swift string literals in these binaries (0 matches even for known literals) — don't use it to check build freshness.
    - ⚠️ **OPEN follow-up:** a BRAND-NEW desktop edit did NOT advance the sim's cursor in-window (initial bootstrap synced the group's notes fine; ongoing new-op delivery didn't). Likely desktop-embed relay OUTBOUND or snapshot-vs-poll, NOT the iOS `.relay` mode. Taylor confirms live convergence on-device (harness-deck `relay-build-result` ask); if new edits don't flow live, chase the desktop embed's relay produce/put-envelope path.
    - ⚠ **Build delivery:** the `202606091025` relay-pairing build uploaded ("Upload succeeded") but Taylor never saw it in TestFlight — check processing / test-group assignment when cutting the next build. He's testing on the **simulator** (127.0.0.1:60192 = the Mac's desktop API port), in hub mode.
  - **Desktop + relay HALVES ARE DONE + verified** (Phases 1-2 converge through the relay; Phase 3 FFI compiles). Remaining = purely the iOS client read-path/mode model above.
  - Open questions for Taylor in the spec (embed relay-URL source of truth; embed first-class-vs-transitional; iOS depositor-vs-consumer).
- [ ] Then Cloudflare relay (`wrangler deploy`, roadmap step 1).
- ⏳ Taylor is away from home — will **confirm iOS pairing / relay sync later** (needs the home network + the iOS device).
- (TestFlight build is up + LIVE; the re-migrated `logseq` mosaic has the Model-B NLP config.)

## 2026-06-08 — Configurable NL triggers + lift-on-blur (Model B Part 2c)

Generalizes Part 2's hardcoded detection into a **config-driven engine** + fixes the lift-timing. Spec: `.docs/ai/phases/2026-06-08-nlp-config-spec.md`; decisions.md 2026-06-08. **Verified `task-tokens-nlp.e2e.mjs` 10/10** + tag-redesign 10/10 (no regression).
- **Config-driven:** `task-tokens.ts` rewritten — `detectTokens(text, spec)` scans by `value_type`: select (Priority p1-p4 = value tokens), number (Points `<n> <trigger>`), date (`<trigger> <NL date+time>`), + the tag's `default_date_property` for bare dates. Per-property `nl_triggers` parsed in `property-registry.ts`; config built in BlockOutliner (`getTagPropertyDefs` + `detectConfigFacet`, replacing the old enabled-tags Set). Adding any property + triggers to a tag → detection, zero code.
- **Dates keep the TIME** (`due thu at 8` → deadline `<date> 08:00`). `due`/`deadline` keyword → Deadline; bare → Scheduled. Multi-word (`next tuesday`) works.
- **Lift-on-blur (timing fix):** removed the Enter-lift + make-task-lift; lift happens ONLY on blur (leaving the block). ⌘↵ make-task just tags — no rewriting the line while you're still editing. Single path, no double-lift.
- **Seed updated** (`tesela-server/src/main.rs`): Priority→p1-p4 + nl_triggers, Deadline/Scheduled nl_triggers, Task gains Points + `default_date_property` + a new Points property. (tesela-fixtures too, but that's benchmarks-only.)
- **Live-mosaic enablement — DONE (re-migrated from Logseq, Taylor's choice).** Quit desktop → backed up the old mosaic to `logseq_pre-nlp` → `tesela init logseq` + `import-logseq --source ~/logseq` (507 notes) → ran the new debug server once to seed the property pages (verified: priority `nl_triggers`, Task `Points`+`detect_tokens`+`default_date_property`) → graceful stop. Desktop relaunch uses the new debug server (Jun 8, has the seed). Detection live on the real mosaic.
  - Cleanup pending (after Taylor confirms the re-import looks right): remove `~/Library/Application Support/tesela/logseq_pre-nlp` (the backup) + the stale `logseq2`.
  - ⚠ The desktop's `tesela-server` is the repo `target/debug` binary — keep it newest (or the relaunch could pick the older release w/o the seed). Release server (Jun 5) lacks `nl_triggers`; rebuild it if it ever becomes newest.

## 2026-06-07 — Tag/chip redesign shipped + iOS TestFlight pipeline

**Tag/chip redesign (Model A) DONE.** Colored per-tag pills from `tags::` only + ↵→chip / ⌘↵→inline gesture (`d33c8b1`); deferred follow-ups done (`02713da`): tag-page `color::` override + removed the dormant trailing chip-widget. Decisions: `decisions.md` 2026-06-07 (look + the Model-A pill rule, both via harness-deck mock-ups). Verified: `tag-redesign.e2e.mjs` 10/10 + A5 9/9.

**iOS/iPad TestFlight — UPLOADED + LIVE.** `scripts/ios-testflight.sh` (FFI device build → resolve+heal SwiftWhisper submodule → stamp build# → archive → `-exportArchive destination=upload`) + `app/Tesela-iOS/ExportOptions.plist`. ONE universal build = iPhone+iPad (`TARGETED_DEVICE_FAMILY 1,2`). **Tesela 1.0 (build 1) uploaded to App Store Connect → processing for TestFlight** (`** EXPORT SUCCEEDED ** / Uploaded Tesela`). Mirrors the seedkeep-ios/hermes-voice release scripts: **shared account-level ASC API key `J79935N6P6`** (`~/.appstoreconnect/AuthKey_J79935N6P6.p8`), **issuer `fe27785a-1413-46ff-bd82-111de0da024f`**, team K7CBQW6MPG. Re-run `scripts/ios-testflight.sh` for every future change (stamps a timestamp build#).
- ⚠ **Landmine (auto-healed by the script):** SwiftWhisper's `whisper.cpp` git submodule fails to clone via SwiftPM (CWD/tmp-pack race). Fix = `git -C <DerivedData>/…/SourcePackages/checkouts/SwiftWhisper submodule update --init --recursive` then re-resolve — the script does this when resolution fails. Both ASR pkgs (SwiftWhisper=local Whisper, FluidAudio=Parakeet) ARE used — can't just drop one.

**Product-test round (Taylor, desktop) — 3 bugs FIXED (`f290fc7`), 1 design pending.**
- [x] **/g split focus tracks the cursor** — GrLeaf focuses its leaf on `focusin` (bubbles from `.cm-content`), not just bubble `onclick` (which misses caret-place / drag-select / link-clicks). Accent + active-note doc follow the cursor.
- [x] **Tag autocomplete UX** — selected-row highlight was invisible in /g (`bg-accent` == popover bg) → now a coral tint + left rail; added a footer hint (`↵ chip · ⌘↵ inline · ↑↓ move`) via a new `type` prop. Arrows already cycled.
- [x] **Stale tag list** — chip tags (block `tags::`) now materialize Tag pages: server `ensure_tag_pages` collects block `tags::` lines (was frontmatter + inline `#tag` only) → suggested in autocomplete. ⚠ **needs the tesela-server binary rebuilt for the desktop** (debug rebuilt this session — newest, so the desktop picks it up; release is stale).
- [ ] **Rapid-fire pill flicker (minor follow-up):** the chip-tag Tag-page creation triggers a broad `["notes"]` refetch that can transiently clobber a just-written pill in the in-memory journal cache (pre-existing optimistic-vs-refetch class; on-disk always correct, settles on reload). Single-tag use is clean. Could scope the invalidate so a Tag-page `NoteCreated` doesn't refetch dailies.
- **Priority + task-property display — DECIDED: Model B** (harness-deck answered; decisions.md 2026-06-08). p1/p2/p3 flags + Todoist detect-inline-then-lift-below. Phased:
  - [x] **Part 1 (display foundation)** — `web/src/lib/priority.ts` (p1/p2/p3 flags, normalizes legacy critical/high/medium/low; P4/low = no flag) + `BlockDateRow` shows a colored ⚑P1 flag in the below-strip + `BlockOutliner` ROW_OWNED_KEYS drops priority/scheduled/deadline from the right-edge chips (dedup) + strip guard includes priority. priority `choices`→`[p1..p4]` in fixtures (tracked) + the live mosaic + gitignored `notes/` seed. **Verified (Chrome):** red ⚑P1 in the strip, no "Priority" chip, no bars. Flag is DISPLAY-ONLY (the click-to-cycle didn't persist in the journal context — pulled it; setting = Part 2). Tag pills unchanged.
  - Spec: `.docs/ai/phases/2026-06-07-task-prop-modelB-spec.md` (seams + the `onSetProperty`-not-`upsertBlockProperty` correction).
  - [x] **Part 2a (priority detect+lift)** — `web/src/lib/task-tokens.ts` (`detectTaskTokens`/`priorityTokenRanges`, scans line-0 for `\bp[1-4]\b`) + inline highlight in `cm-decorations.ts` (`cm-tesela-priority-{1..4}` colored to match the flag) + the lift in BlockEditor's Enter handler (commit at end of prose line → `onChange(stripped)` then `onSetProperty({priority})` then `onEnter("")`). **e2e `task-tokens.e2e.mjs` 6/6** (highlight P1-red, lift→flag, prose stripped, `priority:: p1` materialized via container op — no dual-write). The setter Part 1 lacked.
    - ⚠ **Fresh-block caveat (pre-existing, shared with status:: on new blocks):** the property container op resolves the block by `note_id:bid`; on a *just-typed* block it can race the text-save (optimistic flag shows, but on a very-fast type+Enter the `priority::` may not persist). Realistic typing (block auto-saves first) is fine. Lift is **Enter-only** for now (blur lift deferred — double-lift guard needed).
  - [x] **Part 2b — per-tag-gated NLP + dates + make-task-parse — DONE.** Spec `.docs/ai/phases/2026-06-07-task-prop-modelB-spec.md`; decisions.md 2026-06-08.
    - **Gating:** `detect_tokens` tag flag (seeded on Task; code defaults `task` on regardless) → `detectEnabledTags` Set in BlockOutliner → `detectEnabledTagsFacet` in cm-decorations (mirrors `hiddenPropertyKeysFacet`). Highlight (cm-decorations) + Enter-lift (BlockEditor) gated on `getBlockTags(doc)` ∩ set — DIRECT tags, never inherited. Retrofitted Part 2a priority.
    - **Dates:** `task-tokens.ts` rewritten — `detectTokens` scans line-0 word-windows (longest-valid-first) via `parseDateAndRecurrenceInput`; bare→scheduled (`prefs.bareDateField`), `due`/`deadline` keyword→deadline, recurrence→recurring. Multi-word ("next tuesday") works. `cm-tesela-date` teal mark.
    - **⌘↵ make-task = tag-it-AND-parse-it** in `handleStatusCycle` (strip + lift in one edit).
    - **Verified:** `task-tokens-2b.e2e.mjs` 8/8 (make-task multi-word date + priority, `due friday`→deadline, non-task no-lift, #journal no-lift) + `task-tokens.e2e.mjs` 5/5 (Enter-lift on #Task block, gated highlight) + tag-redesign 10/10 (no regression). Visually confirmed (call mom→P1+Sched Jun 9; submit report→Deadline Jun 12).
    - ⏸ **Deferred:** blur-lift (clicking away without Enter/⌘↵ doesn't lift — Enter + make-task cover the flows; double-lift guard needed first). New mosaics need the CLI rebuilt to seed `detect_tokens` (live mosaic already has it; code default covers it meanwhile). Fresh-block property race still applies (realistic typing auto-saves first).

## 2026-06-06 — Autonomous day (Taylor out; NO user testing until tomorrow)

Self-QA-able / no-input work, priority order. Async product/arch calls → harness-deck. Branch 6 commits ahead of last push (un-pushed).

### Plan (autonomous)
- [x] **A1 — bid-route regression test** ✓ (`block_bid_from_suffix` extracted + test; 7 notes tests green) (Rust): assert `set_block_property`/`clear_block_property` resolve `note_id:<bid>` (load-bearing for the editor seam `f90eefe`/`699041b` — shipped with NO test). Verify: `cargo test -p tesela-server`.
- [x] **A2 — priority/deadline/scheduled chips render** ✓ (`d9d30ee`) — `displayChipsFor` now falls back to the tag's `tag_properties` when no `display_chips` is set (seed has the former, not the latter), excluding `status` (dedicated button). Browser-verified: `priority=high` on a #Task block renders a "Priority: high" chip.
- [x] **A3 — vsplit/hsplit on /g** ✓ (Path B — Graphite-native renderer, decided w/ Taylor; `decisions.md` 2026-06-06). The literal "swap to `<LayoutTree>`" was REJECTED: a 5-agent map proved it mounts v5 BufferShell → regresses default-daily (empty-pane), GrPage side pane, agenda time-grid, inbox look + needs `--v4-*` token re-scope + a shared seed change (trips the "revert if any view regresses" rule). Built `graphite/shell/GrLayoutTree.svelte` + `GrLeaf.svelte` (mirror `components/v5/LayoutTree` split/resizer/drag, Graphite tokens) tiling the EXISTING Gr* views across `tab.layout`; GrLeaf maps empty pageId → daily so the empty-pane regression is structurally avoided. Split commands were ALREADY wired (vsplit/hsplit/setRatio/moveFocus mutate tab.layout; leader/⌘K/`:` reach them) — only the renderer was missing. Added Ctrl-W h/j/k/l (`moveFocus`); focus accent split-only (lone pane unchanged). **Browser-QA'd (fresh /tmp mosaic):** daily/page(+side pane)/inbox(chips+Process-all)/agenda(time-grid) render; vsplit/hsplit/nested-v+h/close/Ctrl-W-nav work; 0 console errors. `pnpm check` clean for the 3 files (1 pre-existing unrelated error in `VoiceCaptureButton.svelte`, from `60e37ad`). Contained to /g.
- [x] **A4 — materializer dedup** ✓ (`d9ee731`) — `flatblock_from_node` drops an in-text solely-`key:: value` line whose key matches a container prop (container wins, case-insensitive); guards the flag-OFF legacy/dual-write dup. New `dedup_intext_props_against_container` helper (no-op when no container props / nothing dropped → common path byte-unchanged). TDD (watched RED → GREEN); 2 tests; full tesela-sync (162) + tesela-server (44) green.
- [x] **A5 — editor read-model regression tests** ✓ (`a8f49bd`) — `web/tests/property-readmodel.e2e.mjs` (Playwright, mirrors vim-*.e2e.mjs) locks ca39efa (status survives a keystroke → exactly one `status::` on disk) + f90eefe (#Task `a[href="/p/task"]` pill survives editing). Drives /g with real ⌘↵ + typing; 9/9 pass, 0 page errors. Run: fresh mosaic + static-serve on :7788 + playwright chromium (recipe in the file header).

**Today's outcome (autonomous):** A1✓ A2✓ A3✓ A4✓ A5✓ + the #Task backfill✓ — all committed. Splits = `4d60534` (Path B Graphite-native renderer). A4 = `d9ee731`. Backfill = `214fa72`. A5 = `a8f49bd`. Plus the earlier A1/A2. **Branch now ~10 commits ahead of last push (un-pushed).** Pending USER: (1) run `tesela --mosaic <real> backfill-task` (dry-run) then `--apply` after stopping the server — see the backfill item below; (2) a hands-on test round of the /g splits + a product-question batch (Taylor flagged both for next session).

### Decided / next
- [x] **Tasks-query DECIDED (Taylor, 2026-06-06):** keep the query STRICT (`kind:block tag:Task`, `system-widgets.ts:50`) — do NOT widen to `has:status`. Make existing status-bearing blocks appear via a ONE-TIME #Task BACKFILL.
  - [x] **#Task backfill BUILT** ✓ (`214fa72`) — `tesela backfill-task` subcommand (`crates/tesela-cli/src/backfill_task.rs`). Scans the Loro engine; adds `Task` via a structured `BlockPropertySet` AddToList on `tags` (materializes ONE `tags:: Task` line, never an in-text dup); union/idempotent. **Dry-run by DEFAULT** (lists `<note>:<block>` + count, writes nothing); `--apply` writes. Acquires the server `flock` → refuses while a server/desktop app holds the mosaic. Opens existing snapshots (no reseed); reuses `device_id.hex`. 5 unit tests (TDD) + smoke-tested end-to-end (lock refusal, dry-run, apply adds one tag + status preserved, idempotent re-run). **▶ USER TO RUN on the real mosaic:** stop the server/desktop app, then `tesela --mosaic "<real mosaic>" backfill-task` (review the dry-run list) → `… backfill-task --apply` → restart. ⚠ needs Loro snapshots present (real mosaic has them; a bare `tesela init` mosaic without a server reseed will show 0 notes).
- [x] **Tag/chip redesign — BUILT ✓ (`d33c8b1`, Model A).** Resolved the literal-tag mechanic via a 2nd harness-deck decision (`decisions.md` 2026-06-07, "Model A — commit-based"): **pill = the block's `tags::` committed tags ONLY** (new `chipTags` in `block-tags.ts`), **colored per-tag** (new `tag-color.ts`, deterministic palette from the Graphite tokens). Any `#tag` in prose stays inline. Gesture in the #tag autocomplete: **↵ commits → `tags::` chip (colored pill); ⌘↵ keeps literal inline `#text`** (`AutocompleteMenu.onselectInline` + `BlockEditor.applyAutocomplete(mode)`; the Mod-Enter keymap yields to the open popup so it doesn't clash with ⌘↵ make-task). The old position-based chip-widget decoration was found **dormant** (bid markers push `#tags` off the trailing edge) → no cm-decorations change needed. **Verified:** `web/tests/tag-redesign.e2e.mjs` 10/10 + `property-readmodel.e2e.mjs` (A5) 9/9 (no status/#Task-survival regression) + visual screenshot; `pnpm check` no new errors.
  - [ ] **Deferred follow-ups (small):** (a) tag-page `color` frontmatter override for `tagColor` (today deterministic-only); (b) dead-code cleanup of the dormant trailing chip-widget path in `cm-decorations.ts` (`TagChipWidget` + the trailing branch); (c) ⚠ unrelated pre-existing: `status::` container materialization on a brand-new block is finicky over vite-dev (P1.13 bid-address) — works on static-serve; not a redesign concern.

## 2026-06-05 (latest) — Properties + types milestone: planned, de-risked, Phase-1 started

**Branch:** main. Spec `phases/2026-06-05-properties-types-spec.md` (+ arch-review addendum). Decisions → `decisions.md` 2026-06-05. Dashboard: harness-deck `tesela` cards (editing-model, config-ui, design-plan, product-qs).

Brainstormed the full milestone w/ Taylor — 4 product calls: full-system (phased) / Hybrid editing + new-entity guard / 3 config surfaces / multi-value + node-refs both ship; **foundation-first**. Ran a 9-agent current-state map + a 7-lens architectural review (both code-verified): review locked op-shape (**dedicated `BlockPropertySet`/`PagePropertySet` ops**), container topology (scalars primitive, `prop_keys` mandatory), **coerce-and-keep** failure policy, and caught **14 blocking issues** — the disjoint-twin heal / `prune_bare_leaf_blocks` / NoteUpsert reseed / set-property route would each have re-introduced the data-loss — all folded into the spec. Commits: `bb870e0` spec · `88cbf59` addendum · `ae2fce1` P1.1.

**⚠ Migration landmine:** migrate-on-write is **flag-gated default-OFF**, flip only after the WHOLE fleet (incl. old iOS FFI) is read-capable — an old build imports the new property containers without error but renders them away (highest-severity loss). Keep emitting `key:: value` lines in the materialized view during transition; dual-read forever.

### Plan — Phase 1 (Foundation), TDD, foundation-first
- [x] **P1.1** typed scalar codec (`tesela-core::property`: `ValueType`/`PropScalar`/`parse_scalar`/`format_scalar`). Verify: `cargo test -p tesela-core` ✓ (`ae2fce1`).
- [x] **P1.2** engine `props`/`prop_keys` container model + per-type read/write helpers (mirror `write_block_text`; `get_or_create_container` only, never `insert_container`). Verify: `cargo test -p tesela-sync` ✓ (`59a4ddd`; `prop_containers` mod, `#[allow(dead_code)]` until P1.4 wires the ops).
- [x] **P1.3** shared `prop_keys` reader — `prop_keys_resolved` (dedup first-occurrence, drop missing-from-props, append props-only lexicographically) + `prop_get_list_dedup` (stable multi-value). Verify: `cargo test -p tesela-sync` ✓ (`9dd0cc4`).
- [x] **P1.4** `BlockPropertySet`/`PagePropertySet` ops + `PropOp` + apply arms (+`OpKind`; missing block = safe no-op; persist/materialize tail). Dropped `Eq` from OpPayload/EncodedOp (PropScalar has f64; verified nothing needs Eq/Hash). Verify: `cargo test -p tesela-sync` ✓ (`7f1479c`; concurrent prose+prop both-survive + AddToList union proven). ⚠ surfaced the container-overwrite hazard → see P1.9b + decisions.md 2026-06-05(b).
- [x] **P1.5** materializer join (`FlatBlock.properties` `#[serde(default)]`; prose then `key:: value` per `prop_keys_resolved` order; `format_scalar` / dedup-comma-join / LoroText). Verify: `cargo test -p tesela-sync` + `-p tesela-core` ✓ (`60d5e15`; **byte-determinism gate passes** — same ops, different orders → byte-identical render; legacy in-text round-trips, no double-emit).
**▶ Convergence design RESOLVED (2026-06-05c, `phases/2026-06-05-properties-convergence-design.md`). Execution order REPLACES the numeric order: P1.9b → P1.9 → P1.7 → P1.8 → P1.6 (P1.6 LAST, flag default-OFF). Eager-seed props/prop_keys (text_seq analog) fixes scalars + distinct-key first-set; per-key list/text first-touch = documented boundary + heal-unions; migrate = deterministic-shape; scalar same-key conflict = silent LWW-by-HLC v1.**
- [x] **P1.6** migrate-on-APPLY — deterministic-shape, flag default-OFF (`TESELA_LORO_MIGRATE_IN_TEXT`; strip SOLELY-property lines via `classify_block_prose_and_props`; prose-only `text_seq`; idempotent; `tags::`→AddToList). Verify: `cargo test -p tesela-sync` ✓ (`8d233b8`; lifts+idempotent, tags→list, mixed-fleet no-double-emit, concurrent-migrate converges byte-identical).
- [x] **P1.7** pruner fix — `block_is_bare` = empty text AND empty `properties`. Verify: `cargo test -p tesela-core` ✓ (`c1ba535`; keeps property-only block, drops empty-seeded-map block). Latent (pruner has no live callers today).
- [x] **P1.8** NoteUpsert non-authoritative over props — `tree_matches_blocks` compares STRIPPED prose on both sides (in-text props ≠ drift); reseed gated SERVER-ONLY w/ `snapshot_tree_props`/`replay_tree_props`. Verify: `cargo test -p tesela-sync` ✓ (`40c1d06`; device + **server-path** clobber tests + reseed-is-server-only). ⚠ adversarial review caught an unwired-helper + TDD-gap mid-run → fix loop wired it + added the server-path proof; I independently re-verified the committed state.
- [x] **P1.9** disjoint-twin heal carries props — `PeerBlockChange.props` + `ResolvedValue`; `reconcile_orphaned_prop_containers` (list=union / scalar=LWW / text=twin) read pre-tombstone; `reassert_prop_heals` per-key BlockPropertySet (AddToList missing members only). Verify: `cargo test -p tesela-sync` ✓ (`6cc2fd8`; distinct-prop both-survive + same-list-key union).
- [x] **P1.9b** container-overwrite convergence — eager-seed `props`+`prop_keys` at the 2 block-creation sites (text_seq analog); fixes scalars + distinct-key first-set; per-key list/text first-touch = documented boundary, healed by P1.9. Verify: `cargo test -p tesela-sync` ✓ (`1a4b265`; concurrent first-property both-survive; snapshot delta ≈48 B/block, immaterial). Page-root needs none (content-addressed).

**✅ Phase-1 BACKEND COMPLETE (P1.1–P1.10, P1.12).** Properties are typed CRDT data; concurrent prose+prop both survive; multi-value unions; materialization byte-deterministic; disjoint-twin heal + migrate (flag-OFF) converge; server routes emit the ops; index passes through unchanged. Full workspace green, independently re-verified each chunk. **Remaining = CLIENT surfaces (need build/device/browser + hands-on QA, NOT fire-and-forget):** P1.11 FFI + iOS Swift wiring, P1.13 web editor seam. ⚠ migrate flag `TESELA_LORO_MIGRATE_IN_TEXT` stays default-OFF until the whole fleet is props-read-capable (P1.11 ships that for iOS).
- [x] **P1.10** routes emit `BlockPropertySet`/`Clear` (registry value_type → PropOp; scoped migrate-on-write strips the one in-text line first to avoid double-emit; post-save bumps/deps read the re-materialized view; deleted the dead whole-note-rewrite helpers). Verify: `cargo test -p tesela-server` ✓ (`3851f28`). ⚠ adversarial review caught a test-invisible parent-meta clobber on NESTED blocks (synth BlockUpsert hardcoded parent=None) → fixed (`StrippedBlockProse.parent` carries the real parent) + a nested-block test; I re-verified.
- [ ] **P1.11** FFI surface (set/clear block+page property; mirror `splice_block_text`; regen bindings; copy header → BOTH `app/Tesela-iOS/CFFI/` and `Generated/`). Verify: `cargo build -p tesela-sync-ffi` + iOS `xcodebuild`. **BLOCK slice DONE 2026-06-10** (`2325a16` FFI + `fd233ec` iOS triage wiring, sim-verified — see A16 above). Remainder: PAGE property set/clear FFI fns (no iOS call site yet).
- [x] **P1.12** index passthrough golden test — container → `render_note_full` → `reindex` → `block_properties` rows == legacy in-text markdown (scalar + multi-value cases). Verify: `cargo test -p tesela-server` ✓ (`a6c6c7b`; TDD load-bearing confirmed via perturbation).
- **🔧 P1.13 REWORK (Anytype mold, route B — design: workflow `properties-done-right-design`):** product testing found the structured WRITE (slices 1–3 + v5, all KEPT) was correct but the editor RE-READ properties from prose text on every keystroke → container-set props (status/deadline/scheduled) wiped on the next edit. ROOT: `handleBlockChange`/`handleLoroText`/bulk-tag re-derived `block.properties` from text.
  - [x] **Preserve structured properties on edit** (`ca39efa`) — those 3 handlers now re-derive only tags+text, leave `properties` (container-sourced, seeded from materialized text on load) untouched; removed dead `parseProperties`. **Browser-verified (fresh /tmp/pdr2):** ⌘↵ status → TYPE → status chip SURVIVES + on-disk one `status::` line (+ `tags::`), no dup. Fixes the "set status, press space, it clears" cluster.
  - [x] **#Task tag survives editing** (user bug #1, FIXED `f90eefe`) — root: ⌘↵ added the tag via the HTTP block-op, never reaching the bound editor's doc; the next keystroke's tag re-derive then dropped the chip. `handleBlockChange` now splices the MINIMAL diff into the block's `text_seq` (`spliceActiveBlock`) for bound blocks (no-ops + HTTP fallback when unbound; only bound PROGRAMMATIC edits change path). **Browser-verified (/tmp/pdr3):** ⌘↵ → type → status AND #Task BOTH survive; 2nd cycle clean; on-disk one `tags::`+one `status::`, no corruption.
  - [ ] **Priority/deadline VALUE persists (covered by the preserve fix) but the CHIP doesn't render** — `displayChipsFor` (BlockOutliner.svelte:216) only renders a chip when the property is in the Tag page's `display_chips` metadata AND the `propertyRegistry`. STATUS has a DEDICATED button so it always shows; priority/deadline/scheduled are registry-gated → "priority doesn't work at all" = no chip renders (the value IS set in the container, just invisible). Real fix = the property REGISTRY + per-type `display_chips` config (Phase-2 registry-facing `Property`), or a dedicated chip render for those keys.
  - [x] **Pre-existing bug batch FIXED + browser-verified** (`7655bf8`, `8d02625`): ⌘K palette letter-spacing (removed `.lb` flex `gap`); ⌘1-9 quick-select badges + Ctrl-jk nav + ⌘-num quick-run (numbered in DISPLAY order via a `displayRows` list — was the scattered filteredRows index); Settings overlay opaque on /g (was transparent — `--v4-bg` is `.v4-root`-scoped + undefined on /g; imported tokens.css + tagged the `.overlay` `.v4-root`); literal `:` in cm-editor INSERT (gated the **GraphiteShell** + v4-layout `:` handlers on `getVimMode()==='INSERT'` — the GraphiteShell one is the active /g handler); auto-tag #Task on already-tagged blocks (`hasAnyTag`→`hasTask`).
  - [x] **vsplit/hsplit dead on /g — FIXED (2026-06-06, A3, Path B).** NOT the LayoutTree swap (that mounts v5 BufferShell → would regress GrPage side pane / agenda time-grid / inbox look / default-daily). Instead `graphite/shell/GrLayoutTree.svelte` + `GrLeaf.svelte` tile the existing Gr* views across `tab.layout` (split commands were already wired; only the renderer was missing). Browser-QA'd all views + splits, 0 errors. See the A3 Plan line above + `decisions.md` 2026-06-06.
  - [ ] **Tasks query empty — HELD (product decision):** the auto-tag fix covers FUTURE edits; existing status-bearing blocks without #Task still won't match `kind:block tag:Task -status:done` (`system-widgets.ts:50`). Widening to `tag:Task OR has:status` needs (a) verifying the query grammar supports `OR`/`has:`, (b) a call on "does any status make a block a task?" — or a one-time #Task backfill. Decide before touching it.
  - Chip look + the tag inline-vs-chip choice (⌘↵ inline / ↵ chip, Logseq-style) = deferred redesign (your call).
- [ ] **P1.13** web editor Hybrid seam — Slice 1 (additive dual-write) ATTEMPTED + browser-QA'd on an isolated mosaic, then **REVERTED**. **QA env note:** the cwd-walk for `.tesela/` OVERRIDES `TESELA_DEFAULT_MOSAIC` (the repo root IS a mosaic) → launch QA via `tesela init /tmp/x` + `tesela-server --mosaic /tmp/x` (the `--mosaic` flag beats the env + cwd-walk; verify 0 notes before editing). **Two QA findings:**
  1. The `note_id:line` block address goes STALE the instant the note reflows/prunes server-side → the editor set-property 404'd (`{block_id:"2026-06-05:1"}` vs block now at line 0). **FIXED on the server routes** (`set_block_property`/`clear_block_property` now ALSO accept `note_id:<bid>` — a non-numeric suffix is a bid passed directly; QA-verified 200; **KEPT in this commit** — ⚠ needs a regression test).
  2. ⛔ The additive dual-write produces a **DUPLICATE property line** (persisted, on-disk count=2): the editor writes `status:: …` into the block TEXT (handleStatusCycle / writePropertyContinuation / Task auto-fill) AND the seam writes the container → BOTH materialize. The server's per-key strip races the editor's text-write and loses. So the map's "let the server strip-then-rematerialize → no duplicate" premise FAILS whenever the editor also writes the prop to text.
- **→ STRUCTURED-FIRST** (design: workflow `p113-structured-first-design`): every editor property write emits the container op (bid-addressed) + optimistic `block.properties`, NEVER a `key:: value` text line — the server materializes ONE line, so no dual-write duplicate (proven structurally: the editor authors zero property text; only the materializer does, once per key).
  - [x] **Slice 1 — status path** — `BlockOutliner.setBlockPropertyStructured` funnel + `handleStatusCycle` rewired (emit container `status` op, optimistic, NO text splice; #Task auto-tag now adds ONLY the tag — the empty capital `Status::`/`Priority::`… placeholders were removed because they collided with the lowercase container line, server strip is case-sensitive). **Browser-QA'd on a fresh isolated mosaic:** Cmd+Enter → `tags:: Task` + exactly ONE `status::` line; set-property **200** (bid-addressed `2026-06-05:<bid>`); cycling repeatedly stays at ONE line (idempotent, no accumulation, on-disk `grep -c` confirmed). `pnpm check` clean.
  - [x] **Slice 2** — `/s` + `/p` property/date chords (`applySlash` status branch, `writePropertyContinuation`, DatePicker `onPick`) + `bulkCycleStatus` all route through `setBlockPropertyStructured` via a NEW `onsetproperty` event on BlockEditor (it only has `bid`; emits UP to BlockOutliner, which owns block identity). Chords now strip ONLY their trigger text + emit the container op — no `key:: value` line. `setBlockStatus` deleted; the `upsertBlockProperty` import dropped. **Browser-QA'd:** type `abc /` → `s` → Done → set-property **200** (bid-addressed) + block renders `abc` with exactly ONE container `status:: done` line (trigger stripped, no dup). `pnpm check` clean. (QA note: CM6's `/`-inputHandler fires only for REAL keystrokes — drive the slash menu via the Chrome `fill` tool, not `press_key`/`execCommand`.)
  - [x] **Slice 3** — `property-update.ts` (TagTable/KanbanBoard/KanbanCard chip + drag) now does an OPTIMISTIC `setQueryData` patch of the `["typed-blocks", tag]` cache BEFORE the round-trip (chip flips instantly) + rollback on error, and switched to stale-proof `<note_id>:<bid>` addressing. Adversarial design caught a real bug: the parser does NOT lowercase keys (`block.rs:284`), so the patch strips ALL case-variants (`withoutKey`) before writing lowercase — else a `Status::`-on-disk block wouldn't flip. `["note",id]` stays invalidate-only (raw markdown, not chip-shaped). `pnpm check` clean. ⚠ **NOT browser-driven:** TagTable/KanbanBoard mount only inside configured query widgets (`QueryWidgetView`/v4 `NoteRenderer` query blocks); the reachable `/p/<type>` page renders the v5 NoteRenderer's OWN grouped list instead. Manual spot-check needed: open a table/kanban query widget → edit a chip → flips before the 200, one line on disk, reload persists.
  - [ ] **Deferred (Phase 2):** manual-typed `key:: value` detect-and-migrate; remove `upsertBlockProperty`; registry-facing `Property` struct.
  - ⚠ the bid-stable routes (committed `699041b`) still need a Rust regression test.
  - [x] **v5 `QueryWidgetView` status writes → container (the OBSERVED dup, FIXED):** root cause was `cycleRowStatus`/`setRowStatus` rewriting the markdown via a whole-note PUT (`setBlockProperty(content) + api.updateNote`), so the text `status::` re-materialized ALONGSIDE the container's value → dup (it was the whole-note PUT, not a WS splice as first guessed). Converted both to `api.setBlockProperty(row.blockId, "status", …)` / `clearBlockProperty` (the container endpoint; `row.blockId` is the fresh query-result `<note_id>:<line>`); dropped the now-unused `setBlockProperty` text-helper import. **Browser-verified on /v4:** clicking tasktwo's status button fired set-property 200 and COLLAPSED the pre-existing `status:: doing`+`status:: todo` dup to ONE `status:: doing` line (the per-key strip removed both stale text lines). `pnpm check` clean. (`editRow`/`deleteRow` still use whole-note PUT — text/structural, not properties, out of scope.)

**Resolved (USER, 2026-06-05):** reflow **APPROVED** · out-of-choices guard default = **add-as-new-choice** · in-editor chip = **render-on-blur**. Folded into the spec (→ P1.5 / Phase-2 guard / P1.13). harness-deck `20260605-properties-product-qs` done.

---

## 2026-06-04 — Product-test fixes (user's full-app test of the Tauri build)

User ran a real product test on the desktop app + reported 8 issues. **6 fixed** (#1/#2/#3-merge/#4/#6/#7):
- **#4 typed callouts** (`19a1b0d`): `[!info]/[!warning]/[!error]/[!note]/[!tip]/[!success]/[!question]` (+ aliases) render the block as a colored box w/ icon (cm-decorations.ts + graphite-editor.css). Set the type by typing the keyword. Verified via Chrome (4 types, correct color+icon, marker hidden).
- **#6 desktop Settings menu** (`e0a4e69` + `.app` rebuild): the /g shell never mounted `FullscreenOverlay` (only /v4 did) → the gear/⌘G/leader-`,` set the store but nothing rendered. Mounted it in GraphiteShell (fixes the gear on /g too). Native "Settings…" (⌘,) menu item → evals `dispatchEvent('tesela:open-settings')` → GraphiteShell opens the overlay. Verified gear + event both open settings.
- **#1 `# Heading` popped the tag autocomplete** (`1771e9e`): the tag popup opened on `#` and stayed through the space. A tag has no spaces → close it when whitespace follows `#`.
- **#7 `yy` only copied the cursor's line** (`1771e9e`): block yank/delete now captures the WHOLE block (`getValue`, bid stripped); block-visual `yy` writes the whole multi-block selection to the OS clipboard (the per-block markBlockRegister no longer clobbers it). Verified: block-visual `yy` over 3 blocks → clipboard `v0\nv1\nv2`.
- **#3 (visible corruption) backspace-merge stranded a bid marker mid-text** (`891177d`): `prev.raw_text + currentText` left the previous block's `<!-- bid -->` in the MIDDLE (the hide-decoration only catches a TRAILING bid → `Hey <bid:…> there` junk). Strip both bids, concat clean, re-emit one at the end.
- **#2 code-block syntax highlighting + copy button** (`ee75c74`): dep-free tokenizer `web/src/lib/code-highlight.ts` (comments/strings/numbers/keywords/literals → `hljs-*` marks, lang from the fence) + a floating copy button. Themed in `graphite-editor.css`. Verified via Chrome.

**Deeper / deferred from the test:**
- **#3 root + #8 (`x` deletes the bid):** the bid marker lives IN the editor text (always display:none, atomic), so `x` past the end / any raw-text op can hit it. The merge patch stops the worst corruption; fully removing the bid from the editor = the block-id-as-property work (deferred). Possible separate multi-line-code-block round-trip bug (the "echo $SHELL duplicated" in image #6) — not reproduced.
- **#5 iPhone doesn't show the Tauri app's notes:** ARCHITECTURAL. The desktop app's embedded server holds the single-writer mosaic lock → the standalone `:7474` hub the iPhone syncs against can't run at the same time; the relay spine that would bridge them isn't live. = the "always-on sync spine" milestone, not a bug.
- **#4 callouts** (styling + selectable type info/warning/error/note) + **#6 Tauri settings menu** — queued feature work.

---

## 2026-06-04 (late) — Desktop blank-screen FIXED + vim parity: register cluster + CRDT undo

**The published `Tesela.app` showed a blank white window — ROOT-CAUSED + fixed (`36a9db8`).** Not vim, not WebKit: `tesela_server_bin()` preferred `target/release/tesela-server` whenever it existed, and a STALE release binary (May 30, pre-`TESELA_STATIC_DIR`) was shadowing the current debug build → the `.app`'s embedded server ignored the static dir → `/g` 404'd → WKWebView blank. Diagnosis: confirmed `/g` serves 200 + warm WebKit (Playwright) renders fine → launched the real `.app`, found its server returns 404 for `/g` with `TESELA_STATIC_DIR` set-but-ignored → `strings` showed the release binary has 0 refs to `TESELA_STATIC_DIR`. **Fix:** rebuilt release `tesela-server` (unblocks the existing `.app` — verified `/g`→200, WKWebView booted with a live `/ws`) + hardened `tesela_server_bin()` to pick the NEWEST of release/debug by mtime. `web/build` serves live, so relaunching the `.app` picks up web fixes with no rebuild.

**SECOND blank later in the session (different cause) → recovery shipped (`9b9da17`, `.app` rebuilt + reinstalled).** A long-running instance's WKWebView content process died after the session's many `web/build` rebuilds → white; and clicking the Dock icon only *refocused* the dead window (so "relaunching did nothing" — the trap). The build itself was verified good (boots in real WebKit, 0 console errors). Added two native recovery paths (app-process-owned, work even when the page JS is dead): **View ▸ Reload (Cmd+R)** → `WebviewWindow::reload()`, and **`RunEvent::Reopen`** (Dock re-open) → show+focus+reload. Ran `npx @tauri-apps/cli@2 build` → installed the new `/Applications/Tesela.app` (old moved to `/tmp/Tesela.app.prev`). **Verified:** new app renders (14 conns); a Dock re-open CHURNS the webview connections = reload confirmed. Cmd+R uses the same `reload()`. **Recovery for any future blank: Cmd+R, or click the Dock icon.** (Single-instance plugin still deferred — the flock already makes us effectively single-instance; #202.)

**Vim parity pass (#207) — 8 of 12 reported bugs fixed.** Earlier batch (`0c06853`,`5498a2f`): #1 cursor-jump, #11 indent-past-parent cap, #7 macro panel CSS, #10 `:` command-line UI.
- **Counts #2 (`602079a`):** `3j`/`5k` cross-block — j/k actions read cm-vim `actionArgs.repeat` → `handleNavigate(dir, count)`. E2E `web/tests/vim-counts.e2e.mjs` 4/4.
- **Register cluster #5/#6 (`90edcea`):** char-wise `delete`/`yank` now push to vim's GLOBAL register controller + system clipboard; `p` is register-aware (char-wise → inline paste; linewise/empty → whole-block paste); named registers (`"ayw`/`"ap`); `yy`/`Y`/`dd` stamp the register linewise. Global register = cross-block (each block is its own cm-vim instance). E2E `web/tests/vim-registers.e2e.mjs` 7/7.
- **Cross-block undo #12 — Loro UndoManager, increment 1 (`de6a81d`):** user chose option C (CRDT-native, Savanne-safe) over the snapshot stack. `NoteDoc` owns a Loro `UndoManager` (records only LOCAL text commits → never reverts a peer's edit; structural ops round-trip via server = imports = not recorded, so they stay on `OutlinerHistory`). `active-note-doc` `undoActiveDoc`/`redoActiveDoc` set an undo-applying flag so the binding applies the inverse `by:"local"` events it normally skips as own-echo. `u`/`Ctrl-r` → Loro text undo → structural snapshot → cm history. Every visible block subscribes to its `text_seq` → undo fans out cross-block. E2E `web/tests/vim-undo.e2e.mjs` 8/8. Spec + known refinements (time-based merge granularity; structural interleave; freshly-created block adds one extra undo step) in `.docs/ai/phases/2026-06-04-loro-undo-spec.md` (increments 2/3).

**Vim still open:** #3 gg/G (attempted + reverted — routing to first/last block focuses the note's trailing empty block → auto-insert mode; muddied by the test mosaic's phantom empty/image blocks; needs a clean-note debug of the focus→insert behavior), #4 f/t `;`/`,` repeat across blocks + match highlight, #8 marks, #9 visual mode (`v`/`Ctrl-v`/`y`). Undo increment-2 (semantic boundaries) blocked on the WS self-echo — see the loro-undo spec.

**Test harness:** standalone Playwright E2E under `web/tests/*.e2e.mjs` (no test-runner in repo). Run vs a static-serving server: `TESELA_STATIC_DIR=$PWD/build TESELA_DEFAULT_MOSAIC=/tmp/mdqa ../target/debug/tesela-server` on :7788, then `node tests/<x>.e2e.mjs` (needs `npx playwright install chromium`).

---

## 2026-06-04 — Step 3 daily win: markdown live-renders in the outliner (web + desktop)

**`**bold**`, `*italic*`, `` `code` ``, `~~strike~~`, and `#..######` headings now RENDER in the block outliner** (`22b04de`), lands on web AND desktop (same `/g` code — the Tauri payoff). Extended `web/src/lib/cm-decorations.ts` `buildDecorations` (the existing live-preview decoration system that already did `#tags`/`[[links]]`/`key::`/fenced-code): style the content + hide the markers when the block editor is NOT focused; **pure raw markdown on focus** (per-block toggle, rebuilt on `focusChanged`). No atomic-range complexity (unfocused = no cursor). Suppressed inside fenced/inline code; bold-before-italic with a `lastIndex=from+1` rewind so a `**bold**` boundary can't swallow the adjacent `*italic*`. **Self-QA'd via Chrome DevTools** on a throwaway mosaic: all five render with markers hidden, raw-on-focus confirmed, other blocks stay rendered, 0 console errors (+ a screenshot). **v2 (`65b4693`): `[text](url)` links, `> ` blockquotes, clean fenced code blocks.** **FINISHED (`8d207da` + `365528c`): images `![](url)` (inline img, http(s) only), highlight `==text==`, horizontal rules `---`, and CLICKABLE md links (open URL in a new tab; mirrors the wiki-link mousedown handler). Security fix `365528c`: the link-open validates the scheme (http/https/mailto only) — a note is untrusted, so `[x](javascript:…)`/`data:`/`file:` are rejected (flagged by automated commit review).** All Chrome-DevTools-QA'd (every element renders, raw-on-focus, 0 console errors, screenshots). **Markdown render is now feature-complete** for the common set. Intentionally OUT of scope: GFM task checkboxes (the app uses `status::`/tags), tables, footnotes. Deferred polish: Tauri external-link-open via the opener plugin, code-block language label, per-line (vs per-block) reveal, relative/attachment images (needs an attachments HTTP route).

---

## 2026-06-04 — Spine 1b-iii (server): snapshot cadence + bootstrap wired into the LIVE relay tick

**The Phase-1 spine mechanism now runs in the live server code, not just the e2e harness** (`a845dbf`). `sync_relay::tick` periodically deposits per-note snapshots covering the applied cursor (env `TESELA_RELAY_SNAPSHOT_INTERVAL_SECS`, default 300s) → the relay compacts its retained op log; and bring-up now `bootstrap_from_snapshots`: if the relay's compaction watermark is ahead of our cursor, import each snapshot + jump the cursor (fresh + long-offline restore), then poll the tail. `RelayState` gained `last_snapshot_at`. Unit test (added `tesela-relay` dev-dep): the LIVE `tick` deposits → relay compacts → a fresh engine restores byte-identically from snapshots alone. **42 tesela-server tests green.**
- **Caveat (by design):** the relay tick is currently DORMANT in prod (`[sync.relay]` bypassed; the live path is the Mac-hub WS). This wires the mechanism so it "just works" when the relay/CF spine is enabled. **Remaining for the spine to go live:** (a) the CF deploy (#189, needs Taylor's account), (b) the iOS `RelayTicker` mirror — needs NEW FFI methods (`put_snapshots`/`fetch_snapshots` aren't exposed through `tesela-sync-ffi` yet), (c) re-point clients at the relay + demote the Mac from hub. Also still open: #195 (Rust relay full-compaction seq reset).

---

## 2026-06-04 — Desktop app (Tauri) — DECISION LOCKED + MVP WORKING on the real mosaic

**Decision (locked, two-Claude + Taylor's own usage all agreed): Tauri-wrap the `/g` web UI, NOT a fresh SwiftUI Mac app.** Reasoning in `decisions.md` 2026-06-03 + the spec `.docs/ai/phases/2026-06-03-tauri-desktop-spec.md`. Short version: `/g` is the most mature surface (CodeMirror+vim, ⌘K, leader, Loro collab); reuse it 100% → step-3 features build ONCE for web+desktop. SwiftUI would rebuild it all, starting from the iOS app which is *behind* `/g`. Native-feel cost ≈ 0 for a vim user in a controlled CM editor. Reversible (FFI/iOS path intact; SwiftUI Mac shelved as a possible premium tier).

**Architecture (built): a native window + a child `tesela-server` bound to LOOPBACK that serves BOTH the API and the static `/g` UI** → the webview loads `http://127.0.0.1:<port>/g`, same-origin, no CORS, the existing host-derived WS just works. **The embedded server is a loopback Loro-replica NODE, not a hub** (127.0.0.1 only, mDNS off) — cross-device sync will flow through the spine (relay/LAN), same as iOS. Do NOT let it bind 0.0.0.0 / become a hub.

**MVP VERIFIED WORKING (2026-06-04) on the real 100-note mosaic.** The native window loaded the full `/g` SPA from the embedded loopback child and exercised the real product: `/g` + `_app/*` chunks, the live-sync `/ws` connected, the rail + daily view pulled real data (`/notes/dailies`, `/notes/tasks`, `/notes?tag=daily&limit=60`, …), and the Loro collab doc bootstrapped (`/loro/notes/2026-06-03/snapshot`). Bind confirmed `127.0.0.1:<port>` only.

**Adversarial review (workflow, 4 lenses) done + all MUST/SHOULD fixes applied + committed (`60e37ad`).** The review's CRITICAL finding: single-writer was the load-bearing invariant but UNENFORCED — fixed with the flock (a 2nd server on the mosaic now fails fast, verified). Also folded in: CORS gated off when embedded (DNS-rebinding), 6 frontend `/api` fetches → `apiBase()` (voice/delete were broken under the same-origin base), watchdog spawn-race closed via `TESELA_PARENT_PID`, two false `/server/restart` env-inheritance doc comments corrected. Follow-ups filed: #201 (tauri build/codesign/notarize + sidecar + lib-embed), #202 (single-instance plugin / restart-no-op / `/properties` stub / CSP).

**What landed (committed `60e37ad`):**
- **T1** `tesela-server`: optional `TESELA_STATIC_DIR` → tower-http `ServeDir` SPA fallback after the API routes (`routes/mod.rs`); `fs` feature added to workspace tower-http. Unset = today's behavior. (+CORS gated off when embedded.)
- **T2** web: `adapter-static` (SPA, `fallback: index.html`; root layout already `ssr=false`); new `web/src/lib/runtime-base.ts` `apiBase()` = `window.__TESELA_API_BASE__ ?? "/api"`; wired into `api-client.ts` BASE_URL + loro `note-doc.defaultBase`. WS already host-derived. `pnpm build` → `web/build`.
- **T3** new `src-tauri/` crate (Tauri 2): picks a free loopback port, spawns the `tesela-server` child (`TESELA_SERVER_BIND`, `TESELA_STATIC_DIR`, `TESELA_DISABLE_MDNS=1`, `TESELA_EXIT_WITH_PARENT=1`), waits for `/health`, opens the window at `…/g`, injects `window.__TESELA_API_BASE__=""`, reaps the child on exit (SIGTERM→graceful, SIGKILL backstop).
- **Lifecycle hardening (data-safety):** a hard SIGKILL of the shell would orphan the child (verified) → an orphan = a 2nd writer on the mosaic. Fixed: `tesela-server` `spawn_parent_death_watchdog` (opt-in `TESELA_EXIT_WITH_PARENT`) exits when `getppid()` changes → `raise(SIGTERM)` on self for graceful shutdown. **Re-tested: SIGKILL the shell → child self-terminates in ~1s, no orphan, port freed.**

**Run:** `cargo build -p tesela-server -p tesela-desktop && cd web && pnpm build && cd .. && TESELA_MOSAIC="$HOME/Library/Application Support/tesela/logseq" ./target/debug/tesela-desktop`. (Don't run a standalone `tesela-server` on the SAME mosaic at the same time — two-writer.)

**NEXT (desktop follow-ups):** bundle a real `.app` (`tauri build`) + codesign/notarize; native menus/tray/auto-update; lib-embed `tesela_server::serve()` (one process vs child — architecturally identical purification); then step-3 feature maturation (markdown render, code blocks, vim polish, properties/types, widgets) which now lands on web+desktop together. Then circle back to the relay live-tick (1b-iii) + the desktop's sync-via-spine.

---

## 2026-06-03 (late) — Phase 2: the Cloudflare Worker relay is WIRE-CONFORMANT (something real)

**The always-on cloud relay exists and is proven byte-identical to the Rust relay** — the encrypted-replica spine now has its cloud endpoint. Built + proven locally without touching Taylor's CF account; production is just the final `wrangler deploy`.

**The artifact:** the TS Cloudflare Worker (`cloudflare-relay/`, one Durable Object per group, DO-SQLite) passes the EXACT same conformance suite the Rust relay passes — **19/19 green against BOTH** the in-process Rust relay AND the Worker via `wrangler dev`.
- **Harness unlock (`397fc30`):** `crates/tesela-relay/tests/conformance.rs` `spawn_relay()` honors `TESELA_RELAY_CONFORMANCE_URL` (+ `_ADMIN_TOKEN`) → the same black-box suite runs against any base URL. "One suite, both implementations" (the file header's long-promised goal).
- **Worker extended to the current protocol (`397fc30`):** found a pre-existing COMMITTED scaffold (May 25, pre-Phase-1) and brought it current — added snapshot endpoints (`PUT /snapshot`, `GET /snapshots`) + snapshot-gated compaction + the `snapshots`/`group_meta` tables; switched ack to DURABLE retention (removed ack-GC, matching the Rust Phase-1a relay); per-IP sliding-window rate limit; body cap → 1 MiB (Rust default; 16 MiB prod override documented); admin delete → 204/404; fixed a DO-SQLite `.one()`-throws-on-zero-rows bug (would 500 instead of 404). The crypto layer (HMAC-SHA256 verify / SHA-256 body hash / canonical_request) was already Rust-correct — the conformance pass empirically proves wire parity (the Rust client drives both servers). wrangler bumped to 4. Run: `cd cloudflare-relay && npx wrangler dev --port 8787 &` then `TESELA_RELAY_CONFORMANCE_URL=http://127.0.0.1:8787 cargo test -p tesela-relay --test conformance`.
- **Adversarial review + hardening (`348603a`):** a skeptical review found real issues the black-box suite didn't exercise — all fixed, suite expanded to 19 (passing on both): `fromHex` was silently coercing non-hex → 0x00 (device-id corruption) → now rejects; the MAC gate no longer over-requires `X-Tesela-Device/Group` (would 401 a client the Rust relay accepts); `POST /ack` now body-capped (was an authed DoS surface); `recordNonce` hardened (bounded). +2 shared tests (test_14 non-hex device → 400, test_15 over-cap ack → 413).
- **Bonus — review found a latent RUST-relay data-loss bug (task #195):** `store.rs insert_op` uses `MAX(seq)+1`, which RESETS to 1 after FULL snapshot compaction (all ops deleted), so a device on `since=watermark` MISSES the new seq-1 op. The Worker is correct (AUTOINCREMENT never reuses seq). Fix the Rust side: seed from `MAX(MAX(seq), compaction_seq)+1`.

**NEXT (Phase 2 finish + beyond):** (a) production `wrangler deploy` — needs Taylor's Cloudflare account/login + verify free-tier fit; (b) 1b-iii — wire the snapshot cadence (auto-deposit) + snapshot-bootstrap into the LIVE desktop `sync_relay.rs` + iOS `RelayTicker` tick loops, and repoint clients at the cloud URL; (c) the LAN P2P track (#190 — transport router + Cloud/Local-only toggle); (d) the still-open step-2 decision: native desktop = Tauri-wrap-`/g` vs fresh SwiftUI. The roadmap order (infra→platform→features→collab) is in `roadmap.md`.

---

## 2026-06-03 (eve) — NEW MILESTONE: multi-transport encrypted-replica sync spine

**Pivot after collab editing shipped:** the reframe is **correctness is solved → availability + identity is the new axis**. Goal: multi-device works when the Mac is off + an always-available encrypted off-site copy of all notes. **Decided architecture (Taylor):** a durable, **E2E-encrypted, always-on cloud REPLICA** (Cloudflare Worker + Durable Object + R2; ~free; zero-knowledge — crypto already exists) that is BOTH the sync spine AND a backup; **PLUS LAN peer-to-peer** (devices sync directly on the same network) with a **user toggle: "Cloud sync" vs "Local-only / device sync"** (Anytype-style). One transport-agnostic sealed TLR2 delta + a client-side transport router. **Cloud is THE spine** (Mac → equal client + .md materializer, not the hub). **Native desktop is the endgame, not web** → the multi-transport sync lives in the shared Rust `tesela-sync` core (FFI → native desktop + iOS); web is a thin secondary. Confirmed: TS Cloudflare Worker (not Rust→WASM); browser key = non-extractable WebCrypto via pairing; device-initiated compaction. Honest constraint: browsers can't do true LAN P2P (local-only web works only via the Mac LAN server). **Spec: `.docs/ai/phases/2026-06-03-encrypted-replica-spine-spec.md`.** Design grounded by 3 workflows (readiness assessment + spine arch + CF encrypted-replica).

**Phase 1 COMPLETE (mechanism, e2e-tested) — the encrypted-replica spine's local/self-host story works.**
- **1a (`e936f56`):** relay durably RETAINS the full encrypted op log (removed `gc_fully_acked_ops`) → it's a zero-knowledge encrypted backup + bootstrap source. e2e: empty engine restores whole mosaic via `since=0`.
- **1b-i (`e8be948`):** relay snapshot store (`relay_snapshots` per-(group,opaque-stream_id) + `relay_group_meta` watermark) + `PUT /snapshot` (batch, sets watermark + GCs ops `seq<=covers_seq`) + `GET /snapshots`. One txn `deposit_snapshot_batch`. 3 conformance tests.
- **1b-ii (`30f66c7`):** client `RelayClient.put_snapshots`/`fetch_snapshots` (AEAD-seal/open, group-only `snapshot_aad` domain-separated so any member opens any member's snapshot) + engine reuse (`export_doc_update(id, None)` = full snapshot; `import_doc_update`). **e2e proves the full loop:** A authors 3 notes → push → deposit snapshots → relay GCs the deltas → fresh empty C bootstraps PURELY from the compacted snapshots → byte-identical. `cargo test -p tesela-relay` 22 green; `tesela-sync` green.
- **Decision (Taylor delegated):** per-note snapshots keyed by an OPAQUE stream_id (v1 = note_id; HMAC-opaque key = privacy follow-up). Not whole-mosaic (fights iOS lazy-load); not raw-leak.

**NEXT: wire the snapshot cadence + snapshot-bootstrap into the LIVE desktop `sync_relay.rs` + iOS `RelayTicker` tick loops (1b-iii, folds into Phase 2 client-repoint), then Phase 2 = the Cloudflare Worker + DO + R2 deploy (#189).** Product roadmap order agreed + written to `roadmap.md` (infra→platform→features→collab). **Open decision for step 2: native desktop = Tauri-wrap-`/g` (lean) vs fresh SwiftUI macOS.**

---

## 2026-06-03 (PM4) — C2.0 wire-compat DE-RISK PASSED: web-as-Loro-peer is feasible

**The make-or-break question is answered YES** (commit `92a8e52`): a JS `loro-crdt@1.12.3` peer natively imports the Rust `loro 1.12.0` server's snapshot bytes (`GET /loro/notes/{id}/snapshot`, 12934 bytes) → decodes the `blocks` movable-tree (9 blocks) → reads `block_id` + `text_seq` ("Hey steve"); + JS↔JS concurrent splices converge. Wire format is shared (same loro-dev monorepo, 1.x encoding). So C2 (web as a real Loro delta peer) is sound. Added `loro-crdt@1.12.3` (pnpm) + SSR-safe `web/src/lib/loro/loro-client.ts` (browser-gated dynamic import; no vite/wasm config needed; build green). Wire-check script `web/scripts/loro-wire-check.mjs`. **Finding for C2.3:** JS `LoroText` has `insert(unicodeIndex)` + `insertUtf8`/`deleteUtf8` but NO `insertUtf16` — convert the editor's UTF-16 offsets via `text.convertPos(i, "utf16", "unicode")`.

**Collab-editing milestone status (approach c, spec `2026-06-03-collab-editing-spec.md`):**
- C1 splice FFI (`e939da1`) ✓ · C1 outbound CollabTextView (`d7180e3`) ✓ · **C1 inbound (`78c5fe0`) ✓ — web→iOS live-apply VERIFIED LIVE on the sim.** When a remote splice lands on the block the iOS user has open, `MockMosaicService.reconcileOpenBlockLive` reads the merged engine text (new FFI `read_block_text`) and `CollabTextInserter.reconcile` applies a minimal UTF-16 diff + caret remap to the focused UITextView. Verified: 3 consecutive web edits (WEB!→OK!→GO!) all landed live in the focused iOS editor, caret correctly preserved after the user's own text (logs: `applied=1` → `read merged=…` → seqOk/bidOk → reconcile). **Gotcha that cost an hour: the app defaults to the LEGACY `AppShell` — must launch `simctl launch … -graphite` (or set `tesela.useGraphiteShell`) to get `GrAppShell`/`GrDailyView` where all the C1 wiring lives; without it `editingBlockId`/`readEngineBlockText`/`openBlockInserter` are all nil.** Adversarial review found+fixed 4 defects pre-verify (caret right-gravity, async-read mirror-overwrite race → seq guard, stale inserter on block-switch). iOS→web outbound is device-verified + code-unchanged (idb `ui text` bypasses the splice delegate, so the sim can't re-verify it). Residual limits in follow-up #187 (async-window misplacement; per-op-diff FFI to match web's event-based apply).
- C2.0 de-risk (`92a8e52`) ✓ · C2.1 TLR2 codec + binary WS (`f20f3f5`) ✓ · C2.2 per-note Loro doc + bootstrap + inbound apply (`68f8100`) ✓ — **web doc CONVERGES with the server over the live delta channel** (converge-check PASS; `blake3(slug)[..16]` confirmed on the wire; `NoteDoc.open/applyInbound/blockTextByBid/subscribe`; `@noble/hashes` added; 226 tests green; SSR clean). · C2.3 (`faeffdd` build + `4c92d6a` fix) ✓ — **web↔web same-block concurrent editing MERGES, verified LIVE in two browser tabs on the real mosaic** (same-position Z1+Z2 converge in both tabs, no clobber; append-at-end propagates as a binary splice; gates green). Spec inversion the agent found & verified: loro-crdt 1.12.3 JS `LoroText.insert/delete` + the subscribe `TextDiff` are UTF-16 indexed (NOT unicode) — CM offsets pass straight through, NO `convertPos` (matches Rust `insert_utf16`). **Two real runtime bugs caught only by the in-browser two-tab test, both fixed in `4c92d6a`:** (1) `NoteDoc.defaultBase()` returned `""` → snapshot fetch `/loro/...` hit the vite SPA 404 (vite only proxies `/api/*`→server, rewriting `/api` off) → doc bootstrapped EMPTY → no block bound → EVERY edit fell to the whole-text HTTP clobber path. Fix: `defaultBase()`→`/api`. The C2.2 "convergence proof" used the headless node check w/ an ABSOLUTE base, so the proxied browser bootstrap was never exercised; the real `/loro/...snapshot` GETs in the server log were iOS hitting :7474 directly. (2) CM doc carries the hidden trailing `<!-- bid:… -->` comment, so a caret past doc-end → offset > clean LoroText length → `spliceBlock` threw → HTTP fallback. Fix: clamp the splice to the LoroText UTF-16 bounds (→append). Follow-up #186 = precise offset map if hidden MID-text markers ever appear.
- C3 (#185, iOS↔web same-block wire-verify + remove DIAG) — web↔web ✓, web→iOS live-apply ✓ (sim), DIAG stripped. REMAINING: iOS→web outbound re-verify on the user's physical devices (idb can't drive the splice delegate); deploy `78c5fe0` to Roshar/Sel for the full bidirectional same-line test.
- **Net:** Same-LINE simultaneous collaborative typing now WORKS web↔web (verified two tabs) and web→iOS (verified sim, focused editor live-merges with caret remap). iOS→web outbound was device-verified earlier + is code-unchanged. **DEPLOYED to Roshar (iPhone 15 Pro) + Sel (iPad Air M2) — the C1-complete build (`78c5fe0`) for the user's bidirectional two-device same-line test.** All DIAG removed.
- **Web collab works on `/g` (Graphite), the route the user actually runs (`a930142`).** The C2.2/C2.3 Loro binding was only in `/v4` (legacy); `/g` never opened the active NoteDoc → editors fell back to HTTP refetch → live edits "drifted" until manual refresh (user-hit). Ported `openActiveNoteDoc(focusedSlug)` into `GraphiteShell` (default daily empty pageId → local today date). Verified on `/g`: outbound edits go binary (no POST /blocks); a peer's same-block edit live-merges (kept both, no clobber). **On-device merge confirmed working 100% by the user (iOS↔iPad combined both blocks); the only gap was this web-side `/g` drift, now fixed.**
- **Graphite is now the DEFAULT shell (`20920b7`).** ALL collab editing (C1/C2) lives in `GrAppShell`/`GrDailyView`; the legacy `AppShell`/`DailyView` has NO collab editor. Before the flip, tapping the icon launched legacy (no collab) — collab only worked via `-graphite`. Now icon-tap → Graphite; legacy is behind `-legacy` / `tesela.useLegacyShell`. (This cost ~1h of mis-diagnosis on the sim before I realized the shell gate.)
- **iOS sim test recipe:** `simctl launch <udid> app.tesela.ios -graphite`; backend via `defaults write app.tesela.ios backend.mode -string http` + `onboardingComplete -bool true` (serverURL defaults to 127.0.0.1:7474, which the sim shares with the Mac). FFI rebuild after a Rust FFI change: `cargo build --release -p tesela-sync-ffi --target aarch64-apple-ios-sim` + regen bindings (`cargo run -p tesela-sync-ffi --features cli --bin uniffi-bindgen -- generate --library target/debug/libtesela_sync_ffi.dylib --language swift --out-dir app/Tesela-iOS/Generated`) + **copy the new header into `app/Tesela-iOS/CFFI/` (the build's SWIFT_INCLUDE_PATHS), not just Generated/**.

---

## 2026-06-03 (PM3) — Collab-editing milestone (approach c) started: splice FFI foundation landed

User chose approach (c) — true same-block collaborative editing — after the re-base fixed lineage/different-block sync. Spec `.docs/ai/phases/2026-06-03-collab-editing-spec.md` (+ C1 iOS code map). Wire-proven root cause: both iOS + web re-author whole block text from a locked editor → `LoroText.update` deletes the peer's concurrent chars. Fix: editors become live views over the per-block `LoroText` (char splices + live-apply inbound + cursor remap).

**C1 step 1 DONE — `splice_block_text` (commit `e939da1`, main):** engine `LoroEngine::splice_block_text(note_id, block_id, utf16_offset, utf16_delete_len, insert)` (get-or-create the SAME `text_seq` LoroText, `delete_utf16` then `insert_utf16` = replace, then BlockUpsert persist/materialize tail) + `SyncEngine` trait method + FFI `SyncEngineHandle::splice_block_text(slug, block_id_hex, …)` (slug→note_id via `stable_uuid_from_slug`; accepts dashed/dashless bid). Uses Loro's `insert_utf16`/`delete_utf16` so iOS `NSRange` + JS UTF-16 offsets map with no conversion. 4 tests incl. `splice_block_text_concurrent_inserts_interleave` (two concurrent splices to ONE block MERGE) + multibyte/emoji UTF-16 + replace + unknown-block-noop. `cargo test -p tesela-sync` 117 + `-p tesela-sync-ffi` 10 green. NOT yet wired to iOS; bindings NOT regenerated.

**C1 step 2 DONE — CollabTextView outbound splices (commit `d7180e3`):** bindings regenerated (`spliceBlockText` in Swift); new `Components/CollabTextView.swift` = `UIViewRepresentable<UITextView>` emitting char splices on `shouldChangeTextIn` (NSRange = UTF-16) → `MockMosaicService.spliceTodayBlock` → `onLocalSplice` → `RelayTicker.spliceAndPush` → FFI `spliceBlockText` → produce/send/commit delta. Editor loads the engine-EXACT block text (`combinedEditableText`/`engineExactText`, tags inline = the `text_seq` value; non-destructive `splitTrailingTags`, no normalization → splice offsets align). Toolbar inserts route through the splice seam. xcodebuild SUCCEEDED. **Sim smoke: editor renders, typing works + syncs end-to-end, no crash** (verified a block edit reached the server). Flagged: brand-new block's first keystroke may no-op if it races the create-writeback (only the creating device touches it — no clobber). **NOT yet cleanly verified:** the today-block splice-MERGE on the wire (idb tapping the 180°-rotated sim is unreliable; engine test `splice_block_text_concurrent_inserts_interleave` proves the merge; decisive runtime check = DIAG wire trace + user device repro, best done WITH C2).

**C1 step 3 (deferred to increment 2):** inbound live-apply of remote splices into the ACTIVE editor + caret remap (so you SEE a peer's edit mid-typing); narrow `isEditingBlock`. Until then, OUTBOUND is splice-based (iOS no longer DELETES a peer's concurrent text — the iOS→web clobber direction is fixed), but the user won't see the peer's edit live while typing.

**Remaining for the FULL bidirectional fix:** C2 — web as a loro-wasm peer (#184): web still sends whole-text via HTTP, so web→iOS can still clobber until web also emits splices. Then C3 integrated wire-verify (iOS↔web same-block interleave) + remove DIAG (#185).

---

## 2026-06-03 (PM2) — web↔device disjoint-lineage fix LANDED + deployed (authoritative re-base)

**The real fix shipped** (root cause in the PM entry below). Devices on a disjoint Loro lineage now deterministically RE-BASE onto the server's lineage, so live web deltas apply + the device stops clobbering web.
- `461293b` engine/FFI: `import_authoritative_snapshot` (server-wins re-base — keep the snapshot-origin node per twinned block_id, tombstone the device's; min-TreeID fallback) + `rebase_twins_onto_snapshot`; `apply_doc_update_status` surfaces loro `ImportStatus.pending`; FFI `apply_delta_frame` → `DeltaApplyOutcome{applied, needs_catchup, note_ids_hex}`; `import_note_snapshot` rerouted to the authoritative path. `import_doc_update` UNCHANGED. Trait default impls forward for non-Loro engines.
- `633b4df` iOS: `applyInboundDelta` — on `needsCatchup` (a delta loro left PENDING ⇒ disjoint/gap), force a full-snapshot authoritative re-base of the visible note (bypass the per-slug debounce; in-flight guard collapses a burst to one fetch). Bindings regenerated for `DeltaApplyOutcome`.
- New test `disjoint_device_authoritative_rebase_then_converges`: after re-base the device has ONE node for the block AND a subsequent device edit + concurrent web edit MERGE (shared lineage) — proves the re-base, not just a text patch.

**Verified:** `cargo test -p tesela-sync` 113 + integration (incl. the 3 disjoint tests, ws_apply_*, snapshot_bootstrap_converge, concurrent_whole_body_clobber, loro_cutover_spike) + `-p tesela-sync-ffi` 7 — all green. Adversarial review **APPROVE** (data-loss-safe: only disjoint twins discarded, never shared-lineage/server data). FFI `.a` rebuilt (device + sim). Sim build SUCCEEDED; **clean-path web→sim live delivery still works** (curl edit appeared live). **Reinstalled on Roshar + Sel.** Server unchanged (the fix is iOS-engine-side; `import_doc_update` + wire format untouched) — no server restart needed.

**[ ] USER device round-trip:** open the app on Roshar + Sel (HTTP, `http://100.112.34.59:7474`). A disjoint device self-heals on the first inbound web delta (force re-base). Then web↔iPhone↔iPad should sync live both ways, no clobber. A fresh install also works (clean bootstrap). Tradeoff (accepted): the re-base discards a device's UN-SYNCED disjoint-twin edits — fine since you're not relying on local data yet.

---

## 2026-06-03 (PM) — web↔device sync ROOT-CAUSED: disjoint Loro lineage; live deltas can't apply (PROVEN, fix pending)

**Symptom (user, devices):** device↔device WS sync works, but web HTTP edits don't reach devices LIVE (only after a hard refresh) + web edits get clobbered.

**PROVEN root cause** (engine tests `crates/tesela-sync/tests/disjoint_device_catchup.rs`, both green; + a live curl→sim repro where a clean sim DID get the web edit live):
- A device that authored a note WITHOUT first importing the server's snapshot is on a DISJOINT Loro lineage.
- `disjoint_device_live_delta_does_not_converge`: the LIVE delta the server broadcasts on a web HTTP edit (`notes.rs upsert_blocks` → `export_doc_update(pre_vv)`) CANNOT apply on a disjoint device — the server-lineage ops reference a tree node the device never imported, so Loro buffers them PENDING and the render stays stale.
- `disjoint_device_catches_up_to_server_lineage`: a FULL-snapshot catch-up (`bootstrapNoteIfNeeded`) DOES heal it (adopts the server text). → why a hard refresh works.
- iOS display prefers the local engine-materialized file (`MockMosaicService.refresh` ~954 `readLocalNote(id:) ?? daily`), so until the engine heals, the display stays stale even after an HTTP refresh.
- A clean sim (bootstrapped before authoring) gets web edits LIVE (verified curl→sim) — so the protocol + server are sound; the bug is purely devices being on a disjoint lineage.

**Server already broadcasts web edits as Loro deltas** (`upsert_blocks` captures `pre_vv` before applying, exports the delta, `ws_delta_tx.send(origin:None)` to all sockets) — so the other-session hypothesis "web isn't a Loro peer, so web→device isn't wired" is INCOMPLETE; the path exists, it just can't apply on a disjoint device.

**First fix attempt REVERTED (was ineffective — caught in review):** gating mock-mode authoring (already gated upstream at `MockMosaicService:850/1309`, so redundant) + an `applyInboundDelta` `applied==0` catch-up trigger (DOESN'T fire: `apply_delta_frame` (`tesela-sync-ffi/src/lib.rs:518`) returns the count of decoded ENTRIES, not materialized ops — a disjoint delta returns `applied=1`, and `import_doc_update` returns Ok even with pending ops). Reproduction tests kept (committed).

**THE REAL FIX (pending, milestone-sized):**
1. **Engine/FFI: report non-convergence.** `import_doc_update`/`apply_delta_frame` should surface whether the import left PENDING ops (Loro `ImportStatus.pending`) — i.e. the delta didn't integrate (disjoint or causal gap).
2. **iOS: on non-convergence, force a catch-up** for the affected note (bypass the `catchupMinInterval` debounce).
3. **Engine: make the catch-up AUTHORITATIVELY re-base** — when importing the server snapshot, for any block_id with twins KEEP the server's node and tombstone the device's (deterministic server-wins, not min-TreeID). This adopts the server's lineage so (a) live deltas then apply and (b) the device's own edits stop minting disjoint twins that clobber web. Discards un-synced disjoint local edits — acceptable (server is truth; user not relying on local data).
4. Verify: extend `disjoint_device_*` so the authoritative re-base → device on server lineage → device edit + web edit MERGE (not clobber). Then FFI rebuild + sim-verify + device deploy.

**Disjoint TRIGGER still unconfirmed** (mock authoring is already gated, so that's NOT it). Candidates: bootstrap failure/404/race in HTTP mode, or pre-fix residual state. The authoritative re-base fixes it regardless of trigger (self-heals any disjoint device), so it's the right robustness fix.

---

## 2026-06-03 — Fresh-install live-sync bug: GrAppShell didn't re-establish WS/hubMode on a runtime backend change

**Symptom (user, devices):** after a fresh install + setting the server URL in Settings, HTTP refresh worked but **live sync was dead both ways** (web→device needed a hard refresh; device edits reached nothing) + SYNC card showed `FfiSyncError.Other("Mac has no relay configured")`, Last tick "never", 0 ops.

**Root cause (confirmed by code + live server log):** `GrAppShell` wired `liveSync.connect` + `relayTicker.hubMode` ONLY in `.task` at launch. `backend.mode` defaults to `"mock"`, so a fresh install launches in mock mode (no WS, no hub mode). `GrSettingsView.save()` then sets the URL/mode + re-attaches HTTP **but never reconnects the WS or sets hubMode**, and there was no `onChange` to react. So the relay coordinator kept spinning (the `/sync/peer/pairing-code` loop + "no relay" error in `/tmp/tesela-srv.log`; the Mac is relay-`configured:false` by design — hub-WS architecture). **Same root cause as the earlier "switching mosaics wasn't working."**

**Fix (`<commit>` GrAppShell):** extracted the backend-dependent bring-up into `activateBackend()` (attach/refresh/`hubMode` set BOTH ways/`liveSync.connect`/daily bootstrap/`start`), called once from `.task` AND re-run via `.onChange(of: backendToken)` (mode|serverURL) on a runtime backend change. Mirrors the old `AppShell.activateMosaic` re-run pattern. `LiveSyncSocket.connect` is re-entrant; the rest idempotent.

**Verified:** sim build SUCCEEDED; adversarial review APPROVE (traced the SwiftUI reactivity chain + re-entrancy + no lost wiring); **runtime sim check** — launching `.http` connects a NEW `/ws` (10→11), refreshes, fetches the daily `/loro` snapshot (bootstrap), and does NOT spin the relay pairing-code loop (hubMode gated). **Reinstalled on Roshar + Sel** (graphite flip, reverted; tree clean). Workaround that also works: force-quit + reopen (the `.task` re-reads the persisted `.http`).

**[ ] USER:** open the app on each device (now no relaunch needed after a Settings change) → live web↔iPhone↔iPad sync should work both ways. **Note:** the old `AppShell` has the same one-directional `hubMode` bug (only ever set true) — out of scope, candidate for the same `else { hubMode = false }` fix if that path ever needs runtime mock switching.

---

## 2026-06-02 (PM) — Block text is now a real CRDT (LoroText) — the 4th & deepest data-loss vector closed

**The bug (wire-proven on devices):** a block's text was a Loro LWW **map register** (`meta.insert("text", ...)`). Two peers editing the SAME block concurrently → higher-(lamport,peer) whole-text write wins, the other side's typing silently lost. This is distinct from the three prior clobbers (disjoint-history twins / stale whole-body PUT / WS snapshot push) — all of those protected WHICH blocks apply; none addressed how one block's concurrent text combines.

**Fix (engine-only, approach b — zero client/wire change):** block text → nested **`LoroText`** sequence CRDT under a new `"text_seq"` key, written via `get_or_create_container` + `update()` (Myers-diffs whole text → minimal splices that interleave). Clients keep sending whole text; `OpPayload`/diff.rs/FFI/note_tree/relay all UNCHANGED. The WS/relay path merges text deltas automatically once text is a LoroText.
- `69fadc8` engine: `write_block_text`/`read_block_text` (non-mutating, prefers text_seq, falls back to legacy `text` register for old snapshots — lazy migrate-on-write, no reseed needed). All readers routed through `read_block_text`. Discriminator (`peer_genuine_block_changes`) + WS-apply heal REWRITTEN: the old `JsonMapOp::Insert{key:"text"}` scan went dead; on a SHARED lineage LoroText merge makes raw-import safe (case a obviated), so the heal is now scoped to DISJOINT twins only (gated `twin_bids.is_empty()` early-return; `server_block_text_history` op-replay only runs when a twin exists). Subagent-built, two-stage reviewed (spec ✅ COMPLIANT + quality APPROVE).
- `43edfee` e2e wire test `ws_concurrent_same_block_edit_merges_over_real_socket` (tesela-server): real socket + real HTTP, shared base, server "web" edit via `POST /notes/merge/blocks` + device WS edit to same block → asserts CHARACTER MERGE (brown+red+jumps, neither lost, NOT LWW). Reproduces the exact "web clobbered by iOS" incident; pre-fix asserts-fail.
- DIAG diagnostics REMOVED (`TESELA_DIAG_WRITES` write-body middleware + ws.rs before/after logging — they were uncommitted; working tree restored clean, no DIAG in source).

**Verified:** `cargo test -p tesela-sync` 113 lib + all integration green (incl. new `concurrent_same_block_text_merges_not_clobbers`, disjoint/clobber/cutover/base-diff suites); `-p tesela-sync-ffi` 7 green; `-p tesela-server` 31 green (incl. the new e2e). Three independent merge proofs: engine convergence test + FFI delta round-trip + real-socket e2e.

**LIVE NOW:** server REBUILT + RESTARTED on the clean LoroText binary — flag-free `target/debug/tesela-server --mosaic "<real logseq mosaic>"`, `RUST_LOG=info,loro=warn`, log `/tmp/tesela-srv.log`, `/health` 200. **Web is correct immediately** (no local engine → uses the server's LoroText path). Device-target + sim-target FFI `.a` both REBUILT.

**MIGRATION HAZARD — CLOSED.** iOS runs its own engine via the FFI; an OLD-FFI device writes the legacy `text` register, shadowed once the server migrates a block to `text_seq`. **Both physical devices now run the new LoroText build** — `app.tesela.ios` reinstalled on Roshar (A885F93A-…) + Sel (3212F155-…) via devicectl, graphite shell (temp `useGraphiteShell→true` flip, reverted; tree clean), BUILD SUCCEEDED. So server + web + iPhone + iPad are all on the new engine. Boot loaded persisted snapshots (no mass migration; existing blocks stay legacy until edited), so at-rest data is fine.

**[ ] USER — multi-device round-trip product test (#140):** open Tesela on web (`localhost:5173/g`) + Roshar + Sel (each pointed at `http://100.112.34.59:7474`, HTTP mode), edit the SAME block on two at once → should MERGE, no revert/clobber. Mechanical correctness already proven 3 ways (engine + FFI + e2e socket); this is the hands-on feel/round-trip confirm.

**Deferred (non-blocking robustness, backlog #177/#178):** LoroText `update_by_line` timeout fallback for >50k-char blocks; bound `server_block_text_history` op-replay on large twinned notes. **Deferred (approach c):** web emits true CodeMirror splice ops for cursor-accurate same-region merges (iOS stays whole-text).

---

## 2026-05-31 (PM) — First convergence fix REGRESSED the live path; delivery-layer redesign in progress

**The device test FAILED.** The engine-convergence fix (below) was correct at the engine level but the LIVE DELIVERY layer regressed: iOS froze, web edits didn't show on iOS, edits reverted. Engine tests proved convergence-given-a-base but never drove the live path — my miss.

**Evidence-based re-diagnosis (not theorized):**
- **Partial WS deltas can't bootstrap a base-less device** — `crates/tesela-sync/tests/partial_delta_needs_base.rs` (PASSES): `export_doc_update(note, Some(pre_vv))` into an empty doc → renders "" (pending). The first fix bootstrapped on the WRITE path, so a receive-only device never got the base.
- **iOS display = HTTP refresh, not the engine.** Inbound WS events only TRIGGER `applyRemoteChange()` → full-note `refresh()` + `refreshLoadedPages()`. So each inbound edit = a full-note re-fetch; a burst → refresh storm → freeze → refreshes never visibly land = "never updated".
- **`RUST_LOG=info` made it worse** — Loro logged ~10 lines per snapshot export, making server snapshot responses ~500ms (vs 12ms at `loro=warn`). FIXED: server restarted `RUST_LOG="info,loro=warn,loro_internal=warn"`.
- Server data ended intact (no dup bids) — revert was live LWW ping-pong + frozen UI, not disk corruption.

**Corrected plan: `phases/2026-05-31-multidevice-delivery-redesign-spec.md`. SIM self-QA gate held** (user approved "fix it properly now, sim-verified").
- [x] **T1 #151** server request logging (`TraceLayer`, `dad4cc2`).
- [x] **T2 #152** iOS bootstrap-on-OPEN via `mosaic.onNoteOpened` (`53afae6`).
- [x] **T4 #153** iOS refresh coalescing — 300ms debounce in `MockMosaicService.scheduleRemoteRefresh` (`29a5528`). **SIM-VERIFIED:** 5-edit burst → exactly 1 sim refresh (was a storm), responsive, web→sim works.
- [x] **T6 #155** ⭐ **THE DOMINANT BUG was WEB-side** (`68d64f3`): web used `invalidateQueries(["notes"])` — a PREFIX match → every mounted notes query refetched per save-echo (~14/edit storm), and the stale daily-list reseeded the editor body → "web edits clear on refresh". Fix: `web/src/lib/ws-refresh-coordinator.ts` (300ms coalesce + 1.5s own-echo suppression) + `api-client` records local saves + `+layout` routes through it. **PLAYWRIGHT-VERIFIED:** 5-edit burst → 1 coalesced refetch pass (~7 reqs, was ~35-70); a typed web edit PERSISTED across a full reload, NO revert. svelte-check clean, 146 unit tests pass.
- [ ] **T3 #150** iOS DELTAS-not-snapshots (real `sinceVv`) — DEFERRED: the iOS storm was already killed by T4 coalescing; deltas are now just a frame-size/perf nicety, not correctness.
- [ ] **T5 #154 / device round-trip (USER)** — the dominant fixes are verified in-hand (web persist/no-revert + sim no-storm). Remaining: full multi-device (web + iPhone + iPad) concurrent test on real devices.

**⚠ Roshar still runs the MORNING build** (engine fix only — NO T2/T4 coalescing). Before a valid multi-device device test, rebuild+reinstall Roshar (clean sandbox) with the T2/T4 build, and clean-install Sel (was paired-not-connected). **Web is already fixed + live** (Vite HMR'd `68d64f3`) — the user can test web editing now by reloading `localhost:5173/g`.

**Current safe state:** server fixed-binary live + quiet + request-logging (`/tmp/tesela-server-fix.log`); web fix live + verified; engine fix (dedup/heal/bootstrap/relay-gate) committed. Web editing should no longer revert.

---

## 2026-05-31 (AM) — [SUPERSEDED claim] Multi-device REVERT engine-fix (correct at engine level; live path regressed — see PM section)

**Symptom (user):** with iPhone (Roshar) + iPad (Sel) both open, web edits stopped persisting — refresh "cleared them away". Worked a while, then broke.

**Root cause (CONFIRMED by deterministic repro):** Loro tree node identity is the internal `TreeID` (peer+counter), NOT our `block_id`/bid. The Mac seeds note docs from disk; iOS `recordNoteDiff` re-authors blocks from its OWN markdown into a doc that **never imported the server's doc as a base** → `BlockUpsert` mints a NEW TreeID per bid under the iOS peer → same bid = two TreeIDs. iOS shipped a full snapshot every keystroke; server imported it and Loro **unioned** the twins. `note_tree_from_doc` rendered both (no dedup); the next web block-diff save updated only ONE twin (FxHashMap scan order = nondeterministic), leaving a stale ghost = "revert". Self-heal quirk: a title/frontmatter edit takes the NoteUpsert reseed path → "works for a while". The disabled-on-Mac relay didn't help because the DEVICES (cached pairing code, shared engine handle) were the injection vector.

**Fix (user chose FULL — heal + converge). Spec: `phases/2026-05-31-multidevice-converge-spec.md`.** Built subagent-driven, two-stage review each, repro test red→green.
- **E1 dedup-by-bid** (`5b05306`,`d1d7b49`): `dedup_twins_by_block_id` (deterministic **min-TreeID** — loro 1.12 exposes no per-text recency, so it's a LOSSY heal, NOT recency-aware) wired into `note_tree_from_doc` (render) + `tombstone_duplicate_twins` in `import_doc_update` (heals on-disk corruption). Repro reframed into T-heal (deterministic non-dup) + T-converge (shared-base correct text).
- **E2 relay gate + B WS cap** (`cc48174`,`09cbb63`): `RelayTicker.hubMode` (gates `tickOnce`/`recordAndPush` coordinator, `dropCoordinator()` on set, cache NOT cleared → reversible); set in BOTH shells under `.http`. `SyncState` `task.maximumMessageSize=64 MiB` (was silently dropping >1 MiB snapshot frames).
- **D shared-base bootstrap** (`b3b5eef`,`979b2ff`,`2f1b729`,`f381e14`): `GET /loro/notes/{id}/snapshot` (mirrors get_loro_index) → FFI `import_note_snapshot` (+regen bindings, +rebuilt device/sim `.a`) → iOS `bootstrapNoteIfNeeded(slug:)` imports the server doc **before first author** (gated on `noteVersion!=nil`, best-effort). Then `recordNoteDiff`'s BlockUpserts resolve to the EXISTING server nodes → true convergence. Reviewer verified the slug/path match makes this hold.
- **Skipped C** (deterministic TreeID-from-bid): loro 1.12 forbids caller-chosen TreeIDs (`pub(crate)`).

**Verified (code):** `cargo test -p tesela-sync` 110 green (incl. T-heal+T-converge), `-p tesela-server` 29 green (incl. `snapshot_bootstrap_converge`); `xcodebuild` (device) → BUILD SUCCEEDED.

**LIVE NOW:** server REBUILT + RESTARTED on the fixed binary — `target/debug/tesela-server --mosaic "<real logseq mosaic>"` (RUST_LOG=info, log `/tmp/tesela-server-fix.log`); `/loro/notes/2026-05-31/snapshot` → 200 (26 KB). Roshar **uninstalled→reinstalled fresh (clean sandbox) + launched** on the fixed Graphite build (flag flip reverted, tree clean).

**PENDING — USER device round-trip (their step; the clean-sandbox part matters):**
- Roshar ready. **Sel (iPad) is paired-but-NOT-connected** — must be connected, then Claude builds+installs there too (clean install). Phone backend serverURL must be `http://100.112.34.59:7474` (Mac Tailscale IP), HTTP mode.
- **CLEAN SANDBOX REQUIRED:** bootstrap SKIPS already-resident docs, so a device still holding PRE-FIX disjoint docs won't re-base them (only the lossy E1 server tombstone heals those). That's why Roshar was uninstalled first; do the same for Sel.
- Test: edit on Mac web `/g` → Roshar <1s; edit on Roshar → web <1s; concurrent edits on web+iPad+iPhone on the SAME note → converge, **no duplicated bullets, no revert**.

**Follow-ups:** #150 iOS snapshot→delta (real `sinceVv` now base is shared) + relay re-export-snapshot + latency; `flushPendingOutbound` not hub-gated (0 callers — guard if ever wired); slug not percent-encoded in iOS `endpoint()` (codebase-wide latent, slugs URL-safe by convention); `cargo install -p tesela-server` to refresh the PATH binary (currently running the debug build from `target/`).

---

## 2026-05-30 (PM) — Instant multi-device sync: Phases 0/A/B landed (engine + server + FFI)

**Active milestone: instant multi-device sync (Mac-hub WebSocket over Tailscale).** Approved + red-teamed spec: `phases/2026-05-30-instant-multidevice-spec.md`. Goal: Mac+phone edits appear <1s, conflict-free, relay bypassed (relay/RTC redesign stays deferred beyond this). Subagent-driven, commit per phase, two-stage review each.

- **Phase 0** (`453db61`): lifted `doc_version`/`export_doc_update`/`import_doc_update` onto the `SyncEngine` trait (were concrete-LoroEngine-only — the FFI holds `Arc<dyn SyncEngine>` and couldn't call them) + fixed a latent recursion trap in `apply_relay_updates` + added `trait_level_delta_methods_converge_cursor_free` test. Relay path untouched. `cargo test -p tesela-sync --lib` = 100 passed.
- **Phase B** (`21029b3`): iOS FFI `SyncEngineHandle` gained `produce_note_delta` / `apply_delta_frame` / `note_version` (cursor-free, TLR2-framed `Vec<u8>`↔Swift `Data`). Bindings regenerated (uniffi 0.31 library mode, mirrors c626d25) into `Generated/` + `CFFI/`. FFI tests pass. **Review verified** the catch-up VV concern is NOT a bug (Loro export(updates(&vv)) is correct; the "clamp" idea was a no-op) — the real requirement is bidirectional catch-up, recorded in Phase D.
- **Phase A** (`4fdaf72`): server is now a real-time hub. New `ws_delta_tx: broadcast::Sender<WsDelta>` (separate binary channel from text `ws_tx`), bidirectional `/ws` (forwards binary delta frames + reads inbound → apply → emit `WsEvent::NoteUpdated` for web + re-fan-out), per-conn-id echo-suppression, emit-on-apply across HTTP/WS-inbound/relay-tick origins. `sync_relay::tick` → `TickOutcome{applied, sent, applied_note_ids}`. **Review confirmed** the two load-bearing invariants sound (cursor rule upheld — pre-VV captured before mutation; loop-freedom sound; concurrency correct). Two Important findings both confined to the DORMANT config-bypassed relay path (snapshot-not-bytes re-export; spurious multi-note WsEvents) → deferred to Phase D. `cargo test -p tesela-server` = 26+2 passed, `--workspace` green.

### Phase C — LANDED + review-fixed (`eacc6f4`, `fb31e9c`)
iOS `LiveSyncSocket` now bidirectional + binary-aware: `.data` frames → `onBinaryDelta` → `relayTicker.applyInboundDelta` (engine owner mediates); local write → `recordAndPush` (records) → `relayTicker.produceDeltaFrame` (cursor-free export) → `liveSync.sendDelta(.data)`. Both shells (GrAppShell + AppShell) wired. **Review fix (`fb31e9c`):** both shells' `onAppliedChanges` routed through `mosaic.applyRemoteChange()` (was direct `refresh()` — bypassed the isEditingBlock/suppression guards; Phase C's sub-second delivery made the mid-edit clobber likely). iOS `xcodebuild` → **BUILD SUCCEEDED** (SourceKit cross-file errors are the known no-explicit-modules false positives).

### Server→web hub path PROVEN live (Playwright, 2026-05-31) — TWO ways
1. **API-PUT (simulated remote edit):** opened a 2nd WS, PUT a marker via the API → `sawBinaryFrame:true` (Phase A `ws_delta_tx` binary Loro delta) + `gotNoteUpdated:true` (finding-#4 web-invalidation WsEvent).
2. **REAL editor keystroke (closes the prior self-QA gap):** clicked into today's first journal block, vim `A` + typed " EDIT9X7" through the actual CodeMirror engine, Esc → after the 500ms debounced save: `persistedEdit:true` (block id `019e7a50-4404…` preserved — clean in-place edit, no block churn), **`sawBinaryFrame:true`** + **`sawNoteUpdated:true`** on the genuine keystroke edit. So the full Graphite-editor → engine → WS-hub path works on a real edit.
- **⌘K palette:** opens (`gr-cmdk-input` focused), 47 commands over the real registry; typing "daily" fuzzy-filters to "Today's daily note" with per-char match highlighting + footer hints. **0 console errors** across the whole session. Test marker reverted + reload-confirmed disk clean (`- key` restored, no EDIT9X7 anywhere).
- So the Mac-hub→web direction of the milestone is solid end-to-end; only the phone leg is unverified (device test, Roshar offline at staging).

### DEVICE TEST (the user's step — Roshar was offline at staging time)
Server is live on the Phase-A binary: `*:7474`, `/health` 200, `/ws` upgrade → 101, relay bypassed. Device FFI lib current (post-Phase-B, 18:22). To run the test when Roshar is reconnected:
1. (Claude) build+install the `-graphite`-default app on Roshar — Roshar was `unavailable` via devicectl at staging; reconnect it (USB or on-tailnet+awake) and Claude reruns the build/install (the temp `useGraphiteShell→true` flip + xcodebuild id=00008130-000110592698001C + devicectl install, then revert).
2. On the phone: Settings → backend serverURL = `http://100.112.34.59:7474` (Mac's Tailscale IP), HTTP mode.
3. Test: edit a note on the Mac web (`/g`) → should appear on Roshar in <1s; edit on Roshar → appear on web in <1s; concurrent same-note edits converge, no flashing.

### NEXT — Phase D (drafting now, independent of the device test)
Bidirectional reconnect catch-up (each side exchanges per-note `doc_version` VV; the other exports missing ops via `export_doc_update(note, since_vv)`; a reconnecting device with offline edits must PUSH, not just pull) + the 2 deferred relay-path follow-ups (relay re-export uses snapshot not exact bytes; multi-note batch spurious WsEvents) + latency measurement. All in the spec §5 Phase D / §10.

### (superseded) NEXT — Phase C (needs the user for final verification)
iOS `LiveSyncSocket` (`app/Tesela-iOS/Sources/Sync/SyncState.swift`): dispatch on frame type (.string→existing JSON path; .data→decode Loro delta + apply via Phase B FFI `applyDeltaFrame` + refresh affected note, NOT full re-fetch); on local write produce via `produceNoteDelta` + `send(.data)`; point socket at Mac Tailscale `ws://100.112.34.59:7474/ws`; relay ticker stays idle/bypassed. **Acceptance = live device round-trip on Roshar** (sim shares Mac network, hides reachability — `feedback_ios_test_on_device`): Mac web↔Roshar edits <1s both ways, concurrent same-note edits converge no flashing. I build/install/launch + drive Mac side; the user confirms the phone screen. Then **Phase D** = bidirectional reconnect catch-up + the 2 deferred relay follow-ups + latency measurement.

### Server currently running (relay bypassed)
`TESELA_SERVER_BIND=0.0.0.0:7474 tesela-server --mosaic "<real logseq mosaic>"` — standalone local hub, `[sync.relay]` commented in mosaic config.toml (backup `config.toml.relay-bak`). NOTE: this running instance predates Phases A — needs a restart on the new binary before Phase C device testing so the `/ws` delta path is live.

---

## 2026-05-30 — Graphite on the iPhone; relay 413 fixed-in-code then BYPASSED

**Graphite build is installed on Roshar (iPhone 15 Pro).** Built device SDK (signed, fixed FFI), boots straight into Graphite (temp `useGraphiteShell→true` flip during build, reverted in-repo — shipping default stays AppShell). Sim + device both run the redesign.

**Relay 413 — root-caused, fixed in code, then deferred + bypassed.** Testing on the phone surfaced the real bug behind "edits revert on web + iOS": the Mac's outbound relay PUT 413'd (ai-business 1.3 MB note → ~5 MB Loro snapshot ≈ 7 MB wire > HA relay `max_body`), while inbound polling kept applying stale ops over fresh edits.
- **Fixed in code** (`08e941b`, `0c97b92`): relay binary `--max-body` default 1 MiB→16 MiB; client `MAX_RELAY_PLAINTEXT_BYTES` 2.5 MB→8 MiB under a new `RELAY_MAX_BODY_BYTES`=16 MiB invariant (+2 regression tests); first-broadcast ships a compact `ExportMode::Snapshot` not full deleted-history; HA add-on/compose/DOCS deploy defaults → 16 MiB (add-on 0.1.0→0.1.1).
- **HA-add-on gotcha:** the live relay reads `max_body` from the add-on **Configuration tab** (`/data/options.json` via `run.sh`), NOT any shell env — so the user's env-var restart never changed it, and config defaults don't retro-apply to an existing install.
- **DECISION (Taylor, 2026-05-30):** stop patching this relay; redesign it after Loro/RTC (likely need an RTC server/proxy anyway). **Relay BYPASSED for local testing** — `[sync.relay]` commented out in the Mac mosaic `config.toml` (backup `config.toml.relay-bak`); Mac is a standalone local server. Verified a PUT persists + survives the old poll window + hits disk. No cross-device sync while bypassed (fine for single-device Graphite testing). See decisions.md + [project_relay_413_blocks_sync].

---

## 2026-05-29 — Loro cutover FINISHED; redesign is next

**Loro is the sole sync engine.** Flag-day + ai-business dedup + DR drill all done, committed, green. Full report: `phases/2026-05-29-loro-cutover-report.md`.

### Commits this session
- `8ef366e` perf(sync): dedup — store frontmatter-only on root meta (lean snapshots).
- `471d619` refactor(sync)!: delete SqliteEngine/DualEngine/op-wire — Loro-only (~3.6k lines deleted).
- `c626d25` build(ios): regenerate UniFFI bindings for the Loro-only FFI.

### Build status
- `cargo build --workspace` + `cargo test --workspace` → GREEN (0 failures).
- `xcodebuild -scheme Tesela -sdk iphonesimulator` → **BUILD SUCCEEDED** (against the rebuilt `.a` + regenerated bindings).
- `cargo install` of `tesela-server` + `tesela` (flag-day binaries) — see this session's install.

### What the flag-day did
- Deleted `sqlite_engine.rs`, `dual_engine.rs`, `tests/convergence.rs`, `examples/two_node.rs`.
- `SyncEngine` trait = Loro-only (dropped `apply_changes`/`produce_changes_since`/`produce_local_authored_since`/`uses_loro_relay_payload`/`ProducedBatch`). Deleted the v1 op-wire (`encode/decode_op_batch`).
- Server: `main.rs` builds a bare `LoroEngine` unconditionally (no `TESELA_LORO_DUAL_WRITE`/`AUTHORITATIVE`; `TESELA_LORO_RESEED` kept for one-time canonical bootstrap). `sync_relay.rs` = Loro v2 only. Deleted dual-write divergence endpoints (kept `/loro/index`).
- **LAN P2P (peer_sync) data-plane RETIRED** — op-replay is incompatible with Loro + fully redundant with the relay spine; `produce`/`receive_envelope` → 501, daemon = no-op, pairing/discovery stay live. Follow-up: reimplement over the Loro relay-update protocol.
- FFI: `open_loro` is the sole constructor; ticks = Loro v2 only.

### Server launch (CHANGED — no flags)
`tesela-server --mosaic "/Users/tfinklea/Library/Application Support/tesela/logseq"` — Loro is now the default engine. Add `TESELA_LORO_RESEED=1` ONLY for a one-time canonical bootstrap from disk (one device). (No server is currently running.)

### DR drill (validated on an isolated copy — non-destructive)
Restore from `notes/*.md` + `TESELA_LORO_RESEED=1` rebuilds all 514 notes; `/health` 200, `/loro/index` = 514. **Dedup payoff: ai-business snapshot 5.13 MB → 2.58 MB** (now under the 5 MB relay limit). Canonical DR = the `.md` files are truth; `.tesela/loro/` is a derived cache.

## Blockers / open
- **Live data reset is USER-COORDINATED (needs the iPhone).** The dedup's size win lands only on fresh docs; the live mosaic still holds bloated snapshots, so ai-business won't sync until a coordinated reset: stop server → backup → `rm -rf <mosaic>/.tesela/loro/` → boot with `TESELA_LORO_RESEED=1` → **wipe + re-bootstrap the iPhone's local docs** (else fresh-identity docs duplicate against its old docs). Until then the server runs fine on existing docs via the backward-compat fallback (ai-business simply stays unsynced, as before). See the report's "Remaining" section.
- Backlog (unchanged): deferred review findings #7/#8 (slug-rename orphans), #10–18; #111 oplog-order (moot post-flag-day).

## ACTIVE MILESTONE — Graphite redesign (foundation DONE; shell next)
Approved spec: `phases/2026-05-29-graphite-redesign-spec.md`. Foundation plan: `phases/2026-05-29-graphite-foundation-plan.md`. Brand-new web (SvelteKit) + iOS (SwiftUI) frontends to the Graphite design system, reach daily-driver parity, then delete the old. Phasing: foundation → shell → daily-driver views → cutover → iterate. Web + iOS in parallel; shared tokens; REUSE vetted lib logic (CodeMirror editing engine) + Loro FFI/MosaicService. Design source: `.docs/ai/design/graphite/`.

### Foundation phase — LANDED (2026-05-29)
Executed subagent-driven (web + iOS in parallel). Commits: `7083956` (tokens.json + web primitives), `e316a6f` (iOS theme + SwiftUI primitives).
- **Shared tokens:** `.docs/ai/design/graphite/tokens.json` (canonical). Web: `web/src/lib/graphite/tokens.css` (mockup's exact `--*` vars scoped to `.gr-root`). iOS: `.graphite` Theme case in `Sources/DesignSystem/Theme.swift` (reuses existing `@Environment(\.theme)` infra).
- **Primitives (both platforms):** GrIcon (web=`@tabler/icons-svelte` name-map; iOS=Tabler→SF-Symbol map), GrButton (ghost/cta), GrChip, GrTypeDot, GrTypeTag, GrRow, GrWidget. Web in NEW `web/src/routes/g/` tree + `lib/graphite/` (old v4/v5 untouched). iOS in NEW `Sources/Graphite/` (Data/Sync/Generated/Views/Components untouched). Components reference tokens only (theme-swappable, no hardcoded hex bar the on-coral CTA ink).
- **Gates (re-verified by me):** web `svelte-check` clean for graphite (lone error = pre-existing v4 VoiceCaptureButton); iOS `xcodebuild -sdk iphonesimulator` → **BUILD SUCCEEDED**. (IDE SourceKit shows false-positive cross-file errors under this project's no-explicit-modules config — xcodebuild is authoritative.)
- **One deferred check:** visual parity of the `/g` primitives gallery vs the mockup screenshots — not done (user's Chrome held the MCP profile; build/type gates pass). View at `localhost:<webdev>/g`; iOS gallery via `GrGalleryView` `#Preview`. Confirm at shell-phase start.

### Shell phase — LANDED (2026-05-29)
Plan: `phases/2026-05-29-graphite-shell-plan.md`. Executed subagent-driven (web + iOS parallel). Commits: `88e4dfe` (web shell), `c897b98` (iOS shell). **All NEW Graphite presentation bound to EXISTING behavior — no behavior rebuilt.**
- **Web** (`web/src/lib/graphite/shell/`, composed at `/g`; primitives gallery moved to `/g/primitives`): GrTopBar (workspace tabs via `getWorkspace`/`switchTab` + ⌘K bar→`openStation` + connection dot via `getConnected`), GrRail (widget host: Quick-capture→`openColonMode`, Pinned=`getFavorites`, Today=`getRecents`, Tasks=placeholder, +Add-widget stub), GrPane (first-class, focus/side splits-ready, placeholder body), GrStatus (`getVimMode` + breadcrumb + clock), GrCommandPalette (mirrors `Station.svelte` over the real `buildV4Commands` + `scoreFuzzy`), GrLeaderOverlay (mirrors `ChordMenu` over `getLeaderTree`), GraphiteShell (composes + mirrors `v4/+layout` capture-phase keydown Space/⌘K/`:`).
- **iOS** (`Sources/Graphite/Shell/`): GrAppShell mirrors AppShell's native tab bar (4 tabs + `.search` glass circle) bound to the SAME `MockMosaicService`/`RelayTicker`/`CaptureComposer`/`StreamingVoiceRecorder` (full relay bring-up + scenePhase + voice wiring mirrored), forced `.graphite`. GrHeader, GrCaptureBar+GrCaptureSheet (reuse CaptureComposer + `MosaicService.capture`), GrTabPlaceholder. `#Preview` only — NOT the app entry until cutover (`TeselaApp` unchanged).
- **Gates (re-verified):** web `svelte-check` clean for graphite; iOS `xcodebuild` → BUILD SUCCEEDED. Old UI untouched on both.
- **Deferred check:** visual+interaction QA of `/g` (⌘K palette, Space leader) — user's Chrome held the MCP profile. Behavior is reused/mirrored (low risk); confirm at `localhost:5173/g`.
- **⚠ Cutover note:** the web palette/leader/commands reuse behavior modules under `lib/v4/` (`buildV4Commands`) + `lib/v5/` (`leader-tree`) + `lib/stores/`. At cutover, deleting the v4/v5 *UI routes/components* must NOT delete these reused *behavior* modules — separate them (move the reused logic out of the to-delete tree) first.

### Daily-driver views — LANDED + self-QA'd (2026-05-29)
Plan: `phases/2026-05-29-graphite-views-plan.md`. Commits: `6a8cbc3` (web views), `84a4dc0` (iOS views), `562b192` (iOS shell toggle). (A mid-run disk-full crash happened; recovered after the user freed space — both parts then completed + gated.) **Editing engines + data layer 100% reused; only presentation new/re-themed.**
- **Web** (`lib/graphite/`): Graphite CodeMirror theme + decoration CSS (`editor/`) re-skins the REUSED BlockOutliner/JournalView under `.gr-root`; `GrDaily` (wraps JournalView), `GrPage` (BlockOutliner + linked-refs + props, mirrors BufferShell fetch/save), `GrInbox` (chips + cards over `executeQuery`), `GrAgenda` (week grid over `getAgenda`); `GraphiteShell` routes the focused buffer by kind (daily/page/inbox/agenda).
- **iOS** (`Sources/Graphite/Views/`): `GrDailyView`/`GrPageView`/`GrLibraryView`/`GrAgendaView`/`GrInboxView` bind the shared `MockMosaicService` + reuse `BlockRow` untouched; render in GrAppShell's tabs (`.search` = native). `TeselaApp` gained a `-graphite` launch-arg / `tesela.useGraphiteShell` toggle (default = shipping AppShell).
- **Self-QA — web (Playwright, live backend on :7474, real mosaic):** `/g` renders the Graphite shell with REAL data — 9 live reused CodeMirror editors showing actual daily blocks, topbar/rail(4 widgets)/status="NORMAL"; **⌘K opens the palette with 39 real commands (Jump-to/Actions); Space opens the leader with the 6 real `getLeaderTree` chords; 0 console errors.** (Synthetic-keypress typing didn't engage CM through the harness — a Playwright/CM focus quirk, not an app bug; the editor is the byte-identical reused engine.)
- **Self-QA — iOS (simulator):** built for the booted `Tesela-Test` sim, installed, launched with `-graphite` → the Graphite **Today** journal renders (tasks/checkboxes, `#tags`, `[[wiki-link]]`, liquid-glass capture bar, Daily·Agenda·Inbox·Library tab bar + search circle) on seed data. (`simctl` can't tap-test the other tabs; they compiled + are wired — user taps to explore. Real data needs device pairing.)

### Adversarial review + fixes (2026-05-29, `0a6cc30`)
25-agent review (find → per-finding verify) of the whole redesign → 5 confirmed real (rest false positives, several mirroring existing v4 patterns). Verify stage notably caught that a proposed "rollback on save error" fix would itself cause data loss. Fixed: GrPage save failures now surfaced (toast + save-state, keep optimistic content, NO rollback); GrInbox.processAll counts+toasts batch failures (caught applyTriage's `false` stale-block return too); GrAppShell wired the missing **LiveSyncSocket** (instant Mac→app WS push, was ~2s poll) + corrected its overclaiming docstring. **Deferred to cutover:** GrAppShell `MosaicRegistry`/profile-switching (single-profile is fine for testing). Both gates green.

### Running services (I own these; for the user's testing)
- **Backend:** `tesela-server --mosaic "/Users/tfinklea/Library/Application Support/tesela/logseq"` on `127.0.0.1:7474` (flag-free; NO reseed — the live data reset stays deferred). Log: `/tmp/tesela-graphite-test.log`.
- **Web dev:** Vite serving the Graphite app at **http://localhost:5173/g** (proxies `/api`→7474). The user tests the daily flow here.

### NEXT — toward cutover
- **Web parity polish:** JournalView's own day headers render (not the `.gr-dayhdr` markup) — fine functionally; pixel-match later. Confirm editing/save round-trip by typing (reused engine; harness couldn't drive CM). Inbox/agenda are functional over the data layer; refine actions.
- **iOS:** wire GrAppShell's full real-data bring-up to parity with AppShell (onboarding/pairing/registry) so `-graphite` shows live data on a fresh device; tap-test all tabs.
- **Cutover** (when parity holds): delete old v4/v5 web + old iOS Views; make GrAppShell the sole entry; **preserve the reused `lib/v4`+`lib/v5` behavior modules** (move them out of the to-delete UI tree first).

## 2026-06-02 — REAL root cause found: iOS `preferLocalIfNewer` wholesale-body pick defeats block merge

Long live-debug session. Earlier theories (disjoint history, web refetch storm) were REAL but not the whole story. The dominant "new block reverts / iOS never updates" mechanism is **`MockMosaicService.preferLocalIfNewer` (app/Tesela-iOS/Sources/Data/MockMosaicService.swift:1555, called :801 + :938)**: on every HTTP refresh iOS compares its local sandbox `<id>.md` file mtime vs the server's `modified_at`; if local is strictly newer it **discards the server body wholesale and keeps/re-renders the LOCAL body**. That LWW-between-whole-documents defeats block-level CRDT merge:
- Web adds a new block → server has it. iOS's local daily is mtime-newer (it materialized today on bootstrap) → iOS keeps its local body which LACKS the web block → "iOS never updates" + can re-push the stale view.
- Existing-block TEXT edits survive because they arrive as block-level relay ops that merge into the same bid (don't depend on whole-body pick) → "edits show in 1s". This is the asymmetry the user reported.
Added `emit_deletes:false` (notes.rs:956 / diff.rs:81) means the SERVER never deletes the new block — confirmed: a server-side new block survives 24s+ with the phone connected-but-idle. So the loss is CLIENT-side reconciliation, not server deletion.

**Verified live this session:** server PID from worktree `.worktrees/sync-live-debug` (log `/tmp/tesela-server-sync-live-debug.log` — NOT the stale `/tmp/tesela-srv.log`/`tesela-server-fix.log`). Roshar reconnects + bootstraps `/loro/notes/<today>/snapshot` + `/ws 101` on relaunch but goes idle when backgrounded (date rolled to 2026-06-02). Could NOT force the full revert live (phone idle), but traced the mechanism in code + saw `emit_deletes:false` survive a body-omitting PUT during cleanup. Data safe (2026-05-31 = 15960 bytes intact).

**FIX DIRECTION (not yet built):** `preferLocalIfNewer` is the wrong primitive in a CRDT world — iOS should render the MERGED engine (Loro) doc, not pick between whole `.md` bodies by mtime. The legitimate case it protects (offline iOS edit not yet shipped → don't let a stale server refresh clobber it) must be preserved by MERGING (engine holds both sides), not by wholesale local-wins. Surgical option: when local is newer, flush pending engine ops + import server delta + render engine state, instead of returning the local file body. Deeper: switch the iOS display path from HTTP-body to engine-rendered (the delivery-redesign spec already flags HTTP-vs-engine split). Verify on a STABLE connected device (Roshar tends to idle; keep it foregrounded, watch the sub-second GET cadence in the live log before testing). #150 (iOS deltas-not-snapshots) is still pending and related.

## 2026-06-02 (later) — T7 deployed to all 3 devices; concurrent-edit CLOBBER is the last bug

T7 (engine-render + catch-up-on-open) built, reviewed (PASS), deployed to web + Roshar(iPhone) + Sel(iPad), all on the live server (worktree `.worktrees/sync-live-debug`, log `/tmp/tesela-server-sync-live-debug.log`, Mac IP 100.112.34.59 baked into device builds). **Live 3-device test results:**
- Tests 1-4 (sequential web/Roshar/Sel new blocks + web refresh) **WORK** — live propagation + no revert. T7 confirmed good.
- **Test 5 (CONCURRENT same-note edits) = REAL DATA LOSS, web-side.** User observed: web (stale) "came along and clobbered" Roshar+Sel's concurrent edits when web next saved. Server itself converges CORRECTLY (saw `web one`/`Roshar two`/`Sel three ...` all present) — the loss is a stale client OVERWRITING via whole-body write, not server corruption.

**Root mechanism (confirmed in code):** web saves via `PUT /notes/{id}` with FULL markdown `{content}` (api-client.ts:74, JournalView.flushSave:331). Server `update_note`→`record_sync_update` diffs that whole body and emits a `BlockUpsert` for EVERY block in it — including blocks web didn't edit but holds STALE (because peers edited them while web was out of sync). `emit_deletes:false` stops deletions but NOT stale-text re-assert. So a stale whole-document write stamps its view over concurrent edits. Same root disease as the iOS `preferLocalIfNewer` (T7) bug, on the WRITE side. No block-upsert server endpoint exists yet (only recur-bump/set-property/delete_block); `diff_note_trees`→BlockUpsert/Move/Delete machinery does.

**Fix chosen (user, ultracode):** BLOCK-GRANULAR WRITES — clients send only the block(s) actually edited, not the whole note body/snapshot. Kills the whole "stale whole-doc write clobbers concurrent edits" class. Web editor is per-block CodeMirror (knows the changed block); iOS recordNoteDiff already diffs but produceDeltaFrame ships full snapshot (#150). Investigating all 3 write surfaces + deterministic repro before spec.

## 2026-06-02 (eve) — Block-granular writes S0-S2 LANDED + LIVE-VERIFIED: concurrent clobber FIXED

Spec `phases/2026-06-02-block-granular-writes-spec.md`. The last data-loss bug (concurrent same-note edits: a stale client's whole-body write re-asserts blocks it didn't edit → clobbers a peer) is FIXED at the root via block-granular writes. Deterministic repro `crates/tesela-sync/tests/concurrent_whole_body_clobber.rs` (one test documents the bug on the legacy diff, one proves the granular fix).
- **S0 #158** (`86c60c9`): server `POST /notes/{id}/blocks` (upsert_blocks) — applies ONLY the submitted ops via record_local, bypassing the whole-body diff; reuses update_note's full WS fan-out (pre_vv→export_doc_update(Some)→ws_delta_tx + NoteUpdated + reindex + ensure_tag_pages). Reviewed PASS (note_id space = stable_uuid_from_slug, same doc as PUT; integration test proves both-survive). 30 server tests green.
- **S1 #159** (`523b25d` + debounce fix `a7c3924`): web routes in-place TEXT edits (handleBlockChange→upsert) + INDENT (handleIndent/bulkIndent→move) through POST /blocks, NOT buildFullContent→PUT. One-path-per-save invariant held (block-op edits never also fire a PUT; null-op/structural→PUT fallback). Restored 500ms debounce + AbortController cancel (a7c3924 also closed a type-then-Enter double-send window) + flush-on-blur/note-switch/teardown. 175 web tests green.
- **S2 #160** (`c247914`): web re-settle — own-echo note_updated inside the 1500ms window is now DEFERRED (deferredNoteIds) + flushed as targeted ['note',id] at window expiry, so the editing client re-fetches the converged file instead of staying split. Event-driven timer, terminates, won't reseed mid-typing.

**LIVE-VERIFIED (Claude-driven, web Playwright + API peer + live log):** concurrent web-edits-A / peer-edits-B → BOTH survive on server, BOTH directions; web converges to show peer's edit WITHOUT manual nudge (S2 works); 10 keystrokes → 1 coalesced POST (debounce works); web writes via POST /blocks not PUT; 0 console errors. The exact failure the user hit is gone.

**Server now: main-tree binary (has the block endpoint) on :7474, log /tmp/tesela-srv.log** (replaced the old sync-live-debug worktree build). Web has S1/S2 via HMR. iPad (Sel) on T7 build connected; iPhone (Roshar) taken by user.

REMAINING: S3 #161 (web structural insert/merge → block ops; mid-insert ordering caveat — engine appends at end, do NOT re-canonicalize via stale PUT) → fully kills the root class; S4 #162 (iOS produceDeltaFrame real sinceVv — perf, FFI regen). Follow-up: BlockOutliner.svelte:1952 onOut state_unsafe_mutation warning on block teardown (reactivity hygiene, non-blocking).

## 2026-06-02 (night) — Block-granular writes COMPLETE (S0-S4 + delete path + dropped-frame fix)

All 5 stages of `phases/2026-06-02-block-granular-writes-spec.md` landed + reviewed. The concurrent-edit clobber (the last data-loss bug) is killed at the root: NO web edit except frontmatter/title uses the whole-body PUT anymore.
- **S3 #161** (`44b53ee`): web structural edits (handleEnter/NewBlockAbove/PasteBlock→upsert; handleBackspaceMerge→survivor-upsert+absorbed-delete in ONE POST). Mid-note insert = end-append v1 (loss-free, position imperfect; NO re-canonicalizing PUT). 182 tests.
- **delete path** (`547da7d`): handleBackspace/handleDeleteBlock/deleteVisualBlocks → pure {kind:delete} ops, dropped the whole-body PUT + separate DELETE. Local-only deletes send nothing. 192 tests.
- **S4 #162** (`186b03c`): iOS produceDeltaFrame uses real sinceVv (lastPushedVV) → ships delta not full snapshot. Swift-only (FFI already had since_vv). Rust test: delta smaller + converges.
- **S4 review fix** (`16debef`): review caught a dropped-frame self-heal REGRESSION — in hub mode the WS is the SOLE author→hub path (relay tick gated off, HTTP-PUT removed), so optimistically advancing the VV permanently skipped a dropped frame's ops. Fix: sendDelta returns sent?; lastPushedVV advances (commitPushedDelta) ONLY on confirmed send → dropped frame re-ships next produce. Corrected the false "relay tick is a fallback" doc comment. Build SUCCEEDED.

Final review (S3+delete+S4): PASS on all correctness invariants (no double-write, split/merge atomicity, first-push-full-snapshot, builders match wire). cargo test -p tesela-sync -p tesela-server ALL GREEN; web 192 tests; xcodebuild SUCCEEDED.

**iOS NOT yet rebuilt/reinstalled on devices with S4** (Sel still on T7 build; Roshar with user). Web has S1/S2/S3/delete via HMR; server on main-tree binary (:7474, log /tmp/tesela-srv.log). To device-test S4: rebuild+reinstall Sel.

OPEN FOLLOW-UPS: #163 (BlockOutliner:1952 onOut teardown warning), #164 (template-insert + bulk status/tag-cycle still whole-body PUT — residual clobber on bulk multi-block ops), mid-insert engine ordering (order_key/anchor so peers render mid-inserts in place). #156 (Graphite has no backend Settings UI — device builds bake the Mac IP). NONE are data-loss for the common single-block edit path.

## 2026-06-02 (late night) — LAST clobber vector CLOSED: whole-body PUT base-diff (P1+P2)

User's "web was clobbered with 2 editing at once" reproduced + root-caused (workflow, ~10 timing variants): NOT the pending-edit-reseed theory (block-granular writes already fixed in-place/insert/merge/delete — all survived). The LAST vector = the whole-body PUT (frontmatter/title/create/fallbacks) diffing the author body vs the CURRENT server file → a stale author re-asserts a peer's concurrently-edited block = REAL server data loss, masked on web display until reload. Spec: `phases/2026-06-02-put-base-diff-spec.md`. User chose BOTH (server base-diff + eliminate web PUT fallbacks).
- **P1 #166** (`3485878`): `UpdateNoteReq.base_content: Option<String>`; record_sync_update diffs base→new when present (only the author's REAL changes → untouched block never re-asserted → peer edit survives), legacy server-file→new when absent (backward compat). **Resolved the sneaky frontmatter path:** NoteUpsert DOES reseed the block tree from content, so a stale frontmatter edit would clobber — fixed via `body_preserving_noteupsert_content` (author's new frontmatter/page-props + SERVER's current blocks → tree_matches_blocks true → no reseed). 3 tests (both-survive, frontmatter-variant, no-base-still-clobbers). Reviewed PASS.
- **P2 #167** (`6fa18c0`): web sends base_content = `lastExternalBody` (the last server-canonical body reseeded from — NOT lastSentBody which is advanced optimistically) on all 4 PUT paths (JournalView/BufferShell/GrPage/PinnedTabContent flushSave + ensureTrailingEmpty); migrated undo/redo to block-ops diff, null-op/POST-failure fallbacks → PUT-with-base, create stays base-less. 207 web tests, svelte-check clean.

**SIM-VERIFIED (Claude-driven, web Playwright + API peer + live log):** web edit + concurrent STALE whole-body PUT-with-base → BOTH survive on server (the headline); CONTROL without base still clobbers (proves base is the fix + web sends it); frontmatter-edit-concurrent-with-block-edit → both survive; real-UI block-vs-block AND block-edit-vs-whole-body-PUT → both survive. Cleanup verified (8 real blocks byte-identical). **The concurrent-edit data-loss saga is over.**

**iOS does NOT use the whole-body PUT** (writes go through the engine block-granular + relay/WS — confirmed in MockMosaicService comments), so iOS can't trigger this clobber; no iOS change needed for P1/P2. Server restarted on the base-diff binary (:7474, log /tmp/tesela-srv.log). Web has P2 via HMR.

OPEN FOLLOW-UPS: #168 (frontmatter+block bundled in one PUT drops the frontmatter — pre-existing, low sev), #165 (block-property writes still whole-body PUT), #163 (BlockOutliner:1952 teardown warning), mid-insert engine ordering, #156 (Graphite no Settings UI). NONE are concurrent-edit data-loss.

## 2026-06-02 (latest) — Editor polish: split/accretion/teardown fixed (shared root = engine append-at-end)

Product test found 3 editor-lifecycle rough edges (no data loss). Root-caused (workflow + live repro): A (split spawns empties) + B (trailing-empty accretion) SHARE ONE root — the Loro engine appended new blocks at document END ignoring order_key (the long-deferred mid-insert caveat). C (state_unsafe_mutation) independent. All fixed + reviewed:
- **C #163** (`3a6592c`): defer `outlinerHasFocus=false` out of Svelte teardown via queueMicrotask + activeElement re-check (BlockOutliner onOut). Kills the state_unsafe_mutation on every delete/split.
- **B #170** (`bf12d50`): ensureTrailingEmpty now scans ALL bullet lines (bid-stripped) → no append if any empty exists; emits bid-STAMPED `- <!-- bid:uuid -->` so server doesn't re-mint+re-append. New `ensure-trailing-empty.ts` pure helper + 14 tests. Invariant preserved: exactly one focusable trailing empty (both states tested).
- **A #169 + durable B** (`f755cc8`): **engine POSITIONAL insert** — `OpPayload::BlockUpsert.after_block_id: Option<[u8;16]>` (#[serde(default)], backward compat); engine `create_at(Root, predecessor_idx+1)` instead of append; web (handleEnter/afterBidFor) + server diff path derive the predecessor bid. Falls back to append on no-hint/unseen/tombstoned/top-insert (loss-free). **Convergence PROVEN:** positional_insert_concurrent_converges (two devices concurrent adjacent insert → identical order, re-run 5×); all convergence tests green (concurrent_whole_body_clobber, disjoint_history_revert, put_base_diff, snapshot_merge_keeps_local). iOS UNTOUCHED (OpPayload is in-process only post-cutover, off the FFI boundary — gets positional inserts via record_note_diff for free). Reviewed PASS.

**Verified:** cargo test -p tesela-sync -p tesela-server 156 green; web 226 tests; svelte-check clean; cargo build --workspace clean. **LIVE smoke (Claude):** POST /blocks with after_bid=first-block → landed at index 1 (right after first), NOT index 8 (end). Cosmetic ordering bug fixed at engine level on the running server.

**Server now: FIX-A binary on :7474** (positional insert + base-diff), log /tmp/tesela-srv.log. Web has all fixes via HMR. NEXT: rebuild+flash sim/devices (iPad sim 60369457, Roshar, Sel) with the current build for the user's real product test. Remaining follow-ups (all non-data-loss): #165 block-property PUT, #168 bundled-frontmatter-PUT, #156 Graphite Settings UI, write-amplification (in-place edit still emits a whole-note PUT alongside the block POST — investigate).

## 2026-06-02 (final) — WS-PUSH CLOBBER fixed + proven end-to-end (the real last bug)

Caught the user's "phone fights web" clobber ON THE WIRE via temporary gated DIAG middleware (TESELA_DIAG_WRITES, content-logging, REMOVED after). Proof: web POST "Awesome sweet" (HTTP, protected) at 18:00:17; 3s later an iOS WS frame (323 bytes = whole-note SNAPSHOT) reverted it to stale "Awesome" via apply_inbound_delta→apply_relay_updates RAW doc.import — **the WS push path had NONE of the HTTP path's protection.** Every prior fix (base-diff, block-granular, S2) was HTTP-only; iOS syncs over the WS = the unguarded side door. Spec: phases/2026-06-02-ws-push-clobber-spec.md.

Root cause (3 links, code-confirmed): (1) iOS shipped a full SNAPSHOT because lastPushedVV is in-memory + empty on first push/session → sinceVv=nil → ExportMode::Snapshot; (2) iOS's "Awesome" was STALE (hadn't merged web's edit); (3) the real merge mechanism was NOT shared-register LWW but DISJOINT-LINEAGE twins + the non-causal min-TreeID dedup (the E1 tombstone) — the device's stale twin won the dedup. 

FIX C+A (user chose both):
- **C #172** (`6623441`): server WS-apply applies ONLY blocks the peer GENUINELY changed (causal discriminator via `export_json_updates_without_peer_compression` to read the server's per-block text HISTORY + the peer's genuinely-new ops fork_vv\server_vv; apply only values the server NEVER held for that block; disjoint twins force-healed via record_local under server peer/lamport; shared-register defers to Loro LWW). Reviewer PROVED load-bearing: disabled the heal → bug-repro test fails (A reverts); heal on → A kept, genuine B applies. Not inverted. One documented conservative edge: re-typing a block's PRIOR value while concurrent keeps server value (rare, not distinct-content loss).
- **A #173** (`d593c15`): iOS seeds lastPushedVV from the bootstrap base (in bootstrapNoteIfNeeded after importNoteSnapshot + a recordAndPush floor for resident-unbootstrapped) BEFORE recordNoteDiff, so produce always exports a bounded delta, never a stale whole-note snapshot. No under-send (seed captured before the edit). No FFI change.
- **e2e regression test** (`48a735b`): `ws_stale_snapshot_does_not_clobber_http_edit_over_real_socket` in main.rs — a REAL tokio_tungstenite WS client pushes a stale whole-note snapshot at the live /ws; asserts the server keeps "Awesome sweet" + applies genuine B. Proven load-bearing (fails without C: A reverts). This drives the genuine apply_inbound_delta path over a real socket (what the sim couldn't — simctl has no tap, iOS only pushes on local edit).

**Verified:** cargo test -p tesela-sync (112) + -p tesela-server (27 incl. e2e real-socket) all green; ws_apply unit tests (4) green; cargo build --workspace clean; xcodebuild SUCCEEDED. DIAG diagnostics REMOVED, server restarted CLEAN on :7474 (Part C, no diag, log /tmp/tesela-srv.log). Sim has Part A; both physical devices need a rebuild+clean-install with C+A before a device product test.

Limitation honestly noted: the live sim could not PUSH a frame (no tap API), so the wire incident was proven via the e2e real-socket test + unit tests, not a live sim→server push. The e2e test IS the real apply_inbound_delta path.

## 2026-06-02 (cleanup) — last block-edit PUT vectors retired + bundled-frontmatter drop fixed

While devices were disconnected, closed the remaining non-data-loss follow-ups:
- **#165** (`0208e52`): block PROPERTY writes (property-update.ts updateBlockProperty/clearBlockProperty) were whole-body `api.updateNote` (no base_content) — the LAST block-edit whole-body-PUT clobber vector. Now route through block-granular `POST /blocks/set-property` (existing) + NEW `POST /blocks/clear-property` (added `clear_block_property` + `remove_property_in_body`, mirrors set-property's persist tail). Removed the fragile client-side first-line splice (server does locate+upsert/remove). setBlockProperty+clearBlockProperty now recordLocalSave (own-echo). 3 server tests + 226 web green. **Every block edit type (text/indent/insert/split/merge/delete/bulk/template/property) now uses a protected block-granular path — no block edit uses whole-body PUT.**
- **#168** (`4e380d4`): a single PUT changing BOTH a block AND frontmatter silently DROPPED the frontmatter (record_sync_update applied block ops + returned, never checked frontmatter). Now after applying block ops, if frontmatter/page_properties changed (old_tree vs new_tree), emit a body-preserving NoteUpsert (author's new frontmatter + server's CURRENT post-op blocks → tree_matches_blocks true → no reseed). Ordering: block ops first, then NoteUpsert reads the updated body. 2 tests (failing-before/passing-after, + peer-block-preserved). 40 server tests green.

Server restarted clean on :7474 (latest binary: WS-clobber C + #165 + #168), today's note intact (11 blocks).

**REMAINING (all non-data-loss):** device rollout (both physical devices DISCONNECTED — rebuild+clean-install C+A when reconnected; iPad sim already has C+A); #156 Graphite Settings UI; mid-insert ordering edge (top-of-doc insert appends — positional insert covers mid-note); the WS-clobber conservative re-type-prior-value edge (documented, rare); write-amplification (in-place edit emits a block POST — fine, the whole-note PUT is retired). The multi-device data-loss saga is closed at every layer (HTTP base-diff + block-granular + WS-apply protection), proven by unit + engine + e2e-real-socket regression tests.

## 2026-06-02 (Graphite Settings) — #156 DONE: in-app Settings page (no more baked server URL)

Built the full Graphite iOS Settings page (`9737f43`, `Sources/Graphite/Views/GrSettingsView.swift`, 652 lines). Reachable via a gear `GrButton(icon:"settings")` in the GrDailyView header → sheet. GrAppShell now owns `@StateObject MosaicRegistry` (was deferred) + `showSettings` + an `openSettings` environment value. Added a tracked `scheme:` block to `project.yml` (was Xcode-autocreated/untracked) so the regenerated xcodeproj is CLI-buildable.

Sections (Graphite-themed, GrHeader/GrRow/GrButton/GrIcon/theme tokens — NOT a native Form): **SERVER** (Mock/HTTP toggle + URL field + Save&refresh CTA [BackendSettingsView.save logic: set mode/serverURL→attach→refresh] + Disconnect + live connection-status dot from mosaic.connection) — THE #156 fix; **MOSAICS** (registry profiles list + add, seedFromLegacyIfNeeded on appear); **PAIR A DEVICE** (presents existing PairDeviceView sheet — QR camera reused as-is); **SYNC** (RelayTicker status rows + hub chip + device.friendlyName field); **CAPTURE** (default-target + bareDateField pickers); **VOICE** (presents VoiceSettingsView/TranscriptionModelsView sheets); **APPEARANCE** (read-only "Graphite (dark)", picker deferred); **About** (version footer). Heavy existing views REUSED as themed sheets (PairDeviceView, MosaicEditView/AddMosaicView, Voice views); form sections REBUILT native-Graphite. Shipping AppShell + Views/Settings/* untouched.

**Verified (Claude-driven on iPad sim via idb):** built (xcodebuild SUCCEEDED), installed, launched; tapped the header gear (idb ui tap) → Settings sheet opened; idb ui describe-all confirmed ALL sections render with bound live state — SERVER URL field shows http://127.0.0.1:7474, "● Connected" status, Mock/HTTP toggle, "Save & refresh"/"Disconnect", MOSAICS/PAIR/SYNC/CAPTURE/VOICE/APPEARANCE/version all present. The user can now set the server URL in-app — device builds no longer need the IP baked in (#156 root need met). Screenshot rendered (sim display 180°-rotated, cosmetic).

**This means future device installs don't need the temp BackendSettings flag flip** — install the shipping build, open Settings, set the server URL. (For the pending C+A device test, still simplest to bake it, but the page is the real fix.)
