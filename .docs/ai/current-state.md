# Current State

## Branch
- `main` @ `7d2e8682` — **pushed to origin** (2026-06-23). iOS Phase-5 property-registry parity + the post-ship audit fixes all shipped (TestFlight builds 43 / 45 / 46; **46 = working authoring**). `.docs/ai/review/` + `AuthKey_*.p8` untracked (latter gitignored; never commit it).

## Last session — instant cross-device sync SHIPPED + working
- **✅ APNs instant-sync WORKING end-to-end** (#72, iOS build 39, CF Worker). Edit on one device → relay deposit → content-available APNs push → other device catches up in seconds. `wrangler tail` confirmed `registered ✓` on both devices + `push → 200 OK` both directions. Full arc (P1 flush-on-background → P2a BGTask → P3 receiver+token-POST+entitlement+relay push) in `phases/2026-06-18-sync-durability-spec.md`. Builds 31–39.
- **The real blocker was a 413, not APNs**: CF Worker `wrangler.toml` shipped the 1 MiB body cap; bumped `TESELA_RELAY_MAX_BODY` → 16 MiB (`bb94742a`). That unblocked sync AND APNs (the failed tick never reached token registration).
- **Decision: CF Worker is the relay NOW** (`desktop.toml relay_url = https://tesela-relay.finklea.workers.dev`); HA self-host **parked** (Rust `tesela-relay` APNs port + HA add-on wiring are committed + ready — `#74`). Both relays zero-knowledge (adversarially verified). ⚠ APNs host = **production** (TestFlight tokens are prod regardless of the `development` entitlement string).
- Autonomous adds (2026-06-20): **dead-APNs-token prune** on 410/BadDeviceToken (both relays, `dc9a8af5`); **recurrence parser** gained biweekly/fortnightly/quarterly/`every other <unit>`/`every weekday` (`776f1d2a`); **3E code-block rendering confirmed DONE** (roadmap was stale — web+iOS already render fenced code).

## Now / pending
- [ ] **Taylor is testing instant-sync (build 39)** — awaiting his report on the feel / suspended-device wake.
- [ ] **CF Worker `wrangler deploy`** to ship the dead-token prune (NOT urgent — only matters after a device reinstall).
- [ ] **#73 desktop /g live-update deploy** — the #70 fix is committed; needs a Tauri rebuild + `/Applications` swap (Taylor's running-app env).
- [x] **#75 clobber RESOLVED + wake-from-suspend CONFIRMED** (2026-06-21): root cause was **config, not a code bug** — the iPhone was on **SERVER=HTTP** (writing to `127.0.0.1:7474`, a dead address on a real phone → edits silently vanished → device diverged → refresh clobbered local). Toggling to **Relay** fixed it: an iPhone edit appeared on the iPad ~1s after unlock (the APNs silent push woke the suspended iPad — the original 2h-gap scenario, fully closed + real-device validated).
- [x] **Sync-trust hardening SHIPPED** (2026-06-21, the silent-desync trap):
  - **localhost warning** (build 40, `d9e0ee36`): physical device + HTTP→`127.0.0.1`/localhost → loud amber warning + one-tap "Switch to Relay" in Settings.
  - **honest connection status** (build 41, `0b7f2403`): `MockMosaicService.refresh` `.http` catch no longer forces green `.ready` on HTTP failure — an unreachable backend now flips `.failed` ("Can't reach <host> — showing your local copy; changes are saved and will sync") on EVERY refresh, lighting up the ConnectionBanner / TopBar dot / Settings app-wide. Reads stay intact; reconnect loop self-heals. Built understand→implement→adversarial-review (3 lenses) workflows; review caught the "edits won't sync" overclaim (writes ride the relay independently) + banner truncation + dead `userInitiated` param — all fixed. New regression test; 25/25 green.
  - Deferred (acceptable, noted): amber "degraded" vs red "failed" visual split; surfacing write-path (`persistTaskToggle`) failures directly.
- [x] **Recurring-task auto-roll — iOS Relay-mode gap CLOSED** (build 42, `37635a7d`, 2026-06-21). Map finding: roll-on-completion was ALREADY built + working on desktop/web + iOS-HTTP (server `apply_post_save_bumps`); the gap was iOS **Relay** mode (engine path bypasses the server routes where the roll lives). `MockMosaicService.rollRecurringComplete` now rolls locally on completion via `onLocalPropertySet`, mirroring `rewrite_block_for_complete` (status→todo, dates stepped per-field, `last_completed`, `recurrence_done`); spent series keep `done`. Understand→implement→adversarial-review (3 lenses); review caught a BLOCKER (spent never persisted `done` → revert) + MAJOR (`[[...]]` wrapping so CRDT converges with the server) + anchor-fallback/suffix minors — all fixed. Convergence verified safe (sync-apply is pure Loro merge, no double-roll). 29/29 tests. iOS parser also synced with the biweekly/quarterly/every-other Rust additions.
  - Deferred: a fully-atomic multi-key engine op (the roll is 4-5 non-atomic ops; status-first order + tight window make the crash edge rare + recoverable).
- [~] **TYPE SYSTEM — Anytype/Logseq-DB parity (in progress, Taylor: "rock solid before anything else").** Spec `phases/2026-06-22-per-type-property-config-spec.md` (APPROVED; 5 product decisions LOCKED). Surprise from the map: the foundation (types/properties as pages, `extends` inheritance, typed value_types, choices) was ALREADY built; the gap is per-type CONFIG (same global Status → different choices/visibility/default per type) + icons/plurals, then views (deferred — keyboard-first is the hard part there).
  - [x] **Phase 1 — data model + BOTH merge engines** (`914f09ba`, all-Opus workflow). per-type `property_overrides` (REPLACE choices, 3-state `show` on_new/on_set/hidden, default), Tabler icon + plural; migration 006; the merge lives in BOTH the Rust resolver (views/config via GET /types) AND the client-side TS registry (editor seed/hide via property-registry.ts) — they MUST stay in sync. Adversarial review caught + I fixed a hidden_{Prop} engine-divergence (now folded at index time). cargo 346/0, web 490/0 + check clean.
  - [x] **Phase 2 — seed/visibility + icon/plural render** (`0e24349c`, all-Opus workflow). `show==on_new` gates seeding (autoFillNamesForTag/autoFillTagDefaults); `hiddenKeysFor` routes off `show`; built-in Tabler icons + `TABLER_ICONS` + tag-page icon render; `plural` threaded through Rust TypeDefinition→ts-rs→"N Tasks" header. web check 0 / test:unit 495 / cargo 346, all green. Panel found only minors (test hardened).
    - ⚠ **show INLINE visibility is INERT** under Phase 9.9's blanket-hide (all props hidden inline, chevron reveals) — `show`'s real effect is SEEDING + the /p menu. Relaxing 9.9 so `on_new` props show inline is a separate UX call (deferred).
    - ⚠ **Built-in icons/plurals/overrides only seed FRESH mosaics** (`if !path.exists()` in server/lib.rs) — Taylor's LIVE mosaic keeps old emoji/no-plural until Phase 3 config UI sets them or a re-seed. **Product-test needs a fresh mosaic** (or hand-edited tag-page frontmatter).
    - Browser product-test NOT yet run (web dev server down + needs fresh mosaic); code is gate+panel verified. Naive plurals "LifeProjects" (Phase 3 fixable).
  - [x] **Phase 3 — config UI** (`dab11d0f` + fix `a0f083aa`, all-Opus). `TagPropertyConfig.svelte` per-property override editor (choices REPLACE / 3-way show / default), Tabler icon picker, plural field; reads the RAW `property_overrides` map (overridden-vs-inherited, never bakes inherit); writes back as single-line compact-JSON flow YAML via `updateFrontmatterKey` (all-empty → removes the key). New `parsePropertyOverridesRaw`/`serializePropertyOverrides` helpers + round-trip test. **Round-trip verified** against the real gray_matter parser (incl. tricky colon/brace/quote values). Review caught + I fixed a BLOCKER: `updateFrontmatterKey` corrupted frontmatter on any value with `$&`/`` $` ``/`$N` (string replacement-pattern) — now a function replacement + regression test. web check 0 / test:unit 499/0.
  - ✅ **BROWSER PRODUCT-TEST PASSED** (2026-06-22, Opus-driven via Chrome DevTools on a from-source server + fresh mosaic). The Task type page rendered icon `checkbox` + "0 Tasks" plural header; the config UI showed the per-type Status override (choices [todo,doing,done,blocked], show on_new, default todo, reading the RAW override not the global 6); added a "review" choice → it persisted to `task.md` frontmatter `property_overrides` on disk AND the server resolver re-resolved `[todo,doing,done,blocked,review]` (full UI→write→disk→reindex→resolve loop). No config-UI console errors (only pre-existing seeded-widget 404s). **Per-type config is feature-complete + verified on web.**
  - [x] **Phase 4 — per-choice colors + audits** (`9ae5355a`, all-Opus, web-only). `choice_colors: {done:"#hex"}` on a Property page → DisplayChip tints select/multi-select value chips (color-mix, both themes); a swatch/palette picker in TagPropertyConfig writes the global Property page. Defaults-on-create + empty-suppression audits passed clean (no fix). check 0 / test:unit 503. **Scope boundary (not a bug):** the task STATUS marker is intentionally priority-colored (Todoist-style shape=status/color=priority/done=green) — `choice_colors` targets OTHER select chips, not status; unifying the hardcoded status-color surfaces is a deferred follow-up.
  - ✅ **DESKTOP REBUILT + RELAUNCHED** (2026-06-22, closes #73): per-type config (Phases 1-4) is now live in `/Applications/Tesela.app` (web build → `cargo install tauri-cli` → `cargo tauri build` [`.app` OK; DMG step failed but not needed] → quit → rsync `--delete` swap → `open`). `feedback_rebuild_relaunch_tauri` authorized (close/rebuild/relaunch w/o asking).
    - 🐞 **Found + fixed a real CLI bug (`15fe4107`)** while landing it on Taylor's live 540-note Logseq mosaic: his 16 type + 11 property pages resolved to NOTHING (`tag_defs`/`property_defs` = 0, GET /types empty) because **`cmd_reindex` called `upsert_note` (search only), never `index_type_info`**. Fix: `index.reindex()` (= upsert + index_type_info). After `tesela reindex -m logseq`: tag_defs 0→16, property_defs 0→11, GET /types serves all 16. **Pre-existing** (NOT the per-type work — migration 006 additive, caches already 0). His types carry OLD config (global Status, ☑ emoji, no plural/overrides) — config UI now lets him upgrade them.
  - ✅ **Taylor's 15 live type pages UPGRADED to the new format** (2026-06-22, his request "fix all old types to match the new one"). In-place via the note API (PUT per page → normal edit, bids preserved, synced + reindexed — NOT a delete/reimport, no data loss). 6 proper built-ins (Person/Domain/Issue/LifeProject/Ritual/ScheduledItem) got plurals + Person icon→`user`; `project` PROMOTED to full seed Project (folder, Projects, Status [planned,active,shipped], Deadline — note: now Status-tracked); Task already done; 7 Logseq content tags (book/framework/study/talk/flashlight/operating-system/programming-language) got cosmetic icon+plural. Registry only has ~21 Tabler icons so content tags use generic `tag`/`bookmark`/`bulb`. Root Tag left as base (📄). All 15 verified resolving via GET /types. iOS won't render per-type config until Phase 5 (deferred) but receives the synced edits.
  - 📋 **Product-test checklist PUBLISHED** to hdeck (`~/.harness/reports/tesela/20260622-session-features/`): iOS ①②③ (instant sync / sync-trust / recurring roll, build 42, testable now) + desktop ④ (per-type config, after rebuild); approval block `session-results` for Taylor to report back.
  - [ ] Phase 5 (iOS property-registry parity — offline per-type choices on iOS, later).
  - ⚠ `tesela-server put_base_diff.rs` sync tests are pre-existing non-deterministic flaky under the full parallel run (pass isolated + as a file) — not a Phase-1/2 regression.

## iOS Phase 5 — property-registry parity (DONE 2026-06-23; spec `phases/2026-06-23-ios-property-registry-parity-spec.md`)
Trigger: real-device test found 4 iOS task-authoring gaps — raw `status::`/`tags::` lines in the edit buffer, no date authoring, no slash commands, no inline NLP — ALL rooted in iOS being hardcoded + never consuming the property registry. All-Opus ultracode: 3-agent map → spec (6 phases) → per-phase workflows (implement + 3-lens adversarial verify) + builds.
- **P5.1** (`f17ede5f`, build 43): `stripPropertyLines` hides raw `key:: value` lines from the task edit buffer (iOS display strip in `reconcileOpenBlockLive`; reads the raw engine `text_seq` which carries them after a NoteUpsert round-trip). Display-only, no convergence risk.
- **P5.2** (`60c398ff`): `PropertyRegistry.swift` — registry built CLIENT-SIDE from synced Property/Tag pages (nested-YAML `FrontmatterParser`; ports web `getTagPropertyDefs`/`applyOverride` exactly; `@Published propertyRegistry`). Perf: gated the nested parse to type pages (review MAJOR).
- **P5.3+P5.6** (`a46c21f4`): date authoring (`.date` in the default toolbar; typed per-key `onSetProperty` → `setBlockProperty` container seam) + select-chip colors from `choice_colors`. Fixed 2 review MAJORs (date wiring on GrDailyView past rows; web-parity contrast mix for chip text).
- **P5.4+P5.5** (`c3d0066c` + `112251c6`): registry-driven slash commands (`/p1`, `/status <choice>`, `/scheduled` → STRUCTURED writes) + inline NLP lifts (`nl_triggers` + `DateParser`, tightened to valid-choice + type-declared-date-field only).
- Invariant held: every property write goes through the typed container seam (converging) — NO `text_seq` pollution. **Build 45 = the full P5.2→P5.6 batch** (build 44 skipped — a WF3 agent cut a stray build).
- 🔎 **POST-SHIP AUDIT (3-finder → adversarial verify) caught build 45's authoring was SILENTLY BROKEN** + 6 more (`acafce54`, **build 46 = working authoring**): the BLOCKER — every structured property write passed a BARE bid that `setBlockProperty` (needs a `noteId:bid` composite) couldn't resolve, so date/slash/NLP no-opped (swallowed by `try?`). Fixed: composite address at all call sites + `resolveLocalBlockBid` falls back to the in-memory containers for fresh (un-stamped-noteId) blocks (the create-then-configure flow). Also fixed: past-daily/yesterday seams wired, registry rebuild moved off-actor, `editPageBlock` strips property lines, PropertyChip formats via the resolved def (custom date→month-day), live-registry capture for slash/NLP. **Lesson:** the per-phase verify lenses confirmed writes were *structured* but never that the bid *resolved end-to-end* — only the audit caught the integration gap unit tests + review missed.
- DEFERRED follow-ups: (1) engine-side raw-lines strip-and-lift in `reconcile_tree_to_blocks` (the DURABLE root cure for ALL clients) — gated on `migrate_in_text`/fleet-readiness (old FFI that can't read the container could re-broadcast a property erase); (2) NLP bare-weekday-mid-sentence noise tuning; (3) the date+recurrence two-key path has no `.relay` typed-seam branch (uses the whole-list writeback — works but coarse); (4) minor NITs (dead `replaceLength`, full CSS-color parity, ChipIcon SF-Symbol approximations).

## Blockers
- None.

## Open questions / next pick
- **Type-system Phase 2** is the active thread (seed/visibility wiring + icons). Also open: **#73 desktop deploy** (Taylor's env), the web in-place recurring-roll toast. iOS editor track, sync-durability arc, sync-trust hardening, recurring auto-roll all complete.
