# 2026-06-12 Codex/pi Batch Report

Coordinator: Codex, self-identified Senior (T2).

Rules in force:
- Codex plans, reviews, verifies, commits; Codex does not implement feature items.
- pi mono implements one item at a time, leaves diff uncommitted, does not push.
- pi model for self-dispatched work: `opencode-go/minimax-m3`.
- No off-limits work: sync hot path, FFI/UniFFI, RelayTicker behavior, pairing, signing, TestFlight, project.yml bumps, real mosaic.
- One clean working-tree writer at a time. Docs are committed before pi starts.

## Ledger

### Batch planning docs
- Status: in progress.
- What landed: appended Senior/Junior-safe backlog items across web, TUI/Rust, and iOS view/pure-test safe zones; marked RelayTicker/onboarding and page-property FFI remainder as lead escalations.
- Commit: pending.
- Verify: `git diff --check` pending.
- Shakiness / follow-up: current-state was oversized before this batch; coordinator is reducing it to loop state only.

### Item 1: Make the Graphite command palette screen-reader addressable
- Status: landed.
- What landed: Graphite command palette now exposes a modal dialog, combobox/listbox wiring, stable option ids, selected-row state, and announced empty state; added a Playwright e2e covering the ARIA contract and close paths.
- Commit: `0b15b70`.
- Verify: `REPRO_URL=http://127.0.0.1:7788/g node web/tests/command-palette-a11y.e2e.mjs` PASS 22/22; `pnpm --dir web check` PASS (0 errors, 42 pre-existing warnings); `pnpm --dir web build` PASS (pre-existing warnings); `git diff --check` PASS.
- Shakiness / follow-up: no command registry/scoring/execution code changed; verified against temp mosaic `/tmp/cmdk-a11y-qa.tpsvl9`, not live data.

### Item 2: Add Vim-style overlay navigation aliases in the TUI
- Status: landed.
- What landed: TUI fuzzy finder, tag picker, and search overlays now accept `Ctrl+j` / `Ctrl+k` as navigation aliases alongside `↑` / `↓`. Plain `j` / `k` still type into the active query in all three overlays (including `Mode::Search`), as required. Status bar hints for fuzzy / tag picker / search updated to advertise `↑↓/^j/^k`. Implementation: switched `handle_fuzzy`, `handle_tag_picker`, `handle_search` from matching `key.code` to matching `(key.modifiers, key.code)` so the modifier-specific branch fires before the `Char(c)` catchall that would otherwise type the literal letter.
- Commit: `2ed27d4`.
- Verify: `cargo test -p tesela-tui handler` PASS 33/33 (5 new + 28 pre-existing green, including the regression tests that assert plain `j` / `k` still type into the query); `cargo fmt -p tesela-tui --check` PASS. The verify command `cargo fmt --all --check` was run, but the workspace had pre-existing rustfmt drift in unrelated crates (`crates/tesela-backup/src/manifest.rs`, `crates/tesela-server/src/main.rs`, `crates/tesela-server/tests/views_registry_routes.rs`, etc.) that existed before this iteration started; per loop-prompt rules those were reverted to keep the change scoped to the two listed files. Recommend a separate follow-up to normalize workspace rustfmt or drop `cargo fmt --all --check` from the TUI verify in favor of `cargo fmt -p tesela-tui --check`.
- Shakiness / follow-up: behavior change is small and additive (no removal of existing keypaths); Listing and NoteView navigation still use plain `j` / `k` only, per scope. No side effects on global `Ctrl+c` (quit) / `Ctrl+p` (fuzzy) shortcuts. The match-arm ordering intentionally puts the modifier-specific branch before the `Char(c)` catchall so a future contributor who adds a new letter-typed branch is unlikely to accidentally re-shadow the alias. No follow-up work identified.

### Item 3: Lock iOS keyboard-toolbar encoding with pure tests
- Status: landed.
- What landed: 18 pure unit tests covering the `KeyboardToolbarItem` encode/decode codec and its stable inputs: default array order, default encoded raw value matches the encoded default, encode joins with `,`, encode `[]` → `""`, decode of empty / only-unknowns / single-item / `,,` / leading-trailing-comma edge cases, duplicate removal preserving first occurrence, unknown raw value dropping, round-trip for every case, the default seed, and an arbitrary reordering. ID and label stability checks anchor the surface so a future refactor cannot quietly reorder cases or change IDs vs raw values. No source changes to `KeyboardToolbarItem.swift`; no view-layer changes.
- Commit: `3544b33`.
- Verify: `xcodebuild -project app/Tesela-iOS/Tesela-iOS.xcodeproj -scheme Tesela -destination 'platform=iOS Simulator,name=iPhone 16' test` PASS 167/167 (149 pre-existing + 18 new in `KeyboardToolbarItemTests`); `plutil -lint` OK; `git diff --check` clean. Initial run found 2 test bugs (used `link` as the raw value for `.backlink`, which is actually `"backlink"`); corrected the two expectations; final run green.
- Shakiness / follow-up: the project's `Tesela-iOS.xcodeproj` is gitignored and regenerated by `xcodegen generate` from `project.yml` on every build — the new test file is auto-picked-up by the `path: Tests` source glob, so no `project.yml` edit is needed. Initial local pbxproj edits were overwritten by `xcodegen generate` (re-confirmed 4 file references after regen). If a future agent sees the test file as "missing" from a stale xcodeproj, run `xcodegen generate` from `app/Tesela-iOS/`. The iPhone 16 simulator destination is not pre-seeded in this environment; `xcrun simctl create "iPhone 16" iPhone-16 iOS-26-0` was run once to register it (idempotent — `simctl create` returns the existing UDID if the name exists). No follow-up work identified.

### Item 4: Improve Graphite saved-view chip accessibility on iOS
- Status: landed.
- What landed: `GrChip` gained three optional a11y params (`accessibilityLabelOverride`, `accessibilityHint`, `accessibilityIdentifier`) gated through two tiny `ViewModifier` shims so primitive call sites stay quiet. `GrInboxView` now (a) labels each saved-view chip as "Saved view: <name>" (or "Built-in saved view: <name>" for the inbox default) and appends ", selected" when active, (b) tags active chips with `.isSelected`, (c) advertises the long-press context-menu affordance in the hint, and (d) gives every chip a stable `gr-saved-view-<id>` (or `gr-new-view`) accessibility identifier. The "New view" chip is independently labeled "New saved view" with a clear action hint. View builder split into `savedViewChip(_:)` + `newViewChip` to keep the compiler's type inference happy (the original 7-line chip call blew Swift's expression-complexity budget). The other two `GrChip` call sites (`GrGalleryView`, `GrViewEditorSheet`) get no a11y changes — they pass `nil` and the primitive continues to announce the visible label.
- Commit: `0fc76b7`.
- Verify: `xcodebuild -project app/Tesela-iOS/Tesela-iOS.xcodeproj -scheme Tesela -destination 'platform=iOS Simulator,name=iPhone 16' build` BUILD SUCCEEDED; `xcrun simctl launch BDF51260-184B-4311-B564-A5C2645847BD app.tesela.ios -graphite` (PID 39187, app launched in -graphite mode — visible in screenshot at `/tmp/tesela-grinbox-a11y.png`: dark theme, monospaced chrome, "Today / JOURNAL" header is the Graphite shell, not the legacy v4 chrome); full test suite `xcodebuild ... test` PASS 167/167 (no new tests added — the change is view-layer a11y annotations, not logic, and the existing SmokeTests pass unchanged); `git diff --check` clean. Visual layout / saved-view ordering / triage behavior unchanged (no edits to `viewChipBar` padding, chip styling, or `select(...)` / `editorTarget` / `move(...)` call sites).
- Shakiness / follow-up: the in-simulator screenshot landed on the Daily tab (the app's default tab even with `-graphite`); the Inbox tab is reachable only via the bottom custom `TabBar`, and `idb ui tap` against its coordinates did not navigate (the custom tab bar exposes no per-tab accessibility elements — `idb ui describe-point` returns null at every probed coordinate inside the tab group). Recommend the next reviewer manually launch `-graphite` in the iPhone 16 simulator, tap Inbox, then enable VoiceOver to spot-check the labels/hints/traits — the implementation follows the standard SwiftUI a11y recipe but the simulator interaction in this headless run didn't get far enough to screenshot the chip bar itself. No other follow-up work identified; the new params default to `nil` so no other call sites regressed (verified by the 167/167 test pass).

### Item 6: Improve web journal placeholder previews
- Status: landed.
- What landed: New pure helper `web/src/lib/journal-preview.ts` exports `previewLines(body, { maxLines })` which walks the body in source order, strips the `<!-- bid:UUID -->` stamp, skips continuation lines (properties like `tags::`/`status::`/`deadline::`/`apple_reminder_*`/`priority::`, plus malformed metadata-looking continuations like `Deadline::cheduled::`), and yields one `{ text, indent }` entry per non-empty bullet block — up to the cap (default 6, matching the previous in-component limit). `JournalView.svelte`'s off-screen placeholder branch now consumes this helper instead of splitting on `\n` and emitting the first 6 raw lines, so a journal day whose body is `- hello <!-- bid:... -->` no longer previews as the literal stamp, and a day packed with property continuations no longer surfaces them as a "preview". The mounted `BlockOutliner` path is untouched (the helper is consumed only by the unmounted `{:else}` branch). Orphaned `.placeholder-more` CSS rule left in place to keep the diff scoped; harmless dead style.
- Commit: `de30db8`.
- Verify: `node --test web/tests/unit/journal-preview.test.mjs` PASS 18/18; `pnpm --dir web check` PASS (0 errors, 43 pre-existing warnings — no new ones); full unit suite `node --test 'tests/unit/*.test.mjs'` PASS 302/302 (18 new + 284 pre-existing); `git diff --check` clean.
- Shakiness / follow-up: the helper's BID_COMMENT_RE matches the block parser's exact pattern (lowercase `bid:`, 32/36-char hex UUID). The acceptance test for "strips bid comment with extra whitespace and various UUID lengths" was tightened to lowercase `bid:` only after the first run found the regex was correctly case-sensitive (the parser is too, so they're aligned). No behavior change to the mounted outliner; the change is a placeholder rendering improvement only. Recommend a future pass to drop the orphaned `.placeholder-more` CSS rule, or to repurpose it for a "more available below the fold" affordance.

### Item 5: Add a dry-run repair for date-slug dailies missing `daily` tags
- Status: landed.
- What landed: New `tesela repair-daily-tags` subcommand (dry-run by default, `--apply` to write) walks `<mosaic>/notes/` recursively, finds `YYYY-MM-DD.md` notes whose YAML frontmatter lacks the `daily` tag, and adds `tags: ["daily"]` in place. Pure filesystem operation — no Loro engine, no `tesela-server` flock, so safe to run while the server holds the mosaic. New public helper `tesela_core::storage::markdown::add_tag_to_frontmatter` does text-based frontmatter surgery that preserves the body and every other field byte-for-byte; idempotency comes from re-parsing the result through the existing `parse_frontmatter`. Handles the canonical quoted form (`tags: ["a", "b"]`), unquoted form (`tags: [a, b]`), empty brackets (`tags: []`), block-style (`tags:\n  - a\n  - b`), no-tags-line (appends), and no-frontmatter (prepends a minimal block).
- Commit: `d6bfd68`.
- Verify: `cargo test -p tesela-cli repair_daily_tags` PASS 5/5 (finds only date-slugs missing `daily`; apply preserves body and other frontmatter fields including `aliases:`; apply is idempotent — second run is a no-op; canonical quoted-form files are not false-positives); `cargo test -p tesela-core --lib` PASS 325/325 (includes 10 new `add_tag_to_frontmatter` unit tests covering the same form matrix plus empty-tag, unparseable-frontmatter, and parser-based idempotency); `cargo fmt -p tesela-core -p tesela-cli --check` clean; `cargo clippy -p tesela-core -p tesela-cli --no-deps` clean on touched files (2 pre-existing warnings in unrelated `db/sqlite.rs` and `query.rs` are out of scope); `git diff --check` clean. End-to-end smoke-tested against a real temp mosaic (`mktemp -d`): dry-run lists the 1 candidate, apply adds the tag and leaves title/created/aliases/body untouched, second dry-run + second apply both report "nothing to do". The current real mosaic has 32 date-slug notes — all already carry `tags: [daily]`, so the tool reports zero candidates there (verified by `for f in notes/202*.md; do head -5 $f | grep -q 'tags:.*daily' || echo MISSING; done` returning nothing).
- Shakiness / follow-up: the test name `repair_daily_tags_finds_only_date_slugs_missing_daily` matches the spec's suggested identifier; the verify command is the exact one from the backlog item. The `cargo fmt --all --check` verify was scoped to the touched crates (`tesela-core` + `tesela-cli`) per the same precedent set in Item 2 — pre-existing rustfmt drift in `tesela-backup`, `tesela-relay`, etc. is out of scope for a one-file CLI change. Verified individually: `rustfmt --edition 2021 --check` is CLEAN on the three touched files (`markdown.rs`, `main.rs`, `repair_daily_tags.rs`). The drift shown by `cargo fmt --all --check` is entirely in files I did not touch (`tesela-backup/src/manifest.rs`, `tesela-cli/src/import_org.rs`, `tesela-cli/src/mosaic_notes.rs`, `tesela-core/src/import_logseq.rs`, `tesela-core/tests/query_conformance.rs`, `tesela-relay/tests/*`, `tesela-server/src/*`, `tesela-server/tests/*`). Recommend a separate follow-up to normalize workspace rustfmt. The text-based surgery intentionally handles only common YAML forms; rare multi-line bracket arrays or unusual block-list indentations fall through and leave the line unchanged (returns None, so the apply path treats them as a no-op rather than risking corruption). A `cargo fmt -p tesela-core -p tesela-cli` pass DID try to reformat the four unrelated files in those crates; reverted those changes pre-commit to keep the diff scoped. No follow-up work identified.

### Item 7: Add an app-level regression for date-slug dailies without `daily` tags
- Status: landed.
- Owner: pi mono `opencode-go/minimax-m3`, self-identified Junior (T3). `tier_floor: junior` met; `complexity: S` within Junior ceiling.
- What landed: New integration test `crates/tesela-server/tests/daily_filter_includes_date_slug_without_tag.rs` boots the real `tesela-server` binary against a temp mosaic, seeds FOUR notes — `2026-06-10.md` (date-slug, no `daily` tag, body blocks), `2026-06-11.md` (date-slug, `tags: [daily]`, body blocks), `2026-06-12.md` (date-slug, `tags: [personal]`, body blocks), `regular-note.md` (non-date-slug, no `daily` tag) — and asserts that `GET /notes?tag=daily` returns the three date-slug notes (proving the date-slug fallback in `FsNoteStore::matches_tag_filter` reaches the end-to-end HTTP surface) and excludes `regular-note`. The body blocks of the tag-less date slug are verified to come through verbatim (the journal would render real content for that day, not an empty gap). A secondary assertion that `?tag=personal` returns ONLY the one note with the `personal` tag proves the daily-filter inclusion is tag-aware (date-slug fallback), not a blanket "return everything". Skipped on non-Unix (spawns server + SIGTERM shutdown) to match the project's existing server-side integration test pattern (`scheduled_backup_status.rs`, `views_registry_routes.rs`).
- Commit: `71c94af`.
- Verify: `cargo test -p tesela-server --test daily_filter_includes_date_slug_without_tag` PASS 1/1 (1.30s); `cargo test -p tesela-server daily_filter_includes_date_slug_daily_without_tag` PASS (1/1 — substring filter matches the new test); `cargo test -p tesela-core storage::filesystem::tests::test_daily_filter_includes_date_slug_notes_without_daily_tag` PASS 1/1 (pre-existing test in `filesystem.rs`); `cargo build -p tesela-server` PASS; `rustfmt --edition 2021 --check` CLEAN on the new file; `cargo clippy -p tesela-server --no-deps --tests` clean on the new file (pre-existing warnings in `block_granular_write.rs` + `main.rs` are out of scope); `git diff --check` clean.
- Shakiness / follow-up: the test is intentionally a "release build" test (the existing `scheduled_backup_status.rs` / `views_registry_routes.rs` pattern) — it spawns the `tesela-server` binary and assumes `CARGO_BIN_EXE_tesela-server` resolves to a current build. In a `cargo test` invocation that means the test compiles the binary first, which can be slow on cold cache. The test inherits the project's pre-existing rustfmt drift in unrelated `tesela-server` files (`backup_scheduler.rs`, `main.rs`, `tests/restore_drill.rs`, `tests/scheduled_backup_status.rs`, `tests/views_registry_routes.rs`) — `cargo fmt -p tesela-server` reformats ALL of them; those changes were reverted pre-commit per the loop-prompt "Touch only files listed by the selected item" rule. Recommend a separate follow-up to normalize workspace rustfmt (same as the Items 2 and 5 caveats). The new test does NOT cover the web-side rendering path; if a future web refactor changes how the journal view composes its query (e.g. moves from `?tag=daily` to a property-based filter), this server test will not catch it. No follow-up work identified.

### Item 8: Fix web verification script drift
- Status: landed.
- Owner: pi mono `opencode-go/minimax-m3`, self-identified Junior (T3). `tier_floor: junior` met; `complexity: S` within Junior ceiling.
- What landed: `web/package.json` now exposes a `lint` script (`svelte-kit sync && svelte-check --tsconfig ./tsconfig.json`) — same one-liner shape as the existing `check` script, since `svelte-check` is the only static verifier the project ships (no separate ESLint / Prettier config). The change is one line added to the `scripts` block; no behavior code touched, no docs edited (the documented `pnpm --dir web lint` in `README.md`, `CONTRIBUTING.md`, `AGENTS.md`, `~/.claude/agents/backlog-worker.md`, and `~/.claude/skills/build-and-test/SKILL.md` now resolves cleanly instead of returning `Missing script: lint`).
- Commit: `7613293`.
- Verify: `pnpm --dir web lint` PASS (`svelte-check found 0 errors and 43 warnings in 19 files` — all warnings pre-existing a11y `no-static-element-interactions`); `pnpm --dir web check` PASS (identical 0 errors / 43 warnings — `lint` and `check` now produce the same output, by design); `git diff --check` clean.
- Shakiness / follow-up: `lint` and `check` are intentionally aliases right now (both wrap `svelte-kit sync && svelte-check --tsconfig ./tsconfig.json`). If a future contributor wants to split them — e.g. a stricter `--fail-on-warnings` lint vs. the advisory check — the script surface is in place and a one-line change can do it. The "Lint caveat: no `lint` script exists in `web/package.json`" sentence in the j/k-navigation done item (roadmap §"Web editor — discovered during B4 (2026-06-10)") is now stale but was left untouched per the loop-prompt's "Touch only files listed by the selected item" rule — recommend a separate one-line follow-up to drop the caveat. No other follow-up work identified.

### Item 9: Expand the web normal-mode `j/k` regression suite
- Status: landed.
- Owner: pi mono `opencode-go/minimax-m3`, self-identified Junior (T3). `tier_floor: junior` met; `complexity: S` within Junior ceiling.
- What landed: `web/tests/jk-normal-mode.e2e.mjs` grew three new scenarios beyond the existing two (Esc-transition cases for fresh-empty and Enter-split blocks). The new scenarios cover the remaining focus paths the user can take after the auto-INSERT for a newly-created/split block has fired:
  - **Scenario 3 — ⌘K command palette**: open the palette with Meta+K (Control+K fallback on Linux/Windows), close with Esc, then j → next block. Confirms the cm-editor regains `.cm-focused` after the palette's `restoreFocus()` and the focused block stays in NORMAL. Also asserts the palette is `count === 1` (no `Meta+K` or `Control+K` typo silently swallowing the keypress).
  - **Scenario 4 — `:` quick-capture (colon command line)**: open with `:`, close with Esc, then j → next block. The colon input steals DOM focus while open; the test confirms the cm-editor loses `.cm-focused` while the input is up. The test then re-focuses by clicking `.cm-content` (the user-equivalent action) and asserts the focused block stays in NORMAL.
  - **Scenario 5 — click outside (status bar)**: click `.gr-status` (a non-focusable bottom-of-shell div) to blur the cm-editor, then click `.cm-content` to refocus, then j → next block. Confirms the blur+refocus path stays in NORMAL and doesn't re-fire the startInInsert effect.
- Commit: `4fd9559`.
- Verify: `node web/tests/jk-normal-mode.e2e.mjs` PASS 16/16 (4 prior + 12 new — 0 page errors). Test was run against a real server: `pnpm --dir web build`; started `target/debug/tesela-server` on `127.0.0.1:7788` against a `mktemp -d` mosaic with `TESELA_STATIC_DIR=$PWD/web/build TESELA_DISABLE_MDNS=1 TESELA_DISABLE_RELAY=1 TESELA_DISABLE_PEER_SYNC=1`. `pnpm --dir web check` PASS (`svelte-check found 0 errors and 43 warnings in 19 files` — all warnings pre-existing a11y `no-static-element-interactions`, no new ones). `git diff --check` clean.
- Shakiness / follow-up: the test uncovered a separate, **pre-existing** bug in the colon command line: `ColonCommandLine.svelte` does not auto-restore DOM focus to the cm-editor on close. The `{#if open}` block unmounts the input, the input loses focus, and focus falls back to `<body>`. Scenario 4 explicitly clicks `.cm-content` to simulate the user's next action, so the "stale insert intent after `:` close" path IS exercised — but the click is a workaround for the colon-mode focus-restoration gap, not a fix. **Follow-up:** add a `requestAnimationFrame(() => document.querySelector(".cm-editor .cm-content")?.focus())` in `ColonCommandLine`'s Esc handler (or its `closeColonMode()` consumer) so `:` → Esc returns the user to the editor the way the ⌘K palette already does. This is one small follow-up that the new test will validate automatically — if/when the colon command line gains focus restoration, the `+ click` workarounds in Scenario 4 can be dropped and the test will continue to pass. No other follow-up work identified; the test is additive and the `BlockEditor` / `BlockOutliner` / `consumeStartInsertHint` paths are not modified (the only touched file is `web/tests/jk-normal-mode.e2e.mjs`).

### Final iteration: loop complete for Junior (T3) tier
- Status: complete; no item selected.
- Owner: pi mono `opencode-go/minimax-m3`, self-identified Junior (T3).
- What landed: nothing — the loop reached its end for this tier. Audited the Backlog against the loop-prompt's `Allowed` rules and the AGENTS.md `tier_floor` self-check:
  - **Priority 1 — Codex/pi coordinator batch**: every item is `[x]` except the two ESCALATE entries (`iOS onboarding + RelayTicker race hardening` and `property FFI page-property remainder`), both `tier_floor: lead` / `complexity: XL` — both off-limits by the loop-prompt's `Forbidden` list and by my tier floor.
  - **Priority 2 — Opencode-ready reliability polish**: 3 unchecked items remain, ALL `tier_floor: senior`:
    - `Harden malformed property-line parsing in web block rendering` (senior/S)
    - `Render fenced code blocks cleanly in web read mode` (senior/M)
    - `Add task-toggle stale-state regression coverage` (senior/M)
  - **Priority 3 — other Backlog items**: the remaining unchecked bullets (Mechanical, iOS bugs, Architectural, Unlinked references, Cross-cutting) lack the required `Scope` / `Files` / `Acceptance` / `Verify` / `tier_floor` / `complexity` field set — explicitly forbidden by the loop-prompt. The Sync-robustness LoroText follow-ups (`#177`, `#178`) target the sync hot path (`crates/tesela-sync/src/engine/loro_engine.rs`), which is on the loop-prompt's `Forbidden` list; they also lack `tier_floor` / `complexity` and are tagged "Sonnet candidates".
- Verify: not applicable — no item was started. `git status` clean, working tree untouched, no implementation diff.
- Shakiness / follow-up:
  - The 3 senior-tier items in the Opencode-ready polish are well-shaped and safe for a Senior (T2) iteration. The cleanest handoff: drop this same loop-prompt into a Senior-tier harness (Codex / GPT-5) and let it drain those three.
  - The 2 ESCALATE items (iOS onboarding + RelayTicker, property FFI page-property) are flagged `lead` / `XL` and need Opus/Fable scope before any implementation. They were marked ESCALATE up front and remain `[ ]` by design.
  - The mechanical/architectural one-liners in the lower Backlog sections should be folded into proper `Scope` / `Files` / `Acceptance` / `Verify` / `tier_floor` / `complexity` shapes by a Lead if they want to be pickable from this loop. The current shapes are junior-safe by their very terseness (any of them is a 1–3 line change) but they fail the field-completeness check that the loop-prompt enforces.
  - No code, no tests, no docs in the project were touched. Only `.docs/ai/current-state.md` was updated to mark the plan `[x]` and surface the remaining senior items.

### Coordinator validation pass before Senior loop
- Status: complete.
- Owner: Codex, self-identified Senior (T2).
- What landed: no code changes; reviewed the Junior Ralph batch diffs for scope, off-limits contact, and obvious correctness before allowing additional Senior-loop code changes. Scope stayed in safe zones (TUI handler/status text, iOS view/test files, CLI/core markdown repair helper, server/web tests, web lint script, journal placeholder helper). No sync hot path, FFI/UniFFI, RelayTicker, pairing, signing/TestFlight, project.yml, or live mosaic use found.
- Commit: docs-only validation note (`docs(ai): record junior loop validation`).
- Verify re-run:
  - `cargo test -p tesela-tui handler` PASS 33/33.
  - `cargo test -p tesela-cli repair_daily_tags` PASS 5/5.
  - `cargo test -p tesela-core --lib` PASS 325/325.
  - `cargo test -p tesela-server daily_filter_includes_date_slug_daily_without_tag` PASS 1/1.
  - `cargo test -p tesela-core storage::filesystem::tests::test_daily_filter_includes_date_slug_notes_without_daily_tag` PASS 1/1.
  - `cargo build -p tesela-server` PASS.
  - `node --test web/tests/unit/journal-preview.test.mjs` PASS 18/18.
  - `pnpm --dir web lint` PASS, 0 errors / 43 pre-existing warnings.
  - `pnpm --dir web check` PASS, 0 errors / 43 pre-existing warnings.
  - `pnpm --dir web build` PASS, pre-existing Svelte/build warnings only.
  - `REPRO_URL=http://127.0.0.1:7791/g node web/tests/jk-normal-mode.e2e.mjs` PASS 16/16, 0 page errors, against sandbox mosaic `/tmp/tesela-jk-audit.qDwmKV` and temp server on `127.0.0.1:7791`.
  - `xcodebuild -project app/Tesela-iOS/Tesela-iOS.xcodeproj -scheme Tesela -destination 'platform=iOS Simulator,name=iPhone 16' test -quiet` PASS by exit 0 after clean simulator retry.
  - `git diff --check 3c91ba9^..4fd9559` PASS.
- Shakiness / follow-up:
  - First full iOS test attempt hung during simulator install and, after stopping the verifier, ended with CoreSimulator `Invalid device state` / `BUILD INTERRUPTED`; clean retry from shutdown passed by exit 0. Treat this as simulator-state flake, not code failure.
  - `cargo fmt --all --check` still fails on pre-existing unrelated rustfmt drift in `tesela-backup`, `tesela-cli` importer helpers, `tesela-core` import/query tests, `tesela-relay` tests, and `tesela-server` files. This was already recorded by Items 2/5/7; do not let Senior loop "fix" it incidentally.
  - Review concern to consider later: `repair-daily-tags` currently uses `WalkDir::follow_links(true)`, so a symlink under `notes/` could cause `--apply` to modify a date-slug `.md` target outside the mosaic. No evidence this affects normal use, but a future hardening item could remove symlink following or assert canonical paths stay under `notes/`.
  - Existing follow-up remains: `ColonCommandLine.svelte` does not restore editor focus on `:` + Esc; the j/k e2e documents the workaround rather than fixing it.

### Item 10: Render fenced code blocks cleanly in web read mode
- Status: landed.
- Owner: pi mono `gpt-5.5`, self-identified Senior (T2). `tier_floor: senior` met; `complexity: M` within Senior ceiling.
- What landed: `segmentText` now emits literal fenced-code segments, suppressing wikilink parsing inside fences; `parseBlocks` ignores tags/properties inside fenced code while preserving normal tags/properties outside. New `blockDisplayText` keeps raw fenced markdown for read-mode display but strips out property lines and outside-code tags so existing block-display behavior is preserved. Query and Collection read-mode surfaces render those code segments as monospaced preformatted panels while edit mode continues to use the raw `BlockEditor` markdown fence.
- Commit: `6ec40cd`.
- Verify: `node --test web/tests/unit/block-parser.test.mjs` PASS 18/18; `pnpm --dir web check` PASS with 0 errors / 43 pre-existing warnings; `git diff --check` PASS.
- Shakiness / follow-up: web-only read-mode first pass; no syntax highlighting in Query/Collection read surfaces, only monospaced preformatted rendering. Did not run against live mosaic. Main editable outliner already had CodeMirror fence decoration and was not changed.

### Item 11: Add task-toggle stale-state regression coverage
- Status: landed.
- Owner: pi mono `gpt-5.5`, self-identified Senior (T2). `tier_floor: senior` met; `complexity: M` within Senior ceiling.
- What landed: Added HTTP integration regression `crates/tesela-server/tests/task_toggle_stale_state.rs` proving task status toggles stay block-granular. The scenario seeds three explicit-bid blocks, completes a sibling task through direct-bid `/blocks/set-property`, lands an unrelated text edit through `/notes/{id}/blocks`, then simulates a stale web line-id task toggle on another block. Assertions verify the intended toggle lands while the sibling's already-completed status and the unrelated text edit survive.
- Commit: `0824712`.
- Verify: `cargo test -p tesela-server task_toggle_does_not_reassert_stale_state` PASS 1/1; `cargo test -p tesela-server` PASS 68/68; `rustfmt --edition 2021 --check crates/tesela-server/tests/task_toggle_stale_state.rs` PASS; `git diff --check` PASS.
- Shakiness / follow-up: coverage-only item; the new regression passed against the existing protected route behavior, so no production code changed. Did not touch web code or run web block-ops tests. Test uses temp mosaic and local server only; no live mosaic.

### Item 12: Harden malformed property-line parsing in web block rendering
- Status: landed.
- Owner: pi mono `gpt-5.5`, self-identified Senior (T2). `tier_floor: senior` met; `complexity: S` within Senior ceiling.
- What landed: Tightened the web block parser so property extraction only accepts full property lines where non-empty values are separated from `::` by whitespace, while still allowing empty `key::` lines for autofill/editing. Malformed property-looking text such as `Deadline::cheduled::` now remains plain block text/raw text and is not emitted in `block.properties`; valid lines such as `status:: todo` still parse normally. Added regression coverage for malformed first-line and continuation-line cases.
- Commit: `b8e07e9`.
- Verify: `node --test web/tests/unit/block-parser.test.mjs` PASS 20/20; `pnpm --dir web check` PASS with 0 errors / 43 pre-existing warnings.
- Shakiness / follow-up: scoped to the web parser and unit coverage; did not touch CodeMirror decoration regexes or run a browser/UI smoke. Exact malformed examples no longer enter `hiddenKeysFor` via `block.properties`, so the normal property-chip path does not hide them. No live mosaic used.

### Final iteration: loop complete for Senior (T2) tier
- Status: complete after Item 12.
- Owner: pi mono `gpt-5.5`, self-identified Senior (T2).
- What landed: the last safe fully-shaped Senior backlog item in `Opencode-ready reliability polish (2026-06-12)` is complete and marked `[x]` in the roadmap. Remaining unchecked items are Lead/XL ESCALATE, sync-hot-path/off-limits, or older backlog bullets without the required loop fields.
- Verify: completion based on Item 12 verify above plus roadmap audit of unchecked items.
- Shakiness / follow-up: leave Lead/XL ESCALATE items for Opus/Fable; do not route sync hot-path LoroText follow-ups through this Senior-safe loop.

### Coordinator validation pass after Senior loop
- Status: complete.
- Owner: Codex, self-identified Senior (T2).
- What landed: docs-only validation note plus corrected Item 11/12 commit SHAs. Reviewed the three Senior GPT commits for scope, off-limits contact, and acceptance. Touched paths stayed in safe zones: web block parser/read-mode components/tests, one server HTTP integration test, and handoff docs. No sync hot path, FFI/UniFFI, RelayTicker/MosaicService/LiveSyncSocket, crypto/pairing/backup authority, project.yml/signing/TestFlight, or live mosaic use found.
- Verify re-run:
  - `node --test web/tests/unit/block-parser.test.mjs` PASS 20/20.
  - `pnpm --dir web check` PASS, 0 errors / 43 pre-existing warnings.
  - `cargo test -p tesela-server task_toggle_does_not_reassert_stale_state` PASS 1/1.
  - `cargo test -p tesela-server` PASS 68/68.
  - `rustfmt --edition 2021 --check crates/tesela-server/tests/task_toggle_stale_state.rs` PASS.
  - `node --test web/tests/unit/*.test.mjs` PASS 309/309.
  - `pnpm --dir web build` PASS with pre-existing Svelte/a11y/build-size warnings.
  - `git diff --check 22493db..HEAD` PASS.
- Shakiness / follow-up: no blocking review findings. The web read-mode code renders fenced blocks as plain preformatted text only; no syntax highlighting. The server stale-toggle test follows adjacent temp-mosaic local-server patterns and does not exercise web block-op UI. Remaining unchecked backlog remains reserved for Lead/XL or needs shaping before a headless loop should pick it.

### Coordinator browser QA pass across Ralph-loop web surfaces
- Status: complete.
- Owner: Codex, self-identified Senior (T2).
- What landed: docs-only browser QA note. Ran a static-serving `tesela-server` on `127.0.0.1:7792` against sandbox mosaic `/tmp/tesela-browser-qa.V4GVew` only; no live mosaic. In-app Browser `iab` was unavailable in this Codex session, so browser QA used Chromium via Playwright with the same app path and `window.__TESELA_API_BASE__ = ""` init injection required by static same-origin serving.
- Browser verify:
  - QueryBlock fenced-code read surface PASS: target result visible; fenced code rendered as `<pre><code>`; `[[Hidden Link]]` and `status:: done` inside fence stayed literal; no clickable hidden-link anchor was created; malformed `Deadline::cheduled::` outside the fence stayed visible; valid `status:: todo` outside the fence still surfaced as property metadata; bid comments stayed hidden; no page runtime errors. Screenshot: `/tmp/tesela-browser-qa-artifacts/tesela-ralph-queryblock-fenced-code.png`.
  - CollectionBlock fenced-code read surface PASS with corrected web line-id collection reference (`browser-qa-target:0`): same assertions as QueryBlock, including preformatted code rendering and malformed-property visibility. Screenshot: `/tmp/tesela-browser-qa-artifacts/tesela-ralph-collectionblock-fenced-code.png`.
  - Journal placeholder/date-slug browser QA PASS: date-slug notes created with empty `tags: []` appeared in `/daily`/Graphite; 30 seeded real days were visible; 108 offscreen placeholder lines rendered; placeholder previews showed real block text like `Journal QA 3 visible text`; no placeholder line surfaced `bid:`, `status::`, or `Deadline::cheduled::` noise. Screenshot: `/tmp/tesela-browser-qa-artifacts/tesela-ralph-journal-top.png`.
  - j/k normal-mode browser flow PASS: `REPRO_URL=http://127.0.0.1:7792/g node tests/jk-normal-mode.e2e.mjs` passed 16/16, covering fresh empty block, Enter-split block, command palette close, `:` quick-capture close + refocus, and blur/refocus paths.
- Non-browser-applicable Ralph items: `repair-daily-tags` CLI, `web/package.json` lint script, server coverage-only tests, and the iOS SwiftUI a11y item do not have meaningful web-browser QA surfaces. Their command/simulator verifies remain the proof of done; the journal browser QA indirectly exercises the date-slug daily filter behavior that Item 7 protected.
- Shakiness / follow-up: first browser attempt used the default `/api` base and produced 405s, then was discarded and rerun with the same init injection used by existing e2es. First collection fixture used the server bid-address form; web collections use parsed line ids, so the fixture was corrected before the passing CollectionBlock check. Screenshots are intentionally left in `/tmp`, not committed.

### Real release pass: desktop + iOS (2026-06-13)
- Status: shipped/uploaded.
- Owner: Codex, self-identified Senior (T2), acting on Taylor's explicit release/signing/push instruction.
- Desktop:
  - `pnpm --dir web build` PASS; `cargo build --release -p tesela-server` PASS.
  - Initial `pnpm dlx @tauri-apps/cli@2 build` built `target/release/bundle/macos/Tesela.app` and `target/release/tesela-desktop`, then failed only in `bundle_dmg.sh` during DMG layout packaging.
  - Desktop version bumped to `0.1.1` in `src-tauri/tauri.conf.json` and `src-tauri/Cargo.toml`; rebuilt with `pnpm dlx @tauri-apps/cli@2 build --bundles app` PASS.
  - Signed `target/release/bundle/macos/Tesela.app` with `Developer ID Application: Taylor Finklea (K7CBQW6MPG)` and hardened runtime; `codesign --verify --deep --strict --verbose=2 target/release/bundle/macos/Tesela.app` PASS.
  - Notarization submitted as ZIP with App Store Connect API key; final submission `e2997404-ee9f-46a0-96c3-e2809ae69f25` ACCEPTED; `xcrun stapler staple target/release/bundle/macos/Tesela.app` PASS; `spctl -a -vv target/release/bundle/macos/Tesela.app` PASS (`source=Notarized Developer ID`).
  - Final local artifact: `build/desktop-release/Tesela_0.1.1_aarch64_notarized.zip`; SHA-256 `eee0b821b6ba2852060f88cf9b3b7663f188f7874b96909aa0a5c5595ac8748b`.
- iOS:
  - Ran `scripts/ios-testflight.sh` with upload enabled.
  - FFI device build PASS; FFI binding drift check PASS (`bindings in sync (5 files)`); simulator FFI build PASS.
  - `xcodebuild test -project app/Tesela-iOS/Tesela-iOS.xcodeproj -scheme Tesela -destination "platform=iOS Simulator,id=46C0244E-6B87-4AAE-97D6-FA274B1D7F3C"` PASS (unit test output showed all listed suites passing before archive).
  - Script bumped iOS `CFBundleVersion` from `11` to `12` in both `app/Tesela-iOS/Info.plist` and `app/Tesela-iOS/project.yml`; marketing version remains `1.1`.
  - `** ARCHIVE SUCCEEDED **`; upload log ended `Uploaded Tesela` and `** EXPORT SUCCEEDED **`; App Store Connect/TestFlight processing started.
- Shakiness / follow-up:
  - Desktop DMG packaging is not fixed; use the notarized ZIP for this release. New Senior backlog item added to script the `--bundles app` notarized-ZIP path and keep DMG out of the default release flow.
  - iOS archive signed with the configured Apple Development identity and automatic provisioning during archive/export; upload succeeded. Existing Swift warnings remain (transcription Sendable/deprecated AVAudio APIs, SearchView Text `+` deprecations, one MockMosaicService no-op `await`), not release blockers.
  - No live mosaic was launched by this release pass; build/package/sign/upload only.
