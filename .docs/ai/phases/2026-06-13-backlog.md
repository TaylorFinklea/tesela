# Active Backlog — 2026-06-13 (Opus triage, post head-to-head)

Source: 5-agent triage reconciling the 2026-06-09 bug-bash (91 findings) + `2026-06-13-opus-return-report.md` + recent Codex/fleet commits against the live code.

**Context resets this doc corrects:**
- Stream A (relay hardening A1–A14) + Stream B (Graphite cutover) are **SHIPPED** — roadmap "Now" is historical.
- Command-registry B1–B4 **merged** (`4766111`) — gpt-5.5 won the 5-model blind head-to-head.
- The 3 properties product-decisions were **already resolved 2026-06-05**; Phase 1 (13 steps) fully shipped → properties is unblocked, NOT waiting on Taylor.
- iOS user-reported bugs (collapse / older-date edit / Views vanish / tags / bid-as-text) are **fixed through TestFlight build 12** — confirm, don't rebuild.

**Routing (evidence-based, `model-scorecard.md`):** gpt-5.5 + minimax = co-lead workhorses for S/M (prefer minimax while its 3× grace lasts, else gpt-5.5). kimi = avoid (3 durable-output failures). Opus = Lead/XL + review/score of every batch. Each item: `tier_floor · complexity · status`. Verify = exact gate. `cd /Users/tfinklea/git/tesela` for all commands.

---

## FLEET-READY — dispatch now (verified-open, no spec dependency)

### Keyboard-first command spine
- [ ] **B5 · keymap-visible** (junior·S·open) — `:keymap` currently `console.log`s; render `formatKeymap()` in a UI panel. Files: `web/src/lib/v4/commands.ts` (~694), `command-registry.svelte.ts` (formatKeymap), `stores/fullscreen-overlay.svelte.ts`, `components/shell/FullscreenOverlay.svelte`. Verify: `pnpm --dir web check` (then human: ':keymap' renders a panel).
- [ ] **B6 · slash-double-onChange** (senior·S·open) — applySlash double-fires onChange on non-Loro blocks (view.dispatch triggers updateListener AND explicit onChange). Guard with `localApplyInProgress` around applySlash's view.dispatch + the query/widget/date/upsert early-returns. File: `web/src/lib/components/BlockEditor.svelte` (~1043–1213, 846–898, 1868). Verify: `node --test web/tests/unit/command-registry.test.mjs && pnpm --dir web check` (human: one PUT not two).

### iOS stability
- [x] **IOS1 · onboarding-gate** (senior·M · DONE wave5, gpt-5.5 5/5) — `@AppStorage("onboardingComplete")` if/else gate on GrAppShell body; reuses OnboardingView (Graphite-themed). xcodebuild green in main.
- [x] **IOS2 · relay-reconnect** (junior·S · DONE wave5, minimax 5/5) — `guard case .http` → `guard currentBackend != .mock`; .relay now reconnects. xcodebuild green.
- [x] **IOS3 · relayTicker-double-start** (junior·S · DONE wave5, minimax 5/5) — consolidated to the single post-bootstrap `relayTicker.start()`. xcodebuild green.

### Desktop (Tauri / `src-tauri`, crate `tesela-desktop`)
- [x] **DSK1 · main.rs audit fixes** (senior·S · DONE `a4f81b03`, gpt-5.5 5/5) — 3 bugs in `src-tauri/src/main.rs`: (a) Reopen handler force-reloads even when a window is visible → drops editor state; guard on `has_visible_windows` (~329); (b) spawn_server relay opt-in never clears an inherited `TESELA_DISABLE_RELAY` → `cmd.env_remove` it in the Some(url) arm (~210); (c) `desktop_config_value` doesn't strip trailing `# comment` from TOML values → panic/disable (~146). Verify: `cargo build -p tesela-desktop`.
- [ ] **DSK2 · wait_for_port child-exit** (junior·S·open) — poll `child.try_wait()` in the connect loop to fail fast on flock conflict instead of a 20s hang. File: `src-tauri/src/main.rs` (~170,248). Verify: `cargo build -p tesela-desktop`.
- [ ] **DSK3 · SIGTERM deadline** (junior·S·open) — extend exit deadline 5s→30s so auto-backup-on-quit (VACUUM INTO + validate) completes; add a log line. File: `src-tauri/src/main.rs` (~349). Verify: `cargo build -p tesela-desktop`.
- [ ] **DSK4 · restart-respawn watchdog env** (senior·M·open) — respawned server inherits `TESELA_EXIT_WITH_PARENT`/`TESELA_PARENT_PID` → watchdog kills it (ppid becomes init). `env_remove` both before re-exec. File: `crates/tesela-server/src/routes/data_ops.rs` (~1264; read `src/main.rs` ~677–706 first). Verify: `cargo build -p tesela-server` (human: Settings→Restart Server reconnects).
- [x] **DSK5 · single-instance** (senior·S · DONE `9aec7bc9`, minimax 5/5) — `tauri-plugin-single-instance` as first plugin, focus-existing on 2nd launch (#202).
- [x] **DSK6 · desktop-release.sh** (senior·M · DONE `c4820230`, gpt-5.5 5/5) — codesign/notarize/staple/zip script; `--skip-notarize` exits 0 with no app/identity/creds; mirrors ios-testflight.sh conventions.
- [x] **DSK7 · system-tray** (senior·M · DONE `58e974bb`, gpt-5.5 5/5) — Tauri v2 TrayIconBuilder show/hide/quit; CloseRequested→prevent_close()+hide(); Exit-reap intact.

### Editor & rendering
- [ ] **ED1 · gfm-table-render** (senior·M·open) — pipe-table CM6 decoration when unfocused; tags/wikilinks must NOT parse inside cells (extend `literal()` guard). File: `web/src/lib/cm-decorations.ts` (mirror MD_HEADING_RE ~729). Verify: `pnpm --dir web check && node --test web/tests/unit/block-parser.test.mjs`.
- [x] **ED2 · ios-heading-italic** (junior·S · DONE wave5, gpt-5.5 5/5) — ATX `#/##/###` sizing + `*italic*` (bold-before-italic; heading gated to segment 0, code untouched). xcodebuild green.

### Properties & types
- [x] **PROP1 · stale-type-defs** (junior·S · DONE `7390af30`, minimax 4.5/5) — `remove_note` leaves `tag_defs`/`property_defs` (FK SET NULL); add explicit DELETE by note_id + change FK to CASCADE (new migration). Add a regression test. Files: `crates/tesela-core/src/db/sqlite.rs` (~180), `schema.rs` (~59). Verify: `cargo test -p tesela-core`.
- [x] **PROP2 · set_block_property atomicity** (senior·S · DONE `9f14b254`, gpt-5.5 5/5) — reordered container-write-before-strip; real failure-injection test (mock SyncEngine) asserts in-text line survives a failed write.
- [ ] **PROP3 · wire-TagPropertyConfig** (junior·S·open) — mount the orphaned `TagPropertyConfig.svelte` (already complete) in `TagPageRenderer.svelte` (has tagName+noteId). Verify: `pnpm --dir web check && pnpm --dir web build` (human: Tag page shows config panel).
- [x] **PROP4 · properties-editable** (senior·S · DONE `b1090353`, minimax 4.5/5) — block props click-to-edit via `api.setBlockProperty`/`clearBlockProperty`; page props left read-only (no clean frontmatter endpoint — that's a follow-up if wanted).
- [x] **PROP5 · new-entity-guard** (senior·M · DONE `45d834cd`, gpt-5.5 5/5 — wave5 re-dispatch) — NewEntityGuard popover + `findStrongFuzzyMatch` + pref toggle (default OFF/opt-in) on tag/link/`/p`-select commit. Toggle-OFF = no regression (traced). <!-- human-QA: enable the toggle in Settings → type a typo tag/link near an existing one → "Did you mean?" (Enter=use existing, Esc=create); minor: settings knob uses translate-x-4.5 (verify it slides). -->
- [ ] **PROP6 · tag-default-autofill** (senior·S·open) — on #tag add, emit `BlockPropertySet` with the registry `default_value` (via `get_resolved_tag_def`) instead of empty `key::` lines; idempotent. Files: `block-tags.ts` (~131), `BlockOutliner.svelte`. Verify: `pnpm --dir web check && cargo test -p tesela-server`.

---

## LEAD — Opus owns (specs + architecture + sync)

- [~] **L1 · sync: minimum key/pairing + cursor migration** (lead·L · CORE DONE 2026-06-13) — spec `phases/2026-06-13-l1-sync-spec.md`. **Scope inverted: crypto spine + seq-fix + A5 cursor hardening were ALREADY shipped** — L1 = a plumbing bug + fences + a seam, not a re-architecture. DONE: **PV** (`693dd9a0` joiner persists relay_url → joins the spine — the real fix), **CT** (`a8c46e36` seq-black-hole regression fence), **GKS** (`8582be46` GroupKeyStore seam). Decisions: defer Ed25519 identity + key-rotation; keychain behind file-fallback.
  - [ ] **L1-FFI** (senior·S, fast-follow) — iOS `encode_pairing_code` threads `relay_url` (`tesela-sync-ffi/src/lib.rs:222/249`) + binding regen + Swift caller. Verify: `cargo test -p tesela-sync-ffi encode_pairing_code_includes_relay_url` + iOS build.
  - [ ] **L1-KS** (senior·M, GATED — live-data hazard) — `KeychainGroupKeyStore` (security-framework, macOS/iOS) behind `tesela_keychain` + file-fallback. **Never delete `group_key.bin` until ALL devices keychain-aware (Taylor's operational call).** Verify: `cargo test -p tesela-sync keychain_store_round_trips --features tesela_keychain`.
  - [ ] **L1-OPS** (Taylor — HARD gates on the HA relay): `--admin-token` SET · `TESELA_RELAY_MAX_BODY=16777216` · relay URL = Tailscale 100.x/WAN never LAN · relay 0.2.x w/ snapshot endpoints. Unset = silent spine break.
- [x] **L2 · spec: slash-as-registry-dispatcher** (lead·L · DONE 2026-06-13) — contract LOCKED: verb-only `SlashContext`, additive `run(arg?, ctx?)` widening, `surface:'editor'` gating, gut-don't-delete migration + grep completion gate. Spec: `phases/2026-06-13-slash-registry-spec.md`. Decomposed into B-impl-1..4 (below). 4 keyboard-first open questions for Taylor in the spec (non-blocking; defaults baked in).
- [x] **L3 · spec: user-rebindable keybindings** (lead·M · DONE 2026-06-14) — spec `phases/2026-06-14-l3-rebindable-keys-spec.md`. Registry-native effective-binding resolution + sparse per-command `BindingOverride` (NOT a keymap-document). Decisions: instant capture, vim OUT, client-local, warn-and-allow. Decomposed → KB1-4:
  - [~] **KB1** (senior·M) — override store + `eventToShortcutGlyph` + `effective*`/`resolveShortcut`/`checkRebind` + `buildKeymapIndex(overrides)`; fix the `scratch` "Space n s" outlier. NO dispatch. **Dispatched wave11 (Sonnet vs Qwen).** Verify: `pnpm --dir web check && node --test web/tests/unit/{shortcut-glyph,keybindings,command-registry}.test.mjs`.
  - [x] **KB2** (senior·L · DONE 2026-06-14, Sonnet-via-Agent 5/5) — `GraphiteShell.onKey` ⌘-ladder → `resolveShortcut(e, commandCtx, keybindings.snapshot())` (call-time read); peek/command-station runs reconciled to match the ladder first. Verified byte-identical by code-equivalence + KB1 unit tests. **LIVE DevTools QA PASSED (2026-06-14):** ⌘K station / ⌘I peek-toggle (open+close) / ⌘\ vsplit / ⌘G graph all byte-identical, AND a rebind works end-to-end (override `fullscreen-graph`→⌘J: ⌘J opened the graph, ⌘G went inert).
  - [ ] **DEV-STACK** (Taylor/next session) — vite (:5173) + the web's tesela-server backend died across the session boundary; stale tesela-server procs (42979/68365, ambiguous mosaics) linger. Restart cleanly (`--mosaic ~/Library/Application Support/tesela/logseq`) to resume browser QA.
  - [ ] **KB3** (senior·M) — `effectiveChord` into leader tree + effective badges in palette/colon.
  - [ ] **KB4** (senior·M) — settings UI: registry-driven rebindable section (capture/reset/⚠).
- [ ] **L4 · spec: lib-embed serve()** (lead·L) — `tesela-server` as a linkable lib for in-process Tauri embed (tokio runtime ownership, flock, shutdown signal, retires the watchdog). Output: spec.
- [ ] **L5 · spec: property query DSL typed comparisons (Phase 4)** (lead·M) — plumb PropertyRegistry into `filter_matches`; TS `query-language.ts` parity. Output: spec + junior TS-mirror sub-item.
- [ ] **L6 · iOS real-data smoke** (lead·M) — sim portion I drive via idb (pair → edit round-trip → Views populate from real notes); Roshar reachability = Taylor.
- [x] **ED1-fix · GFM table render via StateField** (lead·M · DONE wave10, **Sonnet 4.6 5/5** — 3-way head-to-head winner vs Qwen 3.7 Max + kimi/openrouter) — table block-replace now comes from a `StateField` (`teselaTableDecorations` via `EditorView.decorations.from`), NOT the ViewPlugin; focus-gated via a deferred-dispatch `tableFocusTracker`; tags/wikilinks suppressed in cells; +23 tests. **Runtime QA PASSED:** unfocused pipe table renders as an HTML widget with ZERO console errors (the original runtime throw is gone).

---

## HUMAN-VERIFY — Taylor (likely-fixed; confirm, don't rebuild)

- [ ] **H1** — colon `:` opens in Journal NORMAL mode (real browser, not CDP). If broken → fix `getVimMode()` stale-INSERT after blur.
- [ ] **H2** — web `# heading` renders sized when unfocused (real browser; likely already works since 22b04de5).
- [ ] **H3** — iOS build 12 on Roshar: collapse / older-date edit / Views populate / tags-as-chips / no bid-text (commits 468b953d, 6be61b67, 30520ad9).
- [ ] **H4** — NORMAL-mode Enter no-split / Backspace no-merge. Auto: `node web/tests/jk-normal-mode.e2e.mjs`.

---

## SLASH-REGISTRY — L2 spec done, UNBLOCKED (sequential chain, senior, one at a time)
Spec: `phases/2026-06-13-slash-registry-spec.md`. B-impl-2 depends on -1, -3 on -2, -4 on -3 — NOT a parallel-fleet batch.
- [x] **B-impl-1 · widen registry types + thread ctx** (senior·M · DONE wave6, gpt-5.5 5/5) — `editor?`/`surface`/`slashKey`/`'editor'`-category added to `Command`+`CommandContext`+`V4Command` dup; `run`→`(arg?, ctx?)`; `surface` filter in `available()`; ctx threaded through colon/palette/leader; `slash-context.ts` (SlashContext type) created. Purely additive; 8 registry tests green.
- [x] **B-impl-2 · buildSlashContext() producer** (senior·L · DONE wave7, gpt-5.5 5/5 — bundled w/ B-impl-3) — verb-only `buildSlashContext()` wrapping inline code through `dispatchWithLocalApplyGuard`; no `view` exposed; live `view.state` re-read on commit paths; pickers encapsulate their `$state`.
- [x] **B-impl-3 · port slash tree + 2 pilot verbs** (senior·M · DONE wave7, gpt-5.5 5/5) — `getSlashTree()` merges registry leaves deduped by `slashKey` ahead of legacy; `editor.heading`/`editor.date` registered, legacy cases gutted to `return;`. Heading caret proven byte-identical; equivalence test added. **Runtime self-QA PASSED (2026-06-13, real browser):** slash menu shows heading+date first (no dupes), 11 legacy verbs intact; /heading→`# `; /date→picker; editor.date filtered from ⌘K, editor.heading present; zero console errors.
- [x] **B-impl-4a · migrate 6 flat verbs** (senior·L · DONE wave8, Sonnet 4.6 5/5) — task/tag/link/query/collection/template → registry commands (`web/src/lib/editor/commands/*.ts`), applySlash cases gutted to `return;` (masked by slashKey dedup). Mappings verified byte-identical to legacy. **Runtime QA PASSED:** /task renders Task chip+checkbox (addTag), no console errors. widget/property/`/p`/`/s` + switch intact.
- [x] **B-impl-4b · migrate property system + delete switch** (senior·L · DONE wave9, **Sonnet 4.6 5/5** — head-to-head winner vs kimi) — widget/property → registry; `/p`-manual → `findByVerb('property')`; `/s` status → `setProperty` (structured); `legacyBuiltins` dropped; **`applySlash` DELETED**; guard test updated + regression test added. grep gate green; runtime QA PASSED (/s>Todo sets status, menu renders post-deletion). **🎉 NORTH-STAR #1 COMPLETE: `/` slash, `:` colon, ⌘K, and Space-leader all dispatch through one `commandRegistry`.** (kimi-k2.7-code = zero-diff non-attempt — see scorecard.)

## BLOCKED — need a Lead spec first
- [ ] **PROP-MIGRATE · enable `TESELA_LORO_MIGRATE_IN_TEXT`** (lead·S) — gated on confirming ALL devices ≥ build 12 (no old FFI re-broadcasting in-text props). Operational flip + ai-business smoke.

---

## Dispatch log (Opus reviews + scores each → `model-bench.jsonl`)
**Review routine (proven 2x):** read `.bench/<wave>/logs/_summary.txt`; for each item read `<name>.diff.patch`; `git apply --check` then `git apply` on main; run its Verify; commit per-item with provenance (model + score); `git worktree remove --force .worktrees/<wave>-<name>`; append a `source:fleet-dispatch` row to `model-bench.jsonl`.

- **Wave 1** — DONE+scored: DSK1→gpt-5.5 (5/5 `a4f81b03`), PROP1→minimax (4.5/5 `7390af30`).
- **Wave 2** — DONE+scored (all merged): DSK2/3→minimax (5/5 `b8952ebf`), DSK4→gpt-5.5 (5/5 `754bd7ba`), B5→minimax (4.5/5 `ee78107f`), PROP3→minimax (4.5/5 `47c03aea`).
- **Wave 3** — DONE: B6→gpt-5.5 (5/5 `89100bd6`), PROP6→minimax (4.5/5 `555472fa`) merged + scored. **ED1→minimax (3.0) NOT MERGED** — runtime bug: multi-line `Decoration.replace` from the `teselaDecorations` ViewPlugin, which CM6 forbids (line-break-replacing decorations must come from a StateField). Type-check + pure-fn tests passed and missed it. Salvageable parts preserved in `.bench/wave3/logs/ed1.diff.patch` → see **ED1-fix (Lead)**.
- **iOS wave** (TODO): IOS1/2/3 + ED2 — need a submodule-aware worktree (`git submodule update --init` in the worktree before xcodebuild) OR run serially in the main checkout.
- **Remaining fleet-ready** (later waves): DSK5 (single-instance), DSK6 (release.sh), DSK7 (tray), PROP2 (set_block_property atomicity), PROP4 (properties-editable), PROP5 (new-entity-guard).
